<?xml version='1.0' encoding='utf-8'?>
<webwork-reps version="2" webwork2_major_version="2" webwork2_minor_version="21" xml:id="extracted-airplane-descent-linear-model" assembly-id="airplane-descent-linear-model">
  <static seed="585">
    <statement><p>An airplane was cruising at <m>{39000\ {\rm ft}}\text{,}</m> and then began its descent. Because of some air traffic congestion, it will descend to <m>{8900\ {\rm ft}}</m> and then circle the airport for a while. It descends at a rate of <m>{870\ {\textstyle\frac{\rm\mathstrut ft}{\rm\mathstrut min}}}\text{.}</m> A passenger notices the plane is still descending. Set up an inequality where the solution set represents how much time might have passed since the plane started its descent. There is no need to solve it.</p>
<p><fillin characters="20" name="AnSwEr0001"/></p>
</statement>
    <answer>
      <p>
        <m>39000-870t &gt; 8900</m>
      </p>
    </answer>
    <solution><p>This is a rate exercise, and we can use
<md>(\text{initial value}) \pm (\text{rate})\cdot\text{variable} \mathrel{?} (\text{final value})</md>
The question mark in the above is not an equal sign, because we are trying to set up an inequality. The initial value for the altitude is <m>39000\text{,}</m> the altitude is decreasing (so we will subtract, not add), the rate is <m>870\text{,}</m> and the final altitude is <m>8900\text{.}</m> We do not know is how much time has passed, so we decide to use <m>t</m> to represent that time. At the moment the passenger makes their observation, the plane is still descending, so the altitude is greater than the final value. So our inequality is:
<md>{39000-870t &gt; 8900}</md></p>
</solution>
  </static>
  <rendering-data problemSource="Open_Resources_for_Community_College_Algebra/1_8_6_68.pg" origin="generated" domain="https://webwork.pcc.edu" course-id="orcca" user-id="orcca" passwd="orcca" language="en-US"/>
  <pg><![CDATA[
#############################################
###       Generated from PreTeXt source      
###                                          
###         https://pretextbook.org          
###                                          
#############################################
## DBsubject()
## DBchapter()
## DBsection()
## Level()
## KEYWORDS()
## TitleText1()
## EditionText1()
## AuthorText1()
## Section1(not reported)
## Problem1(1.8.6.68)
## Author()
## Institution()
## Language(en-US)

DOCUMENT();

#############
# Load Macros
#############

loadMacros(
  "PGstandard.pl",
  "PGML.pl",
  "AnswerFormatHelp.pl",
  "PCCmacros.pl",
  "parserNumberWithUnits.pl",
  "parserLinearRelation.pl",
  "ORCCA.pl",
  "PGcourse.pl",
);
COMMENT('Authored in PreTeXt');

#######
# Setup
#######

$final = random(5000, 10000, 100);
$end = NumberWithUnits("$final ft");
$rate = random(500, 1000, 10);
$speed = NumberWithUnits("$rate ft/min");
$initial = random(33000, 42000, 1000);
$start = NumberWithUnits("$initial ft");
Context("LinearRelation")->variables->are(t => 'Real');
$inequality = LinearRelation("$initial - $rate t > $final");

#############################
# Exercise Group Introduction
#############################

BEGIN_PGML
Write an inequality to model the scenario. There is no need to solve the inequality.

END_PGML

######
# Body
######

BEGIN_PGML
An airplane was cruising at [`[$start]\text{,}`] and then began its descent. Because of some air traffic congestion, it will descend to [`[$end]`] and then circle the airport for a while. It descends at a rate of [`[$speed]\text{.}`] A passenger notices the plane is still descending. Set up an inequality where the solution set represents how much time might have passed since the plane started its descent. There is no need to solve it.

[@KeyboardInstructions('Use [|t|]* as the variable.')@]**

[_]{$inequality}{20}

END_PGML

##########
# Solution
##########

BEGIN_PGML_SOLUTION
This is a rate exercise, and we can use

[```
                  (\text{initial value}) \pm (\text{rate})\cdot\text{variable} \mathrel{?} (\text{final value})
                ```]

The question mark in the above is not an equal sign, because we are trying to set up an inequality. The initial value for the altitude is [`[$initial]\text{,}`] the altitude is decreasing (so we will subtract, not add), the rate is [`[$rate]\text{,}`] and the final altitude is [`[$final]\text{.}`] We do not know is how much time has passed, so we decide to use [`t`] to represent that time. At the moment the passenger makes their observation, the plane is still descending, so the altitude is greater than the final value. So our inequality is:

[```
                  [$inequality]
                ```]

END_PGML_SOLUTION

#############
# End Problem
#############

ENDDOCUMENT();
]]></pg>
</webwork-reps>
