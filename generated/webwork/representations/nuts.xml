<?xml version='1.0' encoding='utf-8'?>
<webwork-reps version="2" webwork2_major_version="2" webwork2_minor_version="21" xml:id="extracted-nuts" assembly-id="nuts">
  <static seed="1342">
    <statement><p>Gonzalo is buying peanuts and cashews in bulk to make a barrel of mixed nuts. Each pound of peanuts costs $4.59, and each pound of cashews costs $10.10. He can spend a total of $200. Assume Gonzalo will spend the full amount. Write a linear equation in standard form that models the amounts of peanuts and cashews he can purchase.</p>
<p><fillin characters="16" name="AnSwEr0001"/></p>
</statement>
    <answer>
      <p>
        <m>4.59x+10.1y=200</m>
      </p>
    </answer>
  </static>
  <rendering-data problemSource="Open_Resources_for_Community_College_Algebra/3_7_5_30.pg" origin="generated" domain="https://webwork.pcc.edu" course-id="orcca" user-id="orcca" passwd="orcca" language="en-US"/>
  <pg><![CDATA[
#############################################
###       Generated from PreTeXt source      
###                                          
###         https://pretextbook.org          
###                                          
#############################################
## DBsubject()
## DBchapter()
## DBsection()
## Level()
## KEYWORDS()
## TitleText1()
## EditionText1()
## AuthorText1()
## Section1(not reported)
## Problem1(3.7.5.30)
## Author()
## Institution()
## Language(en-US)

DOCUMENT();

#############
# Load Macros
#############

loadMacros(
  "PGstandard.pl",
  "PGML.pl",
  "AnswerFormatHelp.pl",
  "PCCmacros.pl",
  "parserImplicitPlane.pl",
  "contextCurrency.pl",
  "contextForm.pl",
  "randomPerson.pl",
  "ORCCA.pl",
  "PGcourse.pl",
);
COMMENT('Authored in PreTeXt');

#######
# Setup
#######

Context("ImplicitPlane");
Context()->flags->set(reduceConstants=>0);
Context()->flags->set(reduceConstantFormulas=>0);
Context()->flags->set(showExtraParens=>0);
Context()->variables->are(x=>'Real', y=>'Real');
Context()->noreduce('(-x)-y','(-x)+y','(-x)*y','x*(-y)','-n');
$A = random(4,5,0.01);
$B = random(9,11,0.01);
$C = $A*random(12,20) + $B*random(12,20);
$C = int($C/100 + 0.5)*100;
$ansSTstring = "$A*x+$B*y=$C";
$left1 = Compute("$A*x+$B*y")->reduce;
$ansSTstringTeX = $left1->TeX."=$C";
$ansST = ImplicitPlane($ansSTstring);
$evaluator=$ansST->cmp(correct_ans_latex_string => $ansSTstringTeX)->withPostFilter(sub{
  my $ansHash = shift;
  my $student = $ansHash->{original_student_ans};
  my @sides = split('=',"$student");
  #if it's an implicit plane object, reset how student's answer is displayed:
  if (defined $ansHash->{student_formula}) {
    if ($ansHash->{student_formula}->cmp_class eq "an Implicit line") {
      $ansHash->{preview_text_string} = "$sides[0]=$sides[1]";
      my $leftTex = Formula("$sides[0]")->TeX;
      my $rightTex = Formula("$sides[1]")->TeX;
      $ansHash->{preview_latex_string} = "$leftTex=$rightTex";
      $ansHash->{student_ans} = $ansHash->{original_student_ans};
    }
  };
  # if they have it correct, then check each side to see
  # whether x and y are on the same side of =
  if ($ansHash->{score}) {
    if ((($sides[0] =~ /x/) && ($sides[1] =~ /y/)) || (($sides[1] =~ /x/) && ($sides[0] =~ /y/))) {
      $ansHash->{score} = 0;
      $ansHash->{ans_message} = "This is an equation for the line, but it is not in standard form.";
     }
  }
  return $ansHash;
});
$p = randomPerson();
Context("Currency")->flags->set(trimTrailingZeros => 1);
$Ad = Currency($A);
$Bd = Currency($B);
$Cd = Currency($C);

######
# Body
######

BEGIN_PGML
[$p] is buying peanuts and cashews in bulk to make a barrel of mixed nuts. Each pound of peanuts costs [$Ad], and each pound of cashews costs [$Bd]. [$p->They] can spend a total of [$Cd]. Assume [$p] will spend the full amount. Write a linear equation in standard form that models the amounts of peanuts and cashews [$p->they] can purchase.

[@KeyboardInstructions('Associate the amount of peanuts with variable [`x`] and the amount of cashews with variable [`y\text{.}`]')@]**

[_]{$evaluator}{16}

END_PGML

#############
# End Problem
#############

ENDDOCUMENT();
]]></pg>
</webwork-reps>
