#############################################
###       Generated from PreTeXt source      
###       on 2024-08-01T17:56:25-07:00       
###   A recent stable commit (2022-07-01):   
### 6c761d3dba23af92cba35001c852aac04ae99a5f 
###                                          
###         https://pretextbook.org          
###                                          
#############################################
## DBsubject()
## DBchapter()
## DBsection()
## Level()
## KEYWORDS()
## TitleText1()
## EditionText1()
## AuthorText1()
## Section1(not reported)
## Problem1(1.6.4.5)
## Author()
## Institution()
## Language(en-US)

DOCUMENT();

############################################################
# Load Macros
############################################################
loadMacros(
  "PGstandard.pl",
  "PGML.pl",
  "AnswerFormatHelp.pl",
  "PGlateximage.pl",
  "PCCmacros.pl",
  "contextForm.pl",
  "contextInequalitySetBuilder.pl",
  "parserGraphTool.pl",
  "ORCCA.pl",
  "PGcourse.pl",
);
COMMENT('Authored in PreTeXt');

############################################################
# Header
############################################################
TEXT(beginproblem());


############################################################
# PG Setup Code
############################################################
$var = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
Context()->variables->are($var => 'Real');
$c = random(-6,6);
$a = random(1,20);
$b = $c - $a;
$left = Formula("$var - $a")->reduce;
$right = Formula("$b")->reduce;
$sign = list_random('\lt', '\gt', '\leq', '\geq');
%interval = (
  '\lt'  => "(-inf, $c)",
  '\gt'  => "($c, inf)",
  '\leq' => "(-inf, $c]",
  '\geq' => "[$c, inf)"
);
Context("Interval");
$interval = Interval($interval{$sign});
InequalitySetBuilder::UseVerticalSuchThat();
Context("InequalitySetBuilder-Only");
Context()->variables->are($var => 'Real');
%perlsign = (
  '\lt'  => "<",
  '\gt'  => ">",
  '\leq' => "<=",
  '\geq' => ">="
);
$perlsign = $perlsign{$sign};
$setbuilder = Compute("{ $var | $var $perlsign $c}");
$gt = GraphTool("{interval, $interval}")->with(
  availableTools => ['IntervalTool', 'IncludeExcludePointTool'],
  numberLine => 1,
  xAxisLabel => "$var",
  bBox => [ -8, 8 ],
  ticksDistanceX => 1,
  minorTicksX => 0,
  useBracketEnds => 1,
  showInStatic => 0,
  showCoordinateHints => 0,
);
%words = (
  '\lt'  => "less than",
  '\gt'  => "greater than",
  '\leq' => "less than or equal to",
  '\geq' => "greater than or equal to"
);
$words = $words{$sign};
if ($sign eq '\lt' || $sign eq '\gt') {
  $sol[0] = "does not allow";
  $sol[1] = "parenthesis";
  $sol[2] = "infiniteopeninterval";
} else {
  $sol[0] = "allows";
  $sol[1] = "bracket";
  $sol[2] = "infiniteclosedinterval";
}
if ($sign eq '\lt' || $sign eq '\leq') {
  $sol[3] = -12;
} else {
  $sol[3] = 12;
}
$image_1 = createLaTeXImage();
$image_1->addToPreamble(latexImagePreamble());
$image_1->BEGIN_LATEX_IMAGE
\begin{tikzpicture}
  \begin{axis}[numberline, xlabel={\($var\)}]
    \addplot[$sol[2]] coordinates {($sol[3],0) ($c,0)};
  \end{axis}
\end{tikzpicture}

END_LATEX_IMAGE

############################################################
# Body
############################################################

BEGIN_PGML
Solve the inequality. Graph the solution set, and write the solution set using both interval notation and set-builder notation.

[`[$left][$sign][$right]`]

[_]{$gt}{1}

[@KeyboardInstructions('In interval notation.')@]**

[_]{$interval}{12}

[@KeyboardInstructions('In set-builder notation. (If needed, type [|inf|]* or [|infinity|]* for [`\infty\text{,}`] [|>=|]* for [`\geq\text{,}`] and [|<=|]* for [`\leq\text{.}`])')@]**

[_]{$setbuilder}{12}


END_PGML

############################################################
# Solution
############################################################

BEGIN_PGML_SOLUTION
The opposite action to subtraction is addition:

[```\newcommand{\addright}[1]{{\highlight{{{}+#1}}}}
\begin{gathered}
[$left][$sign][$right]\\
[$left]\addright{[$a]}[$sign][$right]\addright{[$a]}\\
[$var] [$sign] [$c]
\end{gathered}
```]

So in set-builder notation, the solution set is [`[$setbuilder]\text{.}`]

We can graph this by shading all numbers [$words] [`[$c]\text{.}`] Since the sign in the inequality [$sol[0]] for [`[$var]`] to equal [`[$c]\text{,}`] we use a [$sol[1]] at [`[$c]\text{.}`]

>> [@image(insertGraph($image_1), width=>600)@]*  <<

We read the graph from left to right to tell us the interval notation for the solution set: [`[$interval]\text{.}`]


END_PGML_SOLUTION

############################################################
# End Problem
############################################################

ENDDOCUMENT();
