<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!--                                                                      -->
<!-- Open Resources for Community College Algebra is licensed under a     -->
<!-- Creative Commons Attribution 4.0 International License.              -->
<!--                                                                      -->
<!-- Under this license, any user of this textbook or the textbook        -->
<!-- contents must provide proper attribution as follows.                 -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- include in every digital format page view (including but not         -->
<!-- limited to EPUB, PDF, and HTML) and on every physical printed page   -->
<!-- the following attribution:                                           -->
<!--                                                                      -->
<!-- Original source material, products with readable and accessible      -->
<!-- math content, and other information freely available at              -->
<!-- pcc.edu/orcca.                                                       -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- reproduce any math content (such as math expressions and equations)  -->
<!-- in a readable manner, and offer math content in at least one web     -->
<!-- accessible manner.                                                   -->
<!--                                                                      -->
<!-- The Portland Community College name, Portland Community College      -->
<!-- logo, ORCCA name, Open Resources for Community College Algebra name, -->
<!-- ORCCA logo, and front and back cover designs are not subject to the  -->
<!-- Creative Commons license and may not be reproduced without the       -->
<!-- prior and express written consent of Portland Community College.     -->
<!--                                                                      -->
<!-- For questions regarding this license, please contact                 -->
<!-- orcca-group@pcc.edu.                                                 -->
<!-- *********************************************************************-->
<section xml:id="section-graphically-solving-equations-and-inequalities">
  <title>Graphically Solving Equations and Inequalities</title>
  <objectives>
    <title>PCC Course Content and Outcome Guide</title>
    <ul>
      <li>
        <xref ref="ccog-solve-equations-and-inequalities-graphically" text="local">MTH 65 CCOG</xref>
      </li>
    </ul>
  </objectives>


  <!-- TODO: content simply pasted here from edition 1 sections. Needs editing. -->


  <subsection permid="Vhk">
    <title>Recognizing Linear and Quadratic Equations</title>
    <p permid="xhq">
      Now that we have solved both linear and quadratic equations,
      it is important to identify each type of equation.
      Recall that a <term>linear equation</term>
      has a degree of one and a <term>quadratic equation</term> has a degree of two.
      If there is any other operation on the variable such as division or a square root then it is not linear or quadratic.
      We can have other operations on numbers, but not the variables.
      Let's look at an example.
    </p>
<!-- todo: should we move this exercise to the first quadratic solving section where I wrote an example like this already? -->
    <exercise xml:id="identify-linear-and-quadratic-equations" permid="PrY">

      <introduction permid="jkP">
        <p permid="PkU">
          Identify whether each equation is linear, quadratic or neither.
        </p>
      </introduction>

      <webwork>
          <setup>

             <pg-code>
                 $linear = PopUp(['?','linear','quadratic','neither'],1);
                 $quadratic = PopUp(['?','linear','quadratic','neither'],2);
                 $neither = PopUp(['?','linear','quadratic','neither'],3);
            </pg-code>
          </setup>
          <statement>
            <p permid="vsd">
              <ol label="a." permid="vNC">
                <li permid="Axv">
                  <p permid="pYq">
                    The equation <m>3=7y^2-8y</m> is <var name="$quadratic" form="popup"/>.
                  </p>
                </li>

                <li permid="gEE">
                  <p permid="Wfz">
                    The equation <m>5x+3=7x-8</m> is <var name="$linear" form="popup"/>.
                  </p>
                </li>

                <li permid="MLN">
                  <p permid="CmI">
                    The equation <m>r^3-7=4</m> is <var name="$neither" form="popup"/>.
                  </p>
                </li>

                <li permid="sSW">
                  <p permid="itR">
                    The equation <m>\sqrt{7}x+4=10</m> is <var name="$linear" form="popup"/>.
                  </p>
                </li>
              </ol>
            </p>
          </statement>
          <solution permid="rdJ">
            <p permid="kje">
              We will check the degree of each equation:

              <ol label="a." permid="Dsb">
                <li permid="Zaf">
                  <p permid="OBa">
                    This is a quadratic equation because there is a <m>y^2</m> term.
                  </p>
                </li>

                <li permid="Fho">
                  <p permid="uIj">
                    This is a linear equation because the highest exponent is one.
                  </p>
                </li>

                <li permid="lox">
                  <p permid="aPs">
                    This is neither linear nor quadratic because it has a degree of three.
                  </p>
                </li>

                <li permid="RvG">
                  <p permid="GWB">
                    This is a linear equation because it has a degree of one.
                    The coefficient is irrational but the variable is not in the square root.
                  </p>
                </li>
              </ol>
            </p>
          </solution>
      </webwork>
    </exercise>
  </subsection>

  <subsection permid="Bot">
    <title>Solving Linear and Quadratic Equations</title>
    <p permid="doz">
      When an equation is <term>linear</term>,
      we move all variable terms to one side of the equation and all constant terms to the other side.
      Then we use division if needed to solve for the variable.
      This is outlined in <xref ref="list-solve-multistep-linear-equation">List</xref>.
    </p>

    <p permid="JvI">
      When an equation is <term>quadratic</term>,
      we have three different methods we can use.
      Here is an outline of the general process for determining which method to use.
    </p>

    <algorithm xml:id="algorithm-solving-linear-and-quadratic-equations" permid="vzh">
      <title>Solving Quadratic Equations</title>
      <idx><h>solving</h><h>linear and quadratic equations</h></idx>

      <p permid="Vag">
        <ol permid="PGt">
          <li permid="xCP">
            <p permid="ndK">
              First, check whether there is a linear term,
              or whether there is only a squared expression and a constant.
              If there is only a squared expression and a constant,
              isolate the squared quantity and use the
              <term>square root method</term>.
            </p>
          </li>

          <li permid="dJY">
            <p permid="TkT">
              If there <em>is</em> a linear term,
              put the equation in <term>standard form</term>
              with all of the terms on one side and zero on the other side.

              <ol permid="gSZ">
                <li permid="ALQ">
                  <p permid="UEH">
                    If the polynomial factors, solve the equation by <term>factoring</term>.
                  </p>
                </li>

                <li permid="JRh">
                  <p permid="zsc">
                    If the polynomial does not factor,
                    use the <term>quadratic formula</term>.
                  </p>
                </li>
              </ol>
            </p>
          </li>
        </ol>
      </p>
    </algorithm>

    <p permid="pCR">
      Here are some examples:
    </p>

    <example xml:id="example-compare-linear-quadratic-quadratic-without-x-term" permid="bGq">
      <statement>
        <p permid="SfA">
          Solve for <m>x</m> in <m>x^2=7x^2-12</m>.
        </p>
      </statement>
      <solution permid="SAZ">
        <p permid="Qqn">
          This is a quadratic equation because there are <m>x^2</m> terms.
          There are no <m>x</m> terms so we will use the square root method.
          We start by combining the <m>x^2</m> terms on the left.
          <md permid="jNF">
            <mrow>x^2\amp=7x^2-12</mrow>
            <mrow>-6x^2\amp=-12</mrow>
            <mrow>x^2\amp=2</mrow>
          </md>
          <md permid="PUO">
            <mrow>x\amp=-\sqrt{2}\amp\text{or}\amp\amp x=\sqrt{2}</mrow>
          </md>
          The solution set is <m>\{-\sqrt{2},\sqrt{2}\}</m>.
        </p>
      </solution>
    </example>

    <example xml:id="example-compare-linear-quadratic-quadratic-formula" permid="HNz">
      <statement>
        <p permid="ymJ">
          Solve for <m>p</m> in <m>p^2=-2p+2</m>.
        </p>
      </statement>
      <solution permid="yIi">
        <p permid="wxw">
          This is a quadratic equation that also contains a linear term so we will put the equation in standard form.
          Then we see that the left side does not factor so we will use the quadratic formula.
          <md permid="wbX">
            <mrow>p^2+2p-2\amp=0</mrow>
            <mrow>p\amp=\frac{-(2)\pm\sqrt{(2)^2-4(1)(-2)}}{2(1)}</mrow>
            <mrow>p\amp=\frac{-2\pm\sqrt{4+8}}{2}</mrow>
            <mrow>p\amp=\frac{-2\pm\sqrt{12}}{2}</mrow>
            <mrow>p\amp=\frac{-2\pm2\sqrt{3}}{2}</mrow>
            <mrow>p\amp=\frac{-2}{2}\pm\frac{2\sqrt{3}}{2}</mrow>
            <mrow>p\amp=-1\pm \sqrt{3}</mrow>
          </md>
          The solution set is <m>\{1-\sqrt{3},1+\sqrt{3}\}</m>.
        </p>
      </solution>
    </example>

    <example xml:id="example-compare-linear-quadratic-linear" permid="nUI">
      <statement>
        <p permid="etS">
          Solve for <m>t</m> in <m>7-t=9t+11</m>.
        </p>
      </statement>
      <solution permid="ePr">
        <p permid="cEF">
          This is a linear equation so we will combine the linear terms on one side and the constant terms on the other side.
          <md permid="cjg">
            <mrow>-10t\amp=4</mrow>
            <mrow>t\amp=\frac{4}{-10}</mrow>
            <mrow>t\amp=-\frac{2}{5}</mrow>
          </md>
          The solution set is <m>\left\{-\frac{2}{5}\right\}</m>.
        </p>
      </solution>
    </example>

    <example xml:id="example-compare-linear-quadratic-quadratic-with-x-term-factor" permid="UbR">
      <statement>
        <p permid="KBb">
          Solve for <m>z</m> in <m>z^2-10=3z+30</m>.
        </p>
      </statement>
      <solution permid="KWA">
        <p permid="ILO">
          This is a quadratic equation with a linear term so we will put it in standard form.
          Then we see that the left side factors so we solve by factoring.
          <md permid="Iqp">
            <mrow>z^2-10=3z+30</mrow>
            <mrow>z^2-3z-40\amp=0</mrow>
            <mrow>(z+5)(z-8)\amp=0</mrow>
          </md>
          <md permid="oxy">
            <mrow>z+5\amp=0\amp\amp\text{ or }\amp z-8\amp=0</mrow>
            <mrow>z\amp=-5\amp\amp\text{ or }\amp z\amp=8</mrow>
          </md>
          The solution set is <m>\{-5, 8\}</m>.
        </p>
      </solution>
    </example>
  </subsection>

  <!-- This subsection taken from edition 1's radical equation solving section. -->
  <!-- Useful here, but needs editing. -->

<subsection permid="FmF">
    <title>Graphing Technology</title>
    <p permid="zqp">
      We can use technology to solve equations by finding where two graphs intersect.
    </p>

    <example permid="Ehm">
      <statement>
        <p permid="ohq">
          Solve the equation <m>1-x=\sqrt{x+5}</m> with technology.
        </p>
      </statement>
      <solution permid="CGu">
        <sidebyside widths="47% 47%" margins="0%" permid="QvE">
          <p permid="kCQ">
            We define <m>f(x)=1-x</m> and <m>g(x)=\sqrt{x+5}</m>,
            and then look for the intersection(s) of the graphs.
            Since the two functions intersect at <m>(-1,2)</m>,
            the solution to <m>1-x=\sqrt{x+5}</m> is <m>-1</m>.
            The solution set is <m>\{-1\}</m>.
          </p>

          <figure xml:id="figure-solving-radical-equation" permid="JfA">
            <caption>Graph of <m>f(x)=1-x</m> and <m>g(x)=\sqrt{x+5}</m></caption>
            <image permid="vqq">
              <description></description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[]
                    \addplot [firstcurve, -{Kite}, variable=\t, domain=0:3.464] ({t^2-5},{t}) node[above, pos=0.75, sloped] {$g(x)=\sqrt{x+5}$};
                    \addplot [soliddot, color=firstcolor] coordinates {(-5,0)};
                    \addplot [secondcurve, domain=-6:7] {1-x} node[below, sloped, pos=0.17] {$f(x)=1-x$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(-1,2)};
                \end{axis}
            \end{tikzpicture}]]>
            </latex-image>
            </image>

          </figure>
        </sidebyside>

        <p permid="QJZ">
          Before we finish with this example,
          we would like to illustrate why there are sometimes extraneous solutions to radical equations.
          It has to do with the squaring-both-sides step of the solving process.
        </p>

        <sidebyside widths="47% 47%" margins="0%" permid="wCN">

          <stack>
            <p permid="wRi">
              A graph of a radical, for example <m>y=\sqrt{x+5}</m>,
              actually graphs as <em>half</em> of a sideways parabola,
              as you can see in <xref ref="figure-solving-radical-equation">Figure</xref>.
              When we square both sides of that equation,
              we get <m>y^2=x+5</m> which actually graphs as a
              <em>complete</em> sideways parabola.
            </p>

            <p permid="cYr">
              The curve and the line now intersect <em>twice</em>!
              This second solution
              (which is <m>4</m>, by the way)
              is the extraneous solution that we would have found had we solved <m>1-x=\sqrt{x+5}</m> algebraically.
            </p>
          </stack>

          <figure xml:id="figure-solving-radical-equation-both-halves" permid="pmJ">
            <caption>Graph of <m>f(x)=1-x</m> and <m>y^2=x+5</m></caption>
            <image permid="bxz">
              <description></description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[]
                    \addplot [firstcurve, variable=\t, domain=-3.464:3.464] ({t^2-5},{t}) node[above, pos=0.9, sloped] {$y^2=x+5$};
                    \addplot [secondcurve, domain=-6:7] {1-x} node[below, sloped, pos=0.17] {$f(x)=1-x$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(-1,2)};
                    \addplot [soliddot, color=fourthcolor] coordinates {(4,-3)};
                \end{axis}
            \end{tikzpicture}]]>
            </latex-image>
            </image>

          </figure>
        </sidebyside>
      </solution>
    </example>

    <example permid="kov">
      <statement>
        <p permid="Uoz">
          Solve the equation <m>3+\sqrt{x+4}=x-\sqrt{x-4}</m> graphically using technology.
        </p>
      </statement>
      <solution permid="iND">
        <sidebyside widths="47% 47%" margins="0%" permid="cJW">
          <p permid="VtS">
            To solve the equation graphically,
            first we will assign the left side of the equation the label
            <m>m(x)=3+\sqrt{x+4}</m> and the right side <m>n(x)=x-\sqrt{x-4}</m>.
            Next we will make graphs of both <m>m</m> and <m>n</m> on the same grid and look for their intersection point(s).
            Since the two functions intersect at about <m>(8.75,6.571)</m>,
            the solution to <m>3+\sqrt{x+4}=x-\sqrt{x-4}</m> is <m>8.75</m>.
            The solution set is <m>\{8.75\}</m>.
          </p>

          <figure permid="BBb">
            <caption>Graph of <m>m(x)=3+\sqrt{x+4}</m> and <m>n(x)=x-\sqrt{x-4}</m></caption>
            <image permid="HEI">
              <description></description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[xmin=-6, ymin=-1, xmax=16, ymax=9, xtick={-4,-2,...,14}, minor xtick=, ytick={1,2,...,8}, minor ytick=]
                    \addplot [firstcurve, -{Kite}, variable=\t, domain=3:7.472] ({t^2-6*t+5},{t}) node[above, pos=0.45, sloped] {$y=m(x)$};
                    \addplot [soliddot, color=firstcolor] coordinates {(-4,3)};
                    \addplot [secondcurve, -{Kite}, variable=\t, domain=3.75:9] ({0.5*(2*t+1+sqrt((2*t+1)^2-4*(t^2+4)))},{t}) node[below, pos=0.15, sloped] {$y=n(x)$};
                    \addplot [secondcurve, -, variable=\t, domain=3.75:4] ({0.5*(2*t+1-sqrt((2*t+1)^2-4*(t^2+4)))},{t});
                    \addplot [soliddot, color=secondcolor] coordinates {(4,4)};
                    \addplot [soliddot, color=fourthcolor] coordinates {(8.75,6.571)};
                \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </figure>
        </sidebyside>
      </solution>
    </example>
  </subsection>

  <exercises>
    <exercisegroup cols="2" permid="dPp">
      <title>Solving Radical Equations Using Technology</title>
      <exercise permid="rxv">
        <webwork>
            <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                do{
                    $a = non_zero_random(-5,5,0.1);
                    $b = non_zero_random(-5,5,0.1);
                    $root = ($a-($b)**2)/(2*$b);
                    $x = $root**2;
                } until ( $root > 0 and sqrt($x + $a) == sqrt($x) + $b);

                $solutions = Formula("{$x}");

                $left = Formula("sqrt(x+$a)")->reduce;
                $right = Formula("sqrt(x)+$b")->reduce;
                $funcNew = Compute("$left-sqrt(x)-$b")->reduce;

                $alt="This picture shows the graph of the function $f(x)=$funcNew. The points ($x,0) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$x],[-2,2]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, 'x','black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, 'y','black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcNew for x in &lt; $min[0],$max[0] > using color:red and weight:1");

                $hx = ($x+$max[0])/2;
                $hy = $funcNew->eval(x=>$hx);
                $gr->lb( new Label($hx, $hy+$marksep[1]/4, "f(x)",'red','center','middle'));

                $xShow = int($x*100)/100;
                $gr->stamps( closed_circle($x, 0, 'red') );
                $gr->lb( new Label($x, $marksep[1]/8, "($xShow,0)",'red','right','bottom'));
            <!-- todo: change this code to not subtract the right side onto the left before graphing. -->
            </pg-code>
            </setup>
            <statement>
              <p permid="lmK">
                Use technology to solve the equation
                <me permid="lWE">
                  <var name="$left" />=<var name="$right" />
                </me>.
              </p>
              <instruction>
                Round your answers to two decimal places if needed.
              </instruction>
              <p permid="RtT">
                <var name="$solutions" width="10" />
              </p>
            </statement>
            <solution permid="OUM">
              <p permid="hIk">
                First, we move all terms to one side of the equals sign:
                <md permid="SdN">
                  <mrow><var name="$left" />\amp=<var name="$right" /></mrow>
                  <mrow><var name="$funcNew" />\amp=0</mrow>
                </md>
              </p>

              <p permid="NPt">
                Next, we use technology to graph the function <m>f(x)=<var name="$funcNew" /></m>:
              </p>

              <sidebyside permid="IRf">

                <image pg-name="$gr" permid="nLR">
                  <description><var name="$alt" /></description>
                </image>

              </sidebyside>

              <p permid="tWC">
                The function's <m>x</m>-intercept gives us one solution of the equation:
                <m>x=<var name="$xShow"/></m>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="XEE">
        <webwork>
            <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                do{
                    $a = non_zero_random(-5,5,0.1);
                    $b = non_zero_random(-5,5,0.1);
                    $root = ($a-($b)**2)/(2*$b);
                    $x = $root**2;
                } until ( $root > 0  and sqrt($x + $a) == sqrt($x) + $b);

                $solutions = Formula("{$x}");

                $left = Formula("sqrt(x+$a)")->reduce;
                $right = Formula("sqrt(x)+$b")->reduce;
                $funcNew = Compute("$left-sqrt(x)-$b")->reduce;

                $alt="This picture shows the graph of the function $f(x)=$funcNew. The points ($x,0) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$x],[-2,2]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, 'x','black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, 'y','black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcNew for x in &lt; $min[0],$max[0] > using color:red and weight:1");

                $hx = ($x+$max[0])/2;
                $hy = $funcNew->eval(x=>$hx);
                $gr->lb( new Label($hx, $hy+$marksep[1]/4, "f(x)",'red','center','middle'));

                $xShow = int($x*100)/100;
                $gr->stamps( closed_circle($x, 0, 'red') );
                $gr->lb( new Label($x, $marksep[1]/8, "($xShow,0)",'red','right','bottom'));
            <!-- todo: change this code to not subtract the right side onto the left before graphing. -->
            </pg-code>
            </setup>
            <statement>
              <p permid="xBc">
                Use technology to solve the equation
                <me permid="ykW">
                  <var name="$left" />=<var name="$right" />
                </me>.
              </p>
              <instruction>
                Round your answers to two decimal places if needed.
              </instruction>
              <p permid="dIl">
                <var name="$solutions" width="10" />
              </p>
            </statement>
            <solution permid="vbV">
              <p permid="adL">
                First, we move all terms to one side of the equals sign:
                <md permid="esf">
                  <mrow><var name="$left" />\amp=<var name="$right" /></mrow>
                  <mrow><var name="$funcNew" />\amp=0</mrow>
                </md>
              </p>

              <p permid="GkU">
                Next, we use technology to graph the function <m>f(x)=<var name="$funcNew" /></m>:
              </p>

              <sidebyside permid="oYo">

                <image pg-name="$gr" permid="TTa">
                  <description><var name="$alt" /></description>
                </image>

              </sidebyside>

              <p permid="msd">
                The function's <m>x</m>-intercept gives us one solution of the equation:
                <m>x=<var name="$xShow"/></m>.
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
  </exercises>


</section>

