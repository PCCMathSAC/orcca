<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!--                                                                      -->
<!-- Open Resources for Community College Algebra is licensed under a     -->
<!-- Creative Commons Attribution 4.0 International License.              -->
<!--                                                                      -->
<!-- Under this license, any user of this textbook or the textbook        -->
<!-- contents must provide proper attribution as follows.                 -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- include in every digital format page view (including but not         -->
<!-- limited to EPUB, PDF, and HTML) and on every physical printed page   -->
<!-- the following attribution:                                           -->
<!--                                                                      -->
<!-- Original source material, products with readable and accessible      -->
<!-- math content, and other information freely available at              -->
<!-- pcc.edu/orcca.                                                       -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- reproduce any math content (such as math expressions and equations)  -->
<!-- in a readable manner, and offer math content in at least one web     -->
<!-- accessible manner.                                                   -->
<!--                                                                      -->
<!-- The Portland Community College name, Portland Community College      -->
<!-- logo, ORCCA name, Open Resources for Community College Algebra name, -->
<!-- ORCCA logo, and front and back cover designs are not subject to the  -->
<!-- Creative Commons license and may not be reproduced without the       -->
<!-- prior and express written consent of Portland Community College.     -->
<!--                                                                      -->
<!-- For questions regarding this license, please contact                 -->
<!-- orcca-group@pcc.edu.                                                 -->
<!-- *********************************************************************-->
<section xml:id="section-using-technology-to-explore-functions" permid="Nai">
  <title>Using Technology to Explore Functions</title>
  <objectives permid="Bal">
    <title>PCC Course Content and Outcome Guide</title>
    <ul permid="gLV">
      <li permid="uPA"><xref ref="mth95-calculator-skills" /></li>
    </ul>
  </objectives>

  <introduction permid="hhu">
    <p permid="VDj">
      Graphing technology allows us to explore the properties of functions more deeply than we can with only pencil and paper.
      It can quickly create a table of values,
      and quickly plot the graph of a function.
      Such technology can also evaluate functions,
      solve equations with functions,
      find maximum and minimum values,
      and explore other key features.
    </p>

    <p permid="BKs">
      There are many graphing technologies currently available,
      including
      (but not limited to)
      physical (hand-held) graphing calculators,
      <url href="https://www.desmos.com/"><pubtitle>Desmos</pubtitle></url>,
      <url href="https://www.geogebra.org/"><pubtitle>GeoGebra</pubtitle></url>,
      <url href="https://www.sagemath.org/"><pubtitle>Sage</pubtitle></url>, and
      <url href="http://www.wolframalpha.com/"><pubtitle>WolframAlpha</pubtitle></url>.
    </p>

    <p permid="hRB">
      This section will focus on <em>how</em>
      technology can be used to explore functions and their key features.
      Although the choice of particular graphing technology varies by each school and curriculum,
      the main ways in which technology is used to explore functions is the same and can be done with each of the technologies above.
    </p>

    <figure permid="NYK">
      <caption>Alternative Video Lesson</caption>
      <video youtubeplaylist="PL2qOxw8-rmqbHBOL9kGQIeEhHOdq4Xho-"/>
    </figure>
  </introduction>

  <subsection permid="thr">
    <title>Finding an Appropriate Window</title>
    <p permid="VRE">
      With a simple linear equation like <m>y=2x+5</m>,
      most graphing technologies will show this graph in a good window by default.
      A common default window goes from <m>x=-10</m> to <m>x=10</m> and <m>y=-10</m> to <m>y=10</m>.
    </p>

    <p permid="BYN">
      What if we wanted to graph something with a much larger magnitude though,
      such as <m>y=2000x+5000</m>?
      If we tried to view this for <m>x=-10</m> to <m>x=10</m> and <m>y=-10</m> to <m>y=10</m>,
      the function would appear as an almost vertical line since it has such a steep slope.
    </p>

    <p permid="ifW">
      Using technology,
      we will create a table of values for this function as shown in <xref ref="table-values-for-an-appropriate-window">Figure</xref>.
      Then we will set the <m>x</m>-values for which we view the function to go from <m>x=-5</m> to <m>x=5</m> and the <m>y</m>-values from
      <m>y=-20{,}000</m> to <m>y=20{,}000</m>.
      The graph is shown in <xref ref="figure-graph-with-appropriate-window">Figure</xref>.
    </p>

    <figure permid="uuo">
      <caption>Creating a table of values to determine an appropriate graphing window</caption>
      <sidebyside widths="47% 47%" margins="0%" permid="CBd">

        <table xml:id="table-values-for-an-appropriate-window" permid="FDn">
          <caption>A table of values</caption>
          <tabular permid="lKw">
            <col halign="center" right="minor"/>
            <col halign="center" />
            <row bottom="medium">
              <cell><m>x</m></cell>
              <cell><m>y=2000x+5000</m></cell>
            </row>
            <row>
              <cell><m>-5</m></cell>
              <cell><m>-5000</m></cell>
            </row>
            <row>
              <cell><m>-4</m></cell>
              <cell><m>-3000</m></cell>
            </row>
            <row>
              <cell><m>-3</m></cell>
              <cell><m>-1000</m></cell>
            </row>
            <row>
              <cell><m>-2</m></cell>
              <cell><m>1000</m></cell>
            </row>
            <row>
              <cell><m>-1</m></cell>
              <cell><m>3000</m></cell>
            </row>
            <row>
              <cell><m>0</m></cell>
              <cell><m>5000</m></cell>
            </row>
            <row>
              <cell><m>1</m></cell>
              <cell><m>7000</m></cell>
            </row>
            <row>
              <cell><m>2</m></cell>
              <cell><m>9000</m></cell>
            </row>
            <row>
              <cell><m>3</m></cell>
              <cell><m>11000</m></cell>
            </row>
            <row>
              <cell><m>4</m></cell>
              <cell><m>13000</m></cell>
            </row>
            <row>
              <cell><m>5</m></cell>
              <cell><m>15000</m></cell>
            </row>
          </tabular>

        </table>

        <figure xml:id="figure-graph-with-appropriate-window" permid="kMh">
          <caption>Graphed with an appropriate window</caption>
          <image permid="Zwe">
            <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-5,xmax=5,
                    ymin=-20,ymax=20,
                    xtick={-4,-3,...,4},
                    ytick={-15,-10,...,15},
                    yticklabels ={-15000,-10000,-5000,0,5000,10000,15000},
                    minor ytick={-20,-15,...,20},
                        ]
                        \addplot+[domain=-5:5,-] {2*x+5};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>
      </sidebyside>

    </figure>

    <p permid="Onf">
      Now let's practice finding an appropriate viewing window with a less familiar function.
    </p>

    <example permid="WtU">
      <p permid="vlm">
        Find an appropriate window for <m>q(x)=\frac{x^3}{100}-2x+1</m>.
      </p>

      <sidebyside widths="47% 47%" margins="0%" permid="iIm">
        <p permid="bsv">
          Entering this function into graphing technology,
          we input <c>q(x)=(x^3)/100-2x+1</c>.
          A default window will generally give us something like this:
        </p>

        <figure xml:id="figure-function-in-default-window" permid="nGN">
          <caption>Function <m>q</m> graphed in the default window.</caption>
          <image permid="RRF">
            <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-10,xmax=10,
                    ymin=-10,ymax=10,
                    xtick={-8,-6,...,8},
                    minor xtick={-9,-7,...,9},
                    ytick={-8,-6,...,8},
                    minor ytick={-10,-9,...,10},
                    minor xtick={-10,-9,...,10},
                        ]
                        \addplot+[domain=-10:10,-,samples=100] {(x^3)/100-2*x+1};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>
      </sidebyside>

      <sidebyside widths="47% 47%" margins="0%" permid="OPv">
        <p permid="HzE">
          We can tell from the lower right corner of <xref ref="figure-function-in-default-window">Figure</xref>
          that we're not quite viewing all of the important details of this function.
          To determine a better window,
          we could use technology to make a table of values.
          Another more rudimentary option is to double the viewing constraints for <m>x</m> and <m>y</m>,
          as shown in <xref ref="figure-function-in-doubled-window">Figure</xref>.
          Many graphing technologies have the ability to zoom in and out quickly.
        </p>

        <figure xml:id="figure-function-in-doubled-window" permid="TNW">
          <caption>Function <m>q</m> graphed in an expanded window.</caption>
          <image permid="xYO">
            <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-20,xmax=20,
                    ymin=-20,ymax=20,
                    xtick={-16,-12,...,16},
                    minor xtick={-20,-19,...,20},
                    ytick={-16,-12,...,16},
                    minor ytick={-20,-19,...,20}
                        ]
                        \addplot+[domain=-20:20,-,samples=100] {(x^3)/100-2*x+1};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>
      </sidebyside>
    </example>
  </subsection>

  <subsection permid="ZoA">
    <title>Using Technology to Determine Key Features of a Graph</title>
    <p permid="aBx">
      The key features of a graph can be determined using graphing technology.
      Here, we'll show how to determine the <m>x</m>-intercepts,
      <m>y</m>-intercepts,
      maximum/minimum values, and the domain and range using technology.
    </p>

    <example permid="uWE">
      <statement>
        <p permid="zVf">
          Graph the function given by <m>p(x)=-1000x^2-100x+40</m>.
          Determine an appropriate viewing window,
          and then use graphing technology to determine the following:

          <ol label="a." permid="MTe">
            <li permid="aWJ">
              <p permid="dar">
                Determine the <m>x</m>-intercepts of the function.
              </p>
            </li>

            <li permid="HdS">
              <p permid="JhA">
                Determine the <m>y</m>-intercept of the function.
              </p>
            </li>

            <li permid="nlb">
              <p permid="poJ">
                Determine the maximum function value and where it occurs.
              </p>
            </li>

            <li permid="Tsk">
              <p permid="VvS">
                State the domain and range of this function.
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="NoD">
        <sidebyside widths="47% 47%" margins="0%" permid="HkW">

          <stack>
            <p permid="mmZ">
              To start, we'll take a quick view of this function in a default window.
              We can see that we need to zoom in on the <m>x</m>-values,
              but we need to zoom out on the <m>y</m>-values.
            </p>

            <p permid="Sui">
              From the graph we see that the <m>x</m>-values might as well run from about <m>-0.5</m> to <m>0.5</m>,
              so we will look at <m>x</m>-values in that window in increments of <m>0.1</m>,
              as shown in <xref ref="table-values-for-an-appropriate-window-small-increments">Table</xref>.
              This table allows us to determine an appropriate viewing window for <m>y=p(x)</m> which is shown in <xref ref="figure-graph-with-appropriate-window-small-increments">Figure</xref>.
              The table suggests we should go a little higher than <m>40</m> on the <m>y</m>-axis,
              and it would be OK to go the same distance in the negative direction to keep the <m>x</m>-axis centered.
            </p>
          </stack>

          <figure xml:id="figure-graph-with-default-windwo-needs-small-increments" permid="KPJ">
            <caption>Graph of <m>y=p(x)</m> in an inappropriate window</caption>
            <image permid="efX">
              <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-10,xmax=10,
                    ymin=-10,ymax=10,
                    xtick={-8,-6,...,8},
                    minor xtick={-10,-9.5,...,10},
                    ytick={-8,-6,...,8},
                    minor ytick={-10,-9.5,...,10}
                        ]
                        \addplot+[domain=-0.3:0.2,-] {-1000*x^2-100*x+40};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
            </image>

          </figure>
        </sidebyside>

        <figure permid="qWS">
          <caption>Creating a table of values to determine an appropriate graphing window</caption>
          <sidebyside widths="47% 47%" margins="0%" permid="nsf">

            <table xml:id="table-values-for-an-appropriate-window-small-increments" permid="qup">
              <caption>Function values for <m>y=p(x)</m></caption>
              <tabular permid="WBy">
                <col halign="center" right="minor"/>
                <col halign="center" />
                <row bottom="medium">
                  <cell><m>x</m></cell>
                  <cell><m>p(x)</m></cell>
                </row>
                <row>
                  <cell><m>-0.5</m></cell>
                  <cell><m>-160</m></cell>
                </row>
                <row>
                  <cell><m>-0.4</m></cell>
                  <cell><m>-80</m></cell>
                </row>
                <row>
                  <cell><m>-0.3</m></cell>
                  <cell><m>-20</m></cell>
                </row>
                <row>
                  <cell><m>-0.2</m></cell>
                  <cell><m>20</m></cell>
                </row>
                <row>
                  <cell><m>-0.1</m></cell>
                  <cell><m>40</m></cell>
                </row>
                <row>
                  <cell><m>0</m></cell>
                  <cell><m>40</m></cell>
                </row>
                <row>
                  <cell><m>0.1</m></cell>
                  <cell><m>20</m></cell>
                </row>
                <row>
                  <cell><m>0.2</m></cell>
                  <cell><m>-20</m></cell>
                </row>
                <row>
                  <cell><m>0.3</m></cell>
                  <cell><m>-80</m></cell>
                </row>
                <row>
                  <cell><m>0.4</m></cell>
                  <cell><m>-160</m></cell>
                </row>
                <row>
                  <cell><m>0.5</m></cell>
                  <cell><m>-260</m></cell>
                </row>
              </tabular>

            </table>

            <figure xml:id="figure-graph-with-appropriate-window-small-increments" permid="QTq">
              <caption>Graph of <m>y=p(x)</m> in an appropriate window showing key features</caption>
              <image permid="Kng">
                <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-0.5,xmax=0.5,
                    ymin=-60,ymax=60,
                    xtick={-0.4,-0.3,...,0.4},
                    minor xtick={-0.5,-0.4,...,0.6},
                    ytick={-40,-20,...,40},
                    minor ytick={-60,-50,...,60},
                    clip=false
                        ]
                        \addplot+[domain=-0.37:0.27,-] {-1000*x^2-100*x+40};
                        \addplot[soliddot] coordinates {(-0.2562,0)} node[above left]{$(-0.2562,0)$};
                        \addplot[soliddot] coordinates {(0.1562,0)} node[above right]{$(0.1562,0)$};
                        \addplot[soliddot] coordinates {(0,40)} node[above right]{$(0,40)$};
                        \addplot[soliddot] coordinates {(-0.05,42.5)} node[above left]{$(-0.05,42.5)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
              </image>

            </figure>
          </sidebyside>

        </figure>

        <p permid="yBr">
          We can now use <xref ref="figure-graph-with-appropriate-window-small-increments">Figure</xref>
          to determine the <m>x</m>-intercepts, the <m>y</m>-intercept,
          the maximum function value, and the domain and range.
        </p>

        <p permid="eIA">
          <ol label="a." permid="yPz">
            <li permid="zzt">
              <p permid="BDb">
                To determine the <m>x</m>-intercepts,
                we will find the points where <m>y</m> is zero.
                These are about <m>(-0.2562,0)</m> and <m>(0.1562,0)</m>.
              </p>
            </li>

            <li permid="fGC">
              <p permid="hKk">
                To determine the <m>y</m>-intercept,
                we need the point where <m>x</m> is zero.
                This point is <m>(0,40)</m>.
              </p>
            </li>

            <li permid="LNL">
              <p permid="NRt">
                The highest point on the graph is the vertex,
                which is about <m>(-0.05,42.5)</m>.
                So the maximum function value is <m>42.5</m> and occurs at <m>-0.05</m>.
              </p>
            </li>

            <li permid="rUU">
              <p permid="tYC">
                We can see that the function is defined for all <m>x</m>-values,
                so the domain is <m>(-\infty,\infty)</m>.
                The maximum function value is <m>42.5</m>,
                and there is no minimum function value.
                Thus the range is <m>(-\infty,42.5]</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
        <!--https://www.desmos.com/calculator/3doxbb3l89-->
    <example permid="bdN">
      <sidebyside widths="47% 47%" margins="0%" permid="Tzo">
        <p permid="gco">
          If we use graphing technology to graph the function <m>g</m> where <m>g(x) = 0.0002x^2 + 0.00146x + 0.00266</m>,
          we may be mislead by the way values are rounded.
          Without technology,
          we know that this function is a quadratic function and therefore has at most two <m>x</m>-intercepts and has a vertex that will determine the minimum function value.
          However, using technology we could obtain a graph with the following key points:
        </p>

        <figure xml:id="figure-graph-with-rounding-issue" permid="YxP">
          <caption>Misleading graph</caption>
          <image permid="CIH">
            <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=4,
                    ymin=-4, ymax=4,
                    xtick={1,2,3},
                    minor xtick={-8,-7,...,4},
                    ytick={-4,-2,...,4},
                    yticklabels={$-0.04$,$-0.02$,0,0.02,0.04},
                    minor ytick={-4,-3,...,4},
                    clip=false,
                        ]
                       \addplot+[domain=-8:4] {(0.0002*x^2 + 0.00146*x + 0.00266)*100};
                        \addplot[soliddot] coordinates {(-3.8,0)} node[below left]{$(-3.8,0)$};
                        \addplot[soliddot] coordinates {(-3.65,0)} node[above]{$(-3.65,0)$};
                        \addplot[soliddot] coordinates {(-3.5,0)} node[below right]{$(-3.5,0)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>
      </sidebyside>

      <p permid="Mjx">
        This <em>looks</em> like there are three <m>x</m>-intercepts,
        which we know is not possible for a quadratic function.
        We can evaluate <m>g</m> at <m>x=-3.65</m> and determine that <m>g(-3.65)= -0.0000045</m>,
        which is <em>approximately</em> zero when rounded.
        So the true vertex of this function is <m>(-3.65,-0.0000045)</m>,
        and the minimum value of this function is <m>-0.0000045</m>
        (not zero).
      </p>

      <p permid="sqG">
        Every graphing tool generally has some type of limitation like this one,
        and it's good to be aware that these limitations exist.
      </p>
    </example>
  </subsection>

  <subsection permid="FvJ">
    <title>Solving Equations and Inequalities Graphically Using Technology</title>
    <p permid="GIG">
      To <em>algebraically</em> solve an equation like <m>h(x)=v(x)</m> for
      <md permid="flq">
        <mrow>h(x)\amp=-0.01(x-90)(x+20)\amp\amp\text{and}\amp v(x)\amp=-0.04(x-10)(x-80)</mrow>
      </md>,
      we'd start by setting up
      <me permid="zeh">
        -0.01(x-90)(x+20)=-0.04(x-10)(x-80)
      </me>
      To solve this, we'd then simplify each side of the equation,
      set it equal to zero, and finally use the quadratic formula.
    </p>

    <p permid="mPP">
      An alternative is to <em>graphically</em>
      solve this equation, which is done by graphing
      <md permid="Lsz">
        <mrow>y\amp=-0.01(x-90)(x+20)\amp\amp\text{and}\amp y\amp=-0.04(x-10)(x-80)</mrow>
      </md>.
    </p>

    <sidebyside widths="47% 47%" margins="0%" permid="fNG">
      <figure xml:id="figure-graph-with-points-of-intersection-two-functions" permid="dhI">
        <caption>Points of intersection for <m>h(x)=v(x)</m></caption>
        <image permid="iPQ">
          <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-60,xmax=100,
                    ymin=-40,ymax=60,
                    xtick={-40,-20,...,80},
                    minor xtick={-50,-40,...,90},
                    ytick={-30,-20,...,50},
                    minor ytick={-30,-20,...,50},
                    clip=false,
                        ]
                        \addplot+[domain=-48.815:100,samples=100] {-0.01*(x-90)*(x+20)};
                        \addplot+[domain=-2.17:92.17,samples=100] {-0.04*(x-10)*(x-80)};
                        \addplot[soliddot, color=fourthcolor] coordinates {(22.46,28.677)} node[above right]{$(22.46,28.677)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(74.207,14.878)} node[below left]{$(74.207,14.878)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <p permid="xaz">
        The points of intersection,
        <m>(22.46,28.677)</m> and <m>(74.207,14.878)</m>,
        show where these functions are equal.
        This means that the <m>x</m>-values give the solutions to the equation <m>-0.01(x-90)(x+20)=-0.04(x-10)(x-80)</m>.
        So the solutions are approximately <m>22.46</m> and <m>74.207</m>,
        and the solution set is approximately <m>\{22.46, 74.207\}</m>.
      </p>
    </sidebyside>

    <p permid="SWY">
      Similarly, to <em>graphically</em>
      solve an equation like <m>h(x)=25</m> for
      <me permid="rzI">
        h(x)=-0.01(x-90)(x+20)
      </me>,
      we can graph
      <me permid="XGR">
        y=-0.01(x-90)(x+20)\qquad\text{and}\qquad y=25
      </me>
    </p>

    <sidebyside widths="47% 47%" margins="0%" permid="LUP">
      <figure xml:id="figure-graph-with-points-of-intersection-constant-function" permid="pwa">
        <caption>Points of intersection for <m>h(x)=25</m></caption>
        <image width="47%" permid="OWZ">
          <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-60,xmax=100,
                    ymin=-40,ymax=60,
                    xtick={-40,-20,...,80},
                    minor xtick={-50,-40,...,90},
                    ytick={-30,-20,...,50},
                    minor ytick={-30,-20,...,50},
                    clip=false,
                        ]
                        \addplot+[domain=-48.815:100,samples=100] {-0.01*(x-90)*(x+20)};
                        \addplot+[domain=-60:100] {25};
                        \addplot[soliddot, color=fourthcolor] coordinates {(12.807,25)} node[above right, rotate=20]{$(12.807,25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(57.913,25)} node[above right, rotate=20]{$(57.913,25)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <p permid="JoR">
        The points of intersection are
        <m>(12.807,25)</m> and <m>(57.913,25)</m>,
        which tells us that the solutions to <m>h(x)=25</m> are approximately <m>12.807</m> and <m>57.913</m>.
        The solution set is approximately <m>\{12.807, 57.913\}</m>.
      </p>
    </sidebyside>

    <example permid="zGx">
      <statement>
        <p permid="EEY">
          Use graphing technology to solve the following inequalities:

          <ol label="a." cols="2" permid="tan">
            <li permid="Ycd">
              <p permid="afL">
                <m>-20t^2-70t+300 \geq -5t+300</m>
              </p>
            </li>

            <li permid="Ejm">
              <p permid="GmU">
                <m>-20t^2-70t+300 \lt -5t+300</m>
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="tvM">
        <p permid="Xeb">
          To solve these inequalities graphically,
          we will start by graphing the equations <m>y=-20t^2-70t+300</m> and
          <m>y=-5t+300</m> and determining the points of intersection:
        </p>

        <figure xml:id="figure-solve-inequality-graphically-setup" permid="jst">
          <caption>Points of intersection for <m>y=-20t^2-70t+300</m> and <m>y=-5t+300</m></caption>
          <image width="47%" permid="vei">
            <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[soliddot, color=fourthcolor] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>

        <p permid="Dlk">
          <ol label="a." permid="eWI">
            <li permid="kqv">

              <sidebyside widths="47% 47%" margins="0%" permid="sbY">
                <p permid="mud">
                  To solve <m>-20t^2-70t+300 \geq -5t+300</m>,
                  we need to determine where the <m>y</m>-values of the graph of <m>y=-20t^2-70t+300</m> are <em>greater</em>
                  than the <m>y</m>-values of the graph of
                  <m>y=-5t+300</m> in addition to the values where the <m>y</m>-values are equal.
                  This region is highlighted in <xref ref="figure-solve-inequality-graphically-geq">Figure</xref>.
                </p>

                <figure xml:id="figure-solve-inequality-graphically-geq" permid="yIv">
                  <caption></caption>
                  <image width="47%" permid="blr">
                    <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    xlabel={$t$},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[highlightcurve,domain=-3.25:0] {-20*x^2-70*x+300};
                        \addplot[closedinterval,color=fourthcolor] coordinates {(-3.25,0) (0,0)};
                        \addplot[soliddot, color=fourthcolor] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[soliddot, color=fifthcolor] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                  </image>

                </figure>
              </sidebyside>

              <p permid="SBm">
                We can see that this region includes all values of <m>t</m> between,
                and including, <m>t=-3.25</m> and <m>t=0</m>.
                So the solutions to this inequality include all values of <m>t</m> for which <m>-3.25\le t \le 0</m>.
                We can write this solution set in interval notation as
                <m>[-3.25,0]</m> or in set-builder notation as <m>\{t\mid -3.25 \leq t \leq 0\}</m>.
              </p>
            </li>

            <li permid="QxE">
              <p permid="ePE">
                To now solve <m>-20t^2-70t+300 \lt -5t+300</m>,
                we will need to determine where the <m>y</m>-values of the graph of
                <m>y=-20t^2-70t+300</m> are <em>less than</em>
                the <m>y</m>-values of the graph of <m>y=-5t+300</m>.
                This region is highlighted in <xref ref="figure-solve-inequality-graphically-lt">Figure</xref>.
              </p>

              <figure xml:id="figure-solve-inequality-graphically-lt" permid="rdW">
                <caption></caption>
                <image width="47%" permid="HsA">
                  <description></description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[xmin=-8,xmax=8,
                    ymin=-100,ymax=400,
                    xtick={-6,-4,...,6},
                    minor xtick={-7,-6,...,7},
                    ytick={-100,0,...,300},
                    minor ytick={-50,0,...,350},
                    xlabel={$t$},
                    clip=false,
                        ]
                        \addplot+[domain=-6.55:3.05] {-20*x^2-70*x+300};
                        \addplot+[domain=-8:8] {-5*x+300};
                        \addplot[highlightcurve,domain=-6.55:-3.25] {-20*x^2-70*x+300};
                        \addplot[highlightcurve,domain=0:3.05] {-20*x^2-70*x+300};
                        \addplot[infiniteopeninterval,color=fourthcolor] coordinates {(-8,0) (-3.25,0)};
                        \addplot[openinfiniteinterval,color=fourthcolor] coordinates {(0,0) (8,0)};
                        \addplot[hollowdot, color=fourthcolor, fill=yellow!30] coordinates {(-3.25,316.25)} node[above right]{$(-3.25,316.25)$};
                        \addplot[hollowdot, color=fifthcolor, fill=yellow!30] coordinates {(0,300)} node[below left]{$(0,300)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
                </image>

              </figure>

              <p permid="KWN">
                We can see that <m>-20t^2-70t+300 \lt -5t+300</m> for all values of <m>t</m> where <m>t\lt -3.25</m> or <m>t\gt 0</m>.
                We can write this solution set in interval notation as
                <m>(-\infty,-3.25)\cup(0,\infty)</m> or in set-builder notation as <m>\{t\mid t\lt -3.25 \text{ or } t\gt 0\}</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
  </subsection>

  <exercises permid="lCS">
<!--TODO: add review and warmup, if appropriate ones exist-->
<!--         <exercisegroup cols="2">
            <title>Review and Warmup</title>
        </exercisegroup>
 -->
    <exercisegroup cols="2" permid="RKb">
      <title>Using Technology to Create a Table of Function Values</title>
      <exercise permid="tRl">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   $a = non_zero_random(-4,4,1);
                   $b = non_zero_random(-19,19,1);
                   $c = non_zero_random(-5,5,1);
                } until (0 &lt; -$b/(2*$a)  and  -$b/(2*$a) &lt; 5);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="ufT">
                Use technology to make a table of values for the function.
              </p>

              <p permid="anc">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="Yjh">

                <tabular halign="center" permid="YqL">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="ZCV">
              <p permid="PzC">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="vGL">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="bNU">
                <md permid="DOa">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="HVd">
                So the table may be completed as:
              </p>

              <sidebyside permid="Eqq">

                <tabular halign="center" permid="nzJ">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="ZYu">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   $a = non_zero_random(-4,4,1);
                   $b = non_zero_random(-19,19,1);
                   $c = non_zero_random(-5,5,1);
                } until (0 &lt; -$b/(2*$a)  and  -$b/(2*$a) &lt; 5);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="Gul">
                Use technology to make a table of values for the function.
              </p>

              <p permid="mBu">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="kxz">

                <tabular halign="center" permid="ExU">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="FKe">
              <p permid="ocm">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="Ujv">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="AqE">
                <md permid="jVj">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="gxN">
                So the table may be completed as:
              </p>

              <sidebyside permid="QEI">

                <tabular halign="center" permid="TGS">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="GfD">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-4,4,0.05);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="SID">
                Use technology to make a table of values for the function.
              </p>

              <p permid="yPM">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="wLR">

                <tabular halign="center" permid="kFd">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="lRn">
              <p permid="MEW">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="sMf">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="YTo">
                <md permid="Qcs">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="Fax">
                So the table may be completed as:
              </p>

              <sidebyside permid="cTa">

                <tabular halign="center" permid="zOb">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="mmM">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-4,4,0.05);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^2 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="eWV">
                Use technology to make a table of values for the function.
              </p>

              <p permid="Lee">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="Jaj">

                <tabular halign="center" permid="QMm">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="RYw">
              <p permid="lhG">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="RoP">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="xvY">
                <md permid="wjB">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="dDh">
                So the table may be completed as:
              </p>

              <sidebyside permid="phs">

                <tabular halign="center" permid="fVk">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="StV">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-12,12,1);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^3 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="rln">
                Use technology to make a table of values for the function.
              </p>

              <p permid="Xsw">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="VoB">

                <tabular halign="center" permid="wTv">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="yfF">
              <p permid="JKq">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="pRz">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="VYI">
                <md permid="cqK">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="CfR">
                So the table may be completed as:
              </p>

              <sidebyside permid="BvK">

                <tabular halign="center" permid="Mct">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="yBe">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                   $a = non_zero_random(-12,12,1);
                   $b = non_zero_random(-200,200,10);
                   $c = non_zero_random(-100,100,1);
                $formula = Compute("$a x^3 + $b*x + $c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="DzF">
                Use technology to make a table of values for the function.
              </p>

              <p permid="jGO">
                <m><var name="$f" />(x)=<var name="$formula" /></m>
              </p>

              <sidebyside permid="hCT">

                <tabular halign="center" permid="daE">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="8" /></cell>
                    <cell><var name="$multians" width="8" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="emO">
              <p permid="ina">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="Ouj">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="uBs">
                <md permid="IxT">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="aIB">
                So the table may be completed as:
              </p>

              <sidebyside permid="NKc">

                <tabular halign="center" permid="sjC">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup cols="2" permid="xRk">
      <title>Determining Appropriate Windows</title>
      <exercise permid="eIn">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $m = random(1001,9999,1)*random(-1,1,2);
                $b = random(1001,9999,1)*random(-1,1,2);
                $func = Formula("$m x + $b")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$b),Point(-$b/$m,0));
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    @xentry=(($ymin-$b)/$m, ($ymax-$b)/$m);
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="PNX">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="vVg">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="KPw">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $m = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $func = Formula("$m x + $b")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$b),Point(-$b/$m,0));
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation
                    @xentry=(($ymin-$b)/$m, ($ymax-$b)/$m);
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="ccp">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="Ijy">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="qWF">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $c = random(100,9999,1)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$c), Point(-$b/(2*$a),$a*(-$b/(2*$a))**2+$b*(-$b/(2*$a))+$c));
                if ($b**2 - 4*$a*$c > 0)
                  {push(@keypoints,Point((-$b-sqrt($b**2 - 4*$a*$c))/(2*$a),0), Point((-$b+sqrt($b**2 - 4*$a*$c))/(2*$a),0));}
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="oqH">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="UxQ">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="XdO">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(10,999,1)*random(-1,1,2);
                $b = random(10,999,1)*random(-1,1,2);
                $c = random(100,9999,1)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$c), Point(-$b/(2*$a),$a*(-$b/(2*$a))**2+$b*(-$b/(2*$a))+$c));
                if ($b**2 - 4*$a*$c > 0)
                  {push(@keypoints,Point((-$b-sqrt($b**2 - 4*$a*$c))/(2*$a),0), Point((-$b+sqrt($b**2 - 4*$a*$c))/(2*$a),0));}
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="AEZ">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="gMi">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="DkX">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(0.0001,0.0005,0.00001)*random(-1,1,2);
                $b = random(0.001,0.005,0.0001)*random(-1,1,2);
                $c = random(0.1,0.9,0.01)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$c), Point(-$b/(2*$a),$a*(-$b/(2*$a))**2+$b*(-$b/(2*$a))+$c));
                if ($b**2 - 4*$a*$c > 0)
                  {push(@keypoints,Point((-$b-sqrt($b**2 - 4*$a*$c))/(2*$a),0), Point((-$b+sqrt($b**2 - 4*$a*$c))/(2*$a),0));}
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="MTr">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="taA">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="jsg">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = random(0.0001,0.0005,0.00001)*random(-1,1,2);
                $b = random(0.001,0.005,0.0001)*random(-1,1,2);
                $c = random(0.1,0.9,0.01)*random(-1,1,2);
                $func = Formula("$a x^2 + $b x + $c")->reduce;
                #important values to appear in plot
                @keypoints = (Point(0,$c), Point(-$b/(2*$a),$a*(-$b/(2*$a))**2+$b*(-$b/(2*$a))+$c));
                if ($b**2 - 4*$a*$c > 0)
                  {push(@keypoints,Point((-$b-sqrt($b**2 - 4*$a*$c))/(2*$a),0), Point((-$b+sqrt($b**2 - 4*$a*$c))/(2*$a),0));}
                @x;
                @y;
                for my $keypoint (@keypoints) {
                  push(@x, ($keypoint->value)[0]);
                  push(@y, ($keypoint->value)[1]);
                };
                #one good answer
                #for x-interval
                $xdiameter = max(@x) - min(@x);
                $xdiameter = 1 if ($xdiameter == 0);
                #Assuming a 160 pixel wide TI-89 screen, set lowx
                # 8 pixels to the left of least special x-value
                # and highx to be 8 pixels to the right of greatest
                $lowx = min(@x) - $xdiameter*8/144;
                $highx = max(@x) + $xdiameter*8/144;
                #Adjust lowx and highx to make resp. smaller and larger with cleaner decimals
                $xwidth = $highx - $lowx;
                $xorder = floor(log10($xwidth));
                $xscale = 10**($xorder-1);
                $lowx = floor($lowx/$xscale)*$xscale;
                $highx = ceil($highx/$xscale)*$xscale;
                #for y-interval
                $ydiameter = max(@y) - min(@y);
                $ydiameter = 1 if ($ydiameter == 0);
                #Assuming a 100 pixel tall TI-89 screen, set lowy
                # 5 pixels below least special y-value
                # and highy to be 5 pixels above greatest
                $lowy = min(@y) - $ydiameter*5/90;
                $highy = max(@y) + $ydiameter*5/90;
                #Adjust lowy and highy to make resp. smaller and larger with cleaner decimals
                $ywidth = $highy - $lowy;
                $yorder = floor(log10($ywidth));
                $yscale = 10**($yorder-1);
                $lowy = floor($lowy/$yscale)*$yscale;
                $highy = ceil($highy/$yscale)*$yscale;
                $x = Interval("[$lowx,$highx]");
                $y = Interval("[$lowy,$highy]");
                $multians = MultiAnswer($x, $y)->with(
                  singleResult => 1,
                  checker => sub {
                    my ($correct, $student, $self) = @_;
                    my ($xstu, $ystu) = @{$student};
                    my ($xcor, $ycor) = @{$correct};
                    my ($xmin,$xmax) = $xstu->value;
                    my ($ymin,$ymax) = $ystu->value;
                    #check end points are not infinite
                    if ($xmin->type eq 'Infinity' or $xmax->type eq 'Infinity' or $ymin->type eq 'Infinity' or $ymax->type eq 'Infinity')
                      {Value::Error("The dimensions of a viewing window cannot be infinite.");}
                    my @errors;
                    #Check important points are all present;
                    for my $keypoint (@keypoints) {
                      if (!$xstu->contains(($keypoint->value)[0]) or !$ystu->contains(($keypoint->value)[1]) ) {
                        push(@errors,"Your viewing window does not contain the important point $keypoint.");
                      }
                    }
                    #Check important points are not right on the boundary;
                    my %xbounds = map { $_ => 1 } ($xmin,$xmax);
                    my %ybounds = map { $_ => 1 } ($ymin,$ymax);
                    for my $keypoint (@keypoints) {
                      if (exists($xbounds{($keypoint->value)[0]}) or exists($ybounds{($keypoint->value)[1]})) {
                        push(@errors,"The important point $keypoint is on the border of your viewing window.");
                      }
                    }
                    if (@errors) {
                      Value::Error(join("$BR",@errors));
                    }
                    #At this point, if for each of the four edges, some key point is within 20% of that edge,
                    #call it good. This may leave other key points appearing very close together.
                    my $xstudiameter = $xmax - $xmin;
                    my $ystudiameter = $ymax - $ymin;
                    my ($rightOK, $topOK, $leftOK, $bottomOK) = (0,0,0,0);
                    for my $keypoint (@keypoints) {
                      if (Interval($xmax-0.2*$xstudiameter,$xmax)->contains(($keypoint->value)[0])) {
                        $rightOK = 1;
                      }
                      if (Interval($ymax-0.2*$ystudiameter,$ymax)->contains(($keypoint->value)[1])) {
                        $topOK = 1;
                      }
                      if (Interval($xmin,$xmin+0.2*$xstudiameter)->contains(($keypoint->value)[0])) {
                        $leftOK = 1;
                      }
                      if (Interval($ymin,$ymin+0.2*$ystudiameter)->contains(($keypoint->value)[1])) {
                        $bottomOK = 1;
                      }
                    }
                    if ($rightOK and $topOK and $leftOK and $bottomOK)
                      {return 1};
                    #Check important points are not visually too close together;
                    #For a 160x100-pixel TI-89, we ask for at least two pixels of separation
                    #using the taxicab metric
                    for my $keypoint1 (@keypoints) {
                      for my $keypoint2 (@keypoints) {
                        if ($keypoint1 != $keypoint2 and abs((($keypoint1->value)[0]-($keypoint2->value)[0])/($xmax - $xmin))*160 + abs((($keypoint1->value)[1]-($keypoint2->value)[1])/($ymax - $ymin))*100 &lt; 2) {
                          Value::Error("Your viewing window makes the important points $keypoint1 and $keypoint2 appear too close together.");
                        }
                      }
                    }
                    #Check that the plot doesn't have the appearance of vertical lines;
                    #For a 160-pixel wide TI-89, we ask for at least four pixels of separation

                    if ($a > 0) {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymax)))/(2*$a));
                    }
                    else {
                      @xentry = ((-$b-sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a), (-$b+sqrt($b**2 - 4*$a*($c-$ymin)))/(2*$a));
                    }
                    for my $xval1 (@xentry) {
                      for my $xval2 (@xentry) {
                        if ($xval1 != $xval2 and abs(($xval1 - $xval2)/($xmax - $xmin)) &lt; 3/160) {
                          Value::Error('Your settings make it appear like a vertical or almost vertical line is part of the plot');
                        }
                      }
                    }
                    return 1;
                  }
                );
            </pg-code>
            </setup>
            <statement>
              <p permid="ZhJ">
                Let <m>f(x)=<var name="$func"/></m>.
                Choose an appropriate window for graphing <m>f</m> that shows its key features.
              </p>
              <instruction>
                Use interval notation. Many answers are possible.
              </instruction>
              <p permid="FoS">
                The <m>x</m>-interval could be <var name="$multians" width="20" /> and the <m>y</m>-interval could be <var name="$multians" width="20" />.
              </p>
            </statement>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup cols="2" permid="dYt">
      <title>Finding Points of Intersection</title>
      <exercise permid="Pzp">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @m = (non_zero_random(-20,20,1), non_zero_random(-20,20,1));
                @b = (non_zero_random(-500,500,1), non_zero_random(-500,500,1));
                $c = non_zero_random(-20000,20000,1000);
                $f = Formula("($b[0]+$m[0]x)($b[1]+$m[1]x)")->reduce;
                $g = Formula("$c");
                $disc = ($m[0]*$b[1]+$m[1]*$b[0])**2 - 4*$m[0]*$m[1]*($b[0]*$b[1]-$c);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="lwb">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="vGy">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @m = (non_zero_random(-20,20,1), non_zero_random(-20,20,1));
                @b = (non_zero_random(-500,500,1), non_zero_random(-500,500,1));
                $c = non_zero_random(-20000,20000,1000);
                $f = Formula("($b[0]+$m[0]x)($b[1]+$m[1]x)")->reduce;
                $g = Formula("$c");
                $disc = ($m[0]*$b[1]+$m[1]*$b[0])**2 - 4*$m[0]*$m[1]*($b[0]*$b[1]-$c);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="RDk">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="bNH">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (non_zero_random(-9,9,1), 0);
                @b = (random(-3,3,1), 0);
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @d = (0, non_zero_random(-9,9,1));
                $f = Formula("$a[0]x^3 + $b[0]x^2 + $c[0]x + $d[0]")->reduce;
                $g = Formula("$a[1]x^3 + $b[1]x^2 + $c[1]x + $d[1]")->reduce;

                $disc = ($b[0]-$b[1])**2*($c[0]-$c[1])**2 - 4*($a[0]-$a[1])*($c[0]-$c[1])**3 - 4*($b[0]-$b[1])**3*($d[0]-$d[1]) -27*($a[0]-$a[1])**2*($d[0]-$d[1])**2 + 18*($a[0]-$a[1])*($b[0]-$b[1])*($c[0]-$c[1])*($d[0]-$d[1]);
                if ($disc > 0) {$ans = 3;}
                elsif ($disc == 0) {
                    if (($g/$c[1])^3 == $f/$a[0]) {$ans = 1;}
                    else {$ans = 2;}
                }
                else {$ans = 1;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="xKt">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="HUQ">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (non_zero_random(-9,9,1), 0);
                @b = (random(-3,3,1), 0);
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @d = (0, non_zero_random(-9,9,1));
                $f = Formula("$a[0]x^3 + $b[0]x^2 + $c[0]x + $d[0]")->reduce;
                $g = Formula("$a[1]x^3 + $b[1]x^2 + $c[1]x + $d[1]")->reduce;

                $disc = ($b[0]-$b[1])**2*($c[0]-$c[1])**2 - 4*($a[0]-$a[1])*($c[0]-$c[1])**3 - 4*($b[0]-$b[1])**3*($d[0]-$d[1]) -27*($a[0]-$a[1])**2*($d[0]-$d[1])**2 + 18*($a[0]-$a[1])*($b[0]-$b[1])*($c[0]-$c[1])*($d[0]-$d[1]);
                if ($disc > 0) {$ans = 3;}
                elsif ($disc == 0) {
                    if (($g/$c[1])^3 == $f/$a[0]) {$ans = 1;}
                    else {$ans = 2;}
                }
                else {$ans = 1;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="dRC">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise width="wide" permid="obZ">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (random(1,9,1), 0);
                @b = (0, random(1,9,1));
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @s = (non_zero_random(-0.9,0.9,0.1), non_zero_random(-0.9,0.9,0.01));

                $f = Formula("$s[0]($a[0] x^2 + $b[0] x + $c[0])")->reduce;
                $g = Formula("$s[1]($a[1] x^2 + $b[1] x + $c[1])")->reduce;

                $disc = ($s[0]*$b[0]-$s[1]*$b[1])**2 - 4*($s[0]*$a[0]-$s[1]*$a[1])*($s[0]*$c[0]-$s[1]*$c[1]);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="JYL">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise width="wide" permid="Uji">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                @a = (random(1,9,1), 0);
                @b = (0, random(1,9,1));
                @c = (non_zero_random(-9,9,1), non_zero_random(-9,9,1));
                @s = (non_zero_random(-0.9,0.9,0.1), non_zero_random(-0.9,0.9,0.01));

                $f = Formula("$s[0]($a[0] x^2 + $b[0] x + $c[0])")->reduce;
                $g = Formula("$s[1]($a[1] x^2 + $b[1] x + $c[1])")->reduce;

                $disc = ($s[0]*$b[0]-$s[1]*$b[1])**2 - 4*($s[0]*$a[0]-$s[1]*$a[1])*($s[0]*$c[0]-$s[1]*$c[1]);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="qfU">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="Aqr">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = non_zero_random(-2,2,0.05);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,0.05);
                $m = non_zero_random(-2,2,0.05);
                $b = random(-2,2,1);

                $f = Formula("$a(x - $h)^2+$k")->reduce;
                $g = Formula("$m x + $b")->reduce;

                $disc = (-2*$h*$a - $m)^2 - 4*$a*($a*$h**2+$k-$b);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="Wnd">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="gxA">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)-y','(-x)+y');
                $a = non_zero_random(-2,2,0.05);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,0.05);
                $m = non_zero_random(-2,2,0.05);
                $b = random(-2,2,1);

                $f = Formula("$a(x - $h)^2+$k")->reduce;
                $g = Formula("$m x + $b")->reduce;

                $disc = (-2*$h*$a - $m)^2 - 4*$a*($a*$h**2+$k-$b);
                if ($disc > 0) {$ans = 2;}
                elsif ($disc == 0) {$ans = 1;}
                else {$ans = 0;}
                $popup = PopUp(['?','zero times', 'one time', 'two times', 'three times'],$ans+1);
            </pg-code>
            </setup>
            <statement>
              <p permid="iBv">
                Use technology to determine how many times the equations <m>y=<var name="$f" /></m> and <m>y=<var name="$g" /></m> intersect.
                They intersect <var name="$popup" form="popup" />.
              </p>
            </statement>
        </webwork>
      </exercise>

    </exercisegroup>
<!--TODO: All subsequent problems here do not use randomization. Make them use randomization. Update the problems in the chapter review when randomized. -->
    <exercisegroup cols="2" permid="KfC">
      <title>Using Technology to Find Key Features of a Graph</title>
      <exercise permid="MEJ">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(-0.873,0), (6.873,0), (0,2.4)");
                $vertex = Point("(3,6)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("(-inf,6]");
            </pg-code>
            </setup>
            <statement>
              <p permid="OIE">
                For the function <m>j</m> defined by
                <me permid="oFc">
                  j(x)=-\frac{2}{5}(x-3)^2+6
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="Zhw">
                  <li permid="wEN">
                    <p permid="Xlf">
                      Any intercepts.
                    </p>

                    <p permid="Dso">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="cLW">
                    <p permid="jzx">
                      The vertex.
                    </p>

                    <p permid="PGG">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="ITf">
                    <p permid="vNP">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="bUY">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="pao">
                    <p permid="Ich">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="ojq">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="KtX">
              <p permid="GPK">
                <ol label="a." permid="LdR">
                  <li permid="Vhx">
                    <p permid="Uqz">
                      The horizontal intercepts are about
                      <m>(-0.873,0)</m> and <m>(6.873,0)</m>.
                      The vertical intercept is <m>(0,2.4)</m>.
                    </p>
                  </li>

                  <li permid="BoG">
                    <p permid="AxI">
                      The vertex is <m>(3,6)</m>.
                    </p>
                  </li>

                  <li permid="hvP">
                    <p permid="gER">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>(-\infty,6]</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="sLS">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(0,12)");
                $vertex = Point("(-1,10)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[10,inf)");
            </pg-code>
            </setup>
            <statement>
              <p permid="uPN">
                For the function <m>k</m> defined by
                <me permid="UMl">
                  k(x)=2(x+1)^2+10
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="FoF">
                  <li permid="NCY">
                    <p permid="MMa">
                      Any intercepts.
                    </p>

                    <p permid="sTj">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="tKh">
                    <p permid="Zas">
                      The vertex.
                    </p>

                    <p permid="FhB">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="ZRq">
                    <p permid="loK">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="RvT">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="FYz">
                    <p permid="xDc">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="dKl">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="qBg">
              <p permid="mWT">
                <ol label="a." permid="rla">
                  <li permid="mfI">
                    <p permid="JRu">
                      There are no horizontal intercepts.
                      The vertical intercept is <m>(0,12)</m>.
                    </p>
                  </li>

                  <li permid="SmR">
                    <p permid="pYD">
                      The vertex is <m>(-1,10)</m>.
                    </p>
                  </li>

                  <li permid="yua">
                    <p permid="WfM">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>[10,\infty)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="YTb">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(0,4)");
                $vertex = Point("(-0.002,3.992)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[3.992,inf)");
            </pg-code>
            </setup>
            <statement>
              <p permid="aWW">
                For the function <m>L</m> defined by
                <me permid="ATu">
                  L(x)=3000x^2+10x+4
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="lvO">
                  <li permid="eBj">
                    <p permid="CmV">
                      Any intercepts.
                    </p>

                    <p permid="iue">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="KIs">
                    <p permid="OBn">
                      The vertex.
                    </p>

                    <p permid="uIw">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="qPB">
                    <p permid="aPF">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="GWO">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="WWK">
                    <p permid="ndX">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="Tlg">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="WIp">
              <p permid="Tec">
                <ol label="a." permid="Xsj">
                  <li permid="DdT">
                    <p permid="zsp">
                      There are no horizontal intercepts.
                      The vertical intercept is <m>(0,4)</m>.
                    </p>
                  </li>

                  <li permid="jlc">
                    <p permid="fzy">
                      The vertex is about <m>(-0.002,3.992)</m>.
                    </p>
                  </li>

                  <li permid="Psl">
                    <p permid="LGH">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>[3.992,\infty)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Fak">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(9.883,0), (0,-8702500)");
                $vertex = Point("(9.883,0)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("(-inf,0]");
            </pg-code>
            </setup>
            <statement>
              <p permid="Hef">
                For the function <m>M</m> defined by
                <me permid="haD">
                  M(x)=-(300x-2950)^2
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="RCX">
                  <li permid="vzu">
                    <p permid="rNQ">
                      Any intercepts.
                    </p>

                    <p permid="XUZ">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="bGD">
                    <p permid="Eci">
                      The vertex.
                    </p>

                    <p permid="kjr">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="HNM">
                    <p permid="QqA">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="wxJ">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="nUV">
                    <p permid="cES">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="IMb">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="CPy">
              <p permid="zll">
                <ol label="a." permid="Dzs">
                  <li permid="Uce">
                    <p permid="oTk">
                      The horizontal intercept is about <m>(9.833,0)</m>.
                      The vertical intercept is <m>(0,-8702500)</m>.
                    </p>
                  </li>

                  <li permid="Ajn">
                    <p permid="Vat">
                      The vertex is about <m>(9.833,0)</m>.
                    </p>
                  </li>

                  <li permid="gqw">
                    <p permid="BhC">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>(-\infty,0]</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="lht">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(0.0035,0), (0,1.1025)");
                $vertex = Point("(0.0035,0)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[0,inf)");
            </pg-code>
            </setup>
            <statement>
              <p permid="nlo">
                For the function <m>N</m> defined by
                <me permid="NhM">
                  N(x)=(300x-1.05)^2
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="xKg">
                  <li permid="MxF">
                    <p permid="hoL">
                      Any intercepts.
                    </p>

                    <p permid="NvU">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="sEO">
                    <p permid="tDd">
                      The vertex.
                    </p>

                    <p permid="ZKm">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="YLX">
                    <p permid="FRv">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="lYE">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="ETg">
                    <p permid="SfN">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="ymW">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="iWH">
              <p permid="fsu">
                <ol label="a." permid="jGB">
                  <li permid="lap">
                    <p permid="euf">
                      The horizontal intercept is <m>(0.0035,0)</m>.
                      The vertical intercept is <m>(0,1.1025)</m>.
                    </p>
                  </li>

                  <li permid="Rhy">
                    <p permid="KBo">
                      The vertex is <m>(0.0035,0)</m>.
                    </p>
                  </li>

                  <li permid="xoH">
                    <p permid="qIx">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>[0,\infty)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="RoC">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intercepts = List("(0.002,0), (0.003,0), (0.0006)");
                $vertex = Point("(0.025,-0.000025)");
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range = Interval("[-0.000025,inf)");
            </pg-code>
            </setup>
            <statement>
              <p permid="Tsx">
                For the function <m>B</m> defined by
                <me permid="toV">
                  B(x) = x^2 - 0.05 x + 0.0006
                </me>,
                use technology to determine the following.
                Round answers as necessary.

                <ol label="a.'" permid="dRp">
                  <li permid="dvQ">
                    <p permid="WPG">
                      Any intercepts.
                    </p>

                    <p permid="CWP">
                      <var name="$intercepts" width="30" />
                    </p>
                  </li>

                  <li permid="JCZ">
                    <p permid="jdY">
                      The vertex.
                    </p>

                    <p permid="Plh">
                      <var name="$vertex" width="30" />
                    </p>
                  </li>

                  <li permid="pKi">
                    <p permid="vsq">
                      The domain.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="bzz">
                      <var name="$domain" width="30" />
                    </p>
                  </li>

                  <li permid="VRr">
                    <p permid="HGI">
                      The range.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                    <p permid="nNR">
                      <var name="$range" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="PdQ">
              <p permid="LzD">
                <ol label="a." permid="PNK">
                  <li permid="BYA">
                    <p permid="TVa">
                      The horizontal intercepts are
                      <m>(0.002,0)</m> and <m>(0.003,0)</m>.
                      The vertical intercept is <m>(0,0.0006)</m>.
                    </p>
                  </li>

                  <li permid="ifJ">
                    <p permid="Acj">
                      The vertex is <m>(0.025,-0.000025)</m>.
                    </p>
                  </li>

                  <li permid="OmS">
                    <p permid="gjs">
                      The domain is <m>(-\infty,\infty)</m>.
                      The range is <m>[-0.000025,\infty)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup cols="2" permid="qmL">
      <title>Solving Equations and Inequalities Graphically Using Technology</title>
      <exercise permid="xvL">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-10,50), (15,25)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-10,15}");
                Context("Interval");
                $ineq[0] = Interval("(-inf,-10)U(15,inf)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="zzG">
                Let <m>s(x)=\frac{1}{5}x^2-2x+10</m> and <m>t(x)=-x+40</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="JYy">
                  <li permid="uub">
                    <p permid="MqB">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="sxK">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="aBk">
                    <p permid="YET">
                      Solve <m>s(x)=t(x)</m>.
                    </p>

                    <p permid="EMc">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="GIt">
                    <p permid="kTl">
                      Solve <m>s(x)\gt t(x)</m>.
                    </p>

                    <p permid="Rau">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="mPC">
                    <p permid="xhD">
                      Solve <m>s(x)\leq t(x)</m>.
                    </p>

                    <p permid="doM">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="vkZ">
              <p permid="rGM">
                <ol label="a." permid="vUT">
                  <li permid="SWL">
                    <p permid="JvV">
                      <m>(-10,-50)</m> and <m>(15,25)</m>
                    </p>
                  </li>

                  <li permid="zdU">
                    <p permid="pDe">
                      <m>\{-10,15\}</m>
                    </p>
                  </li>

                  <li permid="fld">
                    <p permid="VKn">
                      <m>(-\infty,-10)\cup(15,\infty)</m>
                    </p>
                  </li>

                  <li permid="Lsm">
                    <p permid="BRw">
                      <m>[-10,15]</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="dCU">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-4,8), (20,32)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-4,20}");
                Context("Interval");
                $ineq[0] = Interval("(-inf,-4)U(20,inf)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="fGP">
                Let <m>w(x)=\frac{1}{4}x^2-3x-8</m> and <m>m(x)=x+12</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="qfH">
                  <li permid="rzv">
                    <p permid="hYF">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="OfO">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="XGE">
                    <p permid="umX">
                      Solve <m>w(x)=m(x)</m>.
                    </p>

                    <p permid="aug">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="DNN">
                    <p permid="GBp">
                      Solve <m>w(x)\gt m(x)</m>.
                    </p>

                    <p permid="mIy">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="jUW">
                    <p permid="SPH">
                      Solve <m>w(x)\leq m(x)</m>.
                    </p>

                    <p permid="yWQ">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="bsi">
              <p permid="XNV">
                <ol label="a." permid="ccc">
                  <li permid="Qcf">
                    <p permid="fdZ">
                      <m>(-4,8)</m> and <m>(20,32)</m>
                    </p>
                  </li>

                  <li permid="wjo">
                    <p permid="Lli">
                      <m>\{-4,20\}</m>
                    </p>
                  </li>

                  <li permid="cqx">
                    <p permid="rsr">
                      <m>(-\infty,-4)\cup(20,\infty)</m>
                    </p>
                  </li>

                  <li permid="IxG">
                    <p permid="XzA">
                      <m>[-4,20]</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="JKd">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-2,5), (0.75,5)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-2,0.75}");
                Context("Interval");
                $ineq[0] = Interval("(-2,0.75)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="LNY">
                Let <m>f(x)=4x^2+5x-1</m> and <m>g(x)=5</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="WmQ">
                  <li permid="oEP">
                    <p permid="DGJ">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="jNS">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="ULY">
                    <p permid="PVb">
                      Solve <m>f(x)=g(x)</m>.
                    </p>

                    <p permid="wck">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="ATh">
                    <p permid="cjt">
                      Solve <m>f(x)\lt g(x)</m>.
                    </p>

                    <p permid="IqC">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="haq">
                    <p permid="oxL">
                      Solve <m>f(x)\geq g(x)</m>.
                    </p>

                    <p permid="UEU">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="Hzr">
              <p permid="DVe">
                <ol label="a." permid="Ijl">
                  <li permid="Nhz">
                    <p permid="AMd">
                      <m>(-2,5)</m> and <m>(0.75,5)</m>
                    </p>
                  </li>

                  <li permid="toI">
                    <p permid="gTm">
                      <m>\{-2,0.75\}</m>
                    </p>
                  </li>

                  <li permid="ZvR">
                    <p permid="Nav">
                      <m>(-2,0.75)</m>
                    </p>
                  </li>

                  <li permid="FDa">
                    <p permid="thE">
                      <m>(-\infty,-2]\cup[0.75,\infty)</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="pRm">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-0.5,7), (1,7)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-0.5,1}");
                Context("Interval");
                $ineq[0] = Interval("(-0.5,1)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="rVh">
                Let <m>p(x)=6x^2-3x+4</m> and <m>k(x)=7</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="CtZ">
                  <li permid="lKj">
                    <p permid="ZoN">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="FvW">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="RRs">
                    <p permid="lDf">
                      Solve <m>p(x)=k(x)</m>.
                    </p>

                    <p permid="RKo">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="xYB">
                    <p permid="xRx">
                      Solve <m>p(x)\lt k(x)</m>.
                    </p>

                    <p permid="dYG">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="efK">
                    <p permid="KfP">
                      Solve <m>p(x)\geq k(x)</m>.
                    </p>

                    <p permid="qmY">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="nGA">
              <p permid="kcn">
                <ol label="a." permid="oqu">
                  <li permid="KmT">
                    <p permid="Wuh">
                      <m>(-0.5,7)</m> and <m>(1,7)</m>
                    </p>
                  </li>

                  <li permid="quc">
                    <p permid="CBq">
                      <m>\{-0.5,1\}</m>
                    </p>
                  </li>

                  <li permid="WBl">
                    <p permid="iIz">
                      <m>(-0.5,1)</m>
                    </p>
                  </li>

                  <li permid="CIu">
                    <p permid="OPI">
                      <m>(-\infty,-0.5]\cup[1,\infty)</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="VYv">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-6,10), (-0.5,21)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-6,-0.5}");
                Context("Interval");
                $ineq[0] = Interval("(-6,-0.5)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="Ycq">
                Let <m>q(x)=-4x^2-24x+10</m> and <m>r(x)=2x+22</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="iBi">
                  <li permid="iPD">
                    <p permid="uWR">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="bea">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="OWM">
                    <p permid="Hlj">
                      Solve <m>q(x)=r(x)</m>.
                    </p>

                    <p permid="nss">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="vdV">
                    <p permid="TzB">
                      Solve <m>q(x)\gt r(x)</m>.
                    </p>

                    <p permid="zGK">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="ble">
                    <p permid="fNT">
                      Solve <m>q(x)\leq r(x)</m>.
                    </p>

                    <p permid="LVc">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="TNJ">
              <p permid="Qjw">
                <ol label="a." permid="UxD">
                  <li permid="Hsn">
                    <p permid="scl">
                      <m>(-6,10)</m> and <m>(-0.5,21)</m>
                    </p>
                  </li>

                  <li permid="nzw">
                    <p permid="Yju">
                      <m>\{-6,-0.5\}</m>
                    </p>
                  </li>

                  <li permid="TGF">
                    <p permid="EqD">
                      <m>(-6,-0.5)</m>
                    </p>
                  </li>

                  <li permid="zNO">
                    <p permid="kxM">
                      <m>(-\infty,-6]\cup[-0.5,\infty)</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="CfE">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                $intersections = List("(-1.2,-5.4), (1,-12)");
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-1.2,1}");
                Context("Interval");
                $ineq[0] = Interval("(-1.2,1)");
                $ineq[1] = Compute("R - $ineq[0]")->reduce->sort;
            </pg-code>
            </setup>
            <statement>
              <p permid="Ejz">
                Let <m>h(x)=-10x^2-5x+3</m> and <m>j(x)=-3x-9</m>.
                Use graphing technology to determine the following.

                <ol label="a." permid="OIr">
                  <li permid="fUX">
                    <p permid="QEV">
                      What are the points of intersection for these two functions?
                    </p>

                    <p permid="wMe">
                      <var name="$intersections" width="30" />
                    </p>
                  </li>

                  <li permid="Mcg">
                    <p permid="cTn">
                      Solve <m>h(x)=j(x)</m>.
                    </p>

                    <p permid="Jaw">
                      <var name="$solutions" width="30" />
                    </p>
                  </li>

                  <li permid="sjp">
                    <p permid="phF">
                      Solve <m>h(x)\gt j(x)</m>.
                    </p>

                    <p permid="VoO">
                      <var name="$ineq[0]" width="30" />
                    </p>
                  </li>

                  <li permid="Yqy">
                    <p permid="BvX">
                      Solve <m>h(x)\leq j(x)</m>.
                    </p>

                    <p permid="hDg">
                      <var name="$ineq[1]" width="30" />
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="zUS">
              <p permid="wqF">
                <ol label="a." permid="AEM">
                  <li permid="ExH">
                    <p permid="NKp">
                      <m>(-1.2,-5.4)</m> and <m>(1,-12)</m>
                    </p>
                  </li>

                  <li permid="kEQ">
                    <p permid="tRy">
                      <m>\{-1.2,1\}</m>
                    </p>
                  </li>

                  <li permid="QLZ">
                    <p permid="ZYH">
                      <m>(-1.2,1)</m>
                    </p>
                  </li>

                  <li permid="wTi">
                    <p permid="GfQ">
                      <m>(-\infty,-1.2]\cup[1,\infty)</m>
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="imN">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-3.25,2}");
            </pg-code>
            </setup>
            <statement>
              <p permid="kqI">
                Use graphing technology to solve the equation <m>0.4x^2+0.5x-0.2=2.4</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="QxR">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="gcb">
              <p permid="cxO">
                <m>\{-3.25,2\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="OtW">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-6,2}");
            </pg-code>
            </setup>
            <statement>
              <p permid="wFa">
                Use graphing technology to solve the equation <m>-0.25x^2-2x+1.75=4.75</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="cMj">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="Mjk">
              <p permid="IEX">
                <m>\{-6,2\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="uBf">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-10,20}");
            </pg-code>
            </setup>
            <statement>
              <p permid="ITs">
                Use graphing technology to solve the equation <m>(200+5x)(100-2x)=15000</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="paB">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="sqt">
              <p permid="oMg">
                <m>\{-10,20\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="aIo">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{0,30}");
            </pg-code>
            </setup>
            <statement>
              <p permid="VhK">
                Use graphing technology to solve the equation <m>\left(200-5x\right)\left(100+10x\right)=20000</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="BoT">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="YxC">
              <p permid="UTp">
                <m>\{0,30\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="GPx">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-1.5,0,1.5}");
            </pg-code>
            </setup>
            <statement>
<!--3, cubic-->
              <p permid="hwc">
                Use graphing technology to solve the equation <m>2x^3-5x+1=-\frac{1}{2}x+1</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="NDl">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="EEL">
              <p permid="Bay">
                <m>\{-1.5,0,1.5\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="mWG">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{2}");
            </pg-code>
            </setup>
            <statement>
<!--1, cubic-->
              <p permid="tKu">
                Use graphing technology to solve the equation <m>-x^3+8x=-4x+16</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="ZRD">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="kLU">
              <p permid="hhH">
                <m>\{2\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="TdP">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{-23.162,-16.838}");
            </pg-code>
            </setup>
            <statement>
<!--2-->
              <p permid="FYM">
                Use graphing technology to solve the equation <m>-0.05 x^2 - 2.03 x - 19.6=0.05 x^2 + 1.97 x + 19.4</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="mfV">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="QTd">
              <p permid="NoQ">
                <m>\{x\mid x\approx -23.162, x\approx -16.838\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="zkY">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Compute("no solutions");
            </pg-code>
            </setup>
            <statement>
<!--0-->
              <p permid="Sne">
                Use graphing technology to solve the equation <m>-0.02 x^2 + 1.97 x - 51.5=0.05\left(x-50\right)^2-0.03\left(x-50\right)</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="yun">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="xam">
              <p permid="tvZ">
                No solution.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="fsh">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Compute("no solutions");
            </pg-code>
            </setup>
            <statement>
<!--0-->
              <p permid="eBw">
                Use graphing technology to solve the equation <m>-200x^2+60x-55=-20x-40</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="KIF">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="dhv">
              <p permid="ZDi">
                No solution.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Lzq">
        <webwork>
            <setup>

            <pg-code>
                Context("FiniteSolutionSets");
                Context()->flags->remove("NumberCheck");
                $solutions = Formula("{0.094,0.706}");
            </pg-code>
            </setup>
            <statement>
<!--2-->
              <p permid="qPO">
                Use graphing technology to solve the equation <m>150x^2-20x+50=100x+40</m>.
                Approximate the solution(s) if necessary.
              </p>

              <p permid="WWX">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="JoE">
              <p permid="FKr">
                <m>\{x \mid x\approx 0.094, x\approx 0.706\}</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="rGz">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-2)U(-0.5,inf)");
            </pg-code>
            </setup>
            <statement>
<!--gt-->
              <p permid="Deg">
                Use graphing technology to solve the inequality <m>2x^2+5x-3\gt -5</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="jlp">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="pvN">
              <p permid="lRA">
                <m>(-\infty,-2)\cup(-0.5,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="XNI">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-1,5)");
            </pg-code>
            </setup>
            <statement>
<!--gt-->
              <p permid="Psy">
                Use graphing technology to solve the inequality <m>-x^2+4x-7 \gt -12</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="vzH">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="VCW">
              <p permid="RYJ">
                <m>(-1,5)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="DUR">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("[0,1.1]");
            </pg-code>
            </setup>
            <statement>
<!--leq-->
              <p permid="bGQ">
                Use graphing technology to solve the inequality <m>10x^2-11x+7\leq7</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="HNZ">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="BKf">
              <p permid="yfS">
                <m>[0,1.1]</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="kca">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-1]U[-0.5,inf)");
            </pg-code>
            </setup>
            <statement>
<!--leq-->
              <p permid="nVi">
                Use graphing technology to solve the inequality <m>-10x^2-15x+4\leq 9</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="Ucr">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="hRo">
              <p permid="enb">
                <m>(-\infty,-1]\cup[-0.5,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Qjj">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-6.372,-0.628)");
            </pg-code>
            </setup>
            <statement>
<!--gt-->
              <p permid="AjA">
                Use graphing technology to solve the inequality <m>-x^2-6x+1>x+5</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="gqJ">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="NYx">
              <p permid="Kuk">
                <m>(-6.372,-0.628)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="wqs">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,-2.907)U(0.573,inf)");
            </pg-code>
            </setup>
            <statement>
<!--gt-->
              <p permid="MxS">
                Use graphing technology to solve the inequality <m>3x^2+5x-4>-2x+1</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="sFb">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="ufG">
              <p permid="qBt">
                <m>(-\infty,-2.907)\cup(0.573,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="cxB">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $a = 20;
                $b = -34 + 10;
                $c = 6 - 4;
                @r = ((-$b-sqrt(($b)**2-4*$a*$c))/(2*$a),(-$b+sqrt(($b)**2-4*$a*$c))/(2*$a));
                ($low,$high) = (min(@r),max(@r));
                $solutions = OneOf(Interval("(-inf,$low]U[$high,inf)"), Interval("(-inf,0.09]U[$high,inf)"));
            </pg-code>
            </setup>
            <statement>
<!--leq-->
              <p permid="YMk">
                Use graphing technology to solve the inequality <m>-10x+4\leq 20x^2-34x+6</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="ETt">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="amP">
              <p permid="WIC">
                <m>(-\infty,0.09]\cup[1.11,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="IEK">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,inf)");
            </pg-code>
            </setup>
            <statement>
<!--lt arn-->
              <p permid="laC">
                Use graphing technology to solve the inequality <m>-15x^2-6\leq 10x-4</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="RhL">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="GtY">
              <p permid="CPL">
                <m>(-\infty,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="oLT">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("(-inf,inf)");
            </pg-code>
            </setup>
            <statement>
<!--gt arn-->
              <p permid="xoU">
                Use graphing technology to solve the inequality <m>\frac{1}{2}x^2+\frac{3}{2}x \geq \frac{1}{2}x-\frac{3}{2}</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="dwd">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="mBh">
              <p permid="iWU">
                <m>(-\infty,\infty)</m>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="UTc">
        <webwork>
            <setup>

            <pg-code>
                Context("Interval");
                $solutions = Interval("[0,15]");
            </pg-code>
            </setup>
            <statement>
<!--geq-->
              <p permid="JDm">
                Use graphing technology to solve the inequality <m>\frac{3}{4}x\geq \frac{1}{4}x^2-3x</m>.
                State the solution set using interval notation,
                and approximate if necessary.
              </p>

              <p permid="pKv">
                <var name="$solutions" width="30" />
              </p>
            </statement>
            <solution permid="SIq">
              <p permid="Ped">
                <m>[0,15]</m>
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
  </exercises>
</section>

