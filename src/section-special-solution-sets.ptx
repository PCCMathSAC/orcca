<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2019                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!--                                                                      -->
<!-- Open Resources for Community College Algebra is licensed under a     -->
<!-- Creative Commons Attribution 4.0 International License.              -->
<!--                                                                      -->
<!-- Under this license, any user of this textbook or the textbook        -->
<!-- contents must provide proper attribution as follows.                 -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- include in every digital format page view (including but not         -->
<!-- limited to EPUB, PDF, and HTML) and on every physical printed page   -->
<!-- the following attribution:                                           -->
<!--                                                                      -->
<!-- Original source material, products with readable and accessible      -->
<!-- math content, and other information freely available at              -->
<!-- pcc.edu/orcca.                                                       -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- reproduce any math content (such as math expressions and equations)  -->
<!-- in a readable manner, and offer math content in at least one web     -->
<!-- accessible manner.                                                   -->
<!--                                                                      -->
<!-- The Portland Community College name, Portland Community College      -->
<!-- logo, ORCCA name, Open Resources for Community College Algebra name, -->
<!-- ORCCA logo, and front and back cover designs are not subject to the  -->
<!-- Creative Commons license and may not be reproduced without the       -->
<!-- prior and express written consent of Portland Community College.     -->
<!--                                                                      -->
<!-- For questions regarding this license, please contact                 -->
<!-- orcca-group@pcc.edu.                                                 -->
<!-- *********************************************************************-->
<section xml:id="section-special-solution-sets">
  <title>Special Solution Sets</title>
  <objectives>
    <title>PCC Course Content and Outcome Guide</title>
    <ul>
      <li>
        <xref ref="ccog-solve-linear-equations-symbolically" text="local">MTH 60 CCOG</xref>
      </li>
    </ul>
  </objectives>

  <introduction>
    <p>
      Most of the time after solving a linear equation in one variable, you end with a direct
      statement of what the variable must equal. For example, <m>2x+8=14</m> concludes with
      <m>x=3</m>. There is only one solution, <m>3</m> in this case.
    </p>
    <p>
      Similarly, after solving a linear inequality, you typically end with a statement like
      <m>x\lt5</m>, and the solution set is represented with either interval notation like
      <m>(-\infty,5)</m>, or with set-builder notation like <m>\{x\mid x\lt5\}</m>. Visually on a
      number line, it is either the left half or the right half of the number line.
    </p>

    <p>
      <em>Occasionally</em> you will have a linear equation or inequality that doesn't work this
      way. A linear equation might have <em>many</em> solutions, not just one. Or it might have
      <em>none at all</em>. An inequality might also have no solutions. Or maybe every number on the
      entire number line will be a solution. In this section we explore these kinds of equations and
      inequalities, where the solution set is <q>special</q>.
    </p>

    <figure>
      <caption>Alternative Video Lessons</caption>
      <video youtubeplaylist="PLmuZCPhWPt-esiq8AiMfACzqOpC5bJygR"/>
    </figure>
  </introduction>

  <subsection>
    <title>Special Solution Sets</title>
    <p>
      Recall that for the equation <m>x+2=5</m>, there is only one number which will make the
      equation true: <m>3</m>. This means that the solution is <m>3</m>, and we write the
      <term>solution set</term> as <m>\{3\}</m>. We say the equation's solution set has one
      <term>element</term>, <m>3</m>.
    </p>

    <p>
      We'll now explore equations where <em>all real numbers</em> are solutions, and other equations
      where <em>no real number</em> is a solution.
    </p>

    <example>
      <p>
        Solve for <m>x</m> in <m>3x=3x+4</m>.
      </p>
      <p>
        To solve this equation, we need to move all terms containing <m>x</m> to one side of the
        equals sign. We set out to remove the <m>3x</m> from the right side of the equation, by
        subtracting <m>3x</m> from each side:
        <md>
          <mrow>3x\amp=3x+4</mrow>
          <mrow>3x\subtractright{3x}\amp=3x+4\subtractright{3x}</mrow>
          <mrow>0\amp=4</mrow>
        </md>
      </p>
      <p>
        But now <m>x</m> is no longer present in the equation. Our effort to remove <m>3x</m> from            <idx><h>empty set</h></idx>
        the <em>right</em> side coincidentally removed it from the <em>left</em> side too. What
        value can we substitute in for <m>x</m> to make <m>0=4</m> become true? That's a trick
        question; you could never make <m>0=4</m> become true. We say this equation has <q>no
        solution</q>. Or we say that the equation has an <q>empty solution set</q>. We can write an
        empty solution set as <m>\emptyset</m>, or <m>\{\text{ }\}</m>. (Be careful notto confuse
        the empty set symbol <m>\emptyset</m> with the number zero, <m>0</m>.)
      </p>
      <p>
        The equation <m>0=4</m> isunambiguously false no matter what <m>x</m> might be. This shows
        us there is no solution to the original equation.
      </p>
    </example>

    <example xml:id="example-solving-linear-equation-identity-intro">
      <p>
        Solve for <m>x</m> in <m>2x+1=2x+1</m>.
      </p>
      <p>
        We will move all terms containing <m>x</m> to one side of the equals sign:
        <md>
          <mrow>2x+1\amp=2x+1</mrow>
          <mrow>2x+1\subtractright{2x}\amp=2x+1\subtractright{2x}</mrow>
          <mrow>1\amp=1</mrow>
        </md>
      </p>
      <p>
        Once again, <m>x</m> is no longer present in the equation. What value can we substitute in
        for <m>x</m> to make <m>1=1</m> be true? This is another trick question; <em>any</em> value
        for <m>x</m> would work. This means that all real numbers are solutions to the equation
        <m>2x+1=2x+1</m>. We say this equation's solution set contains <em>all real numbers</em>.
        We can write this set using interval notation as <m>(-\infty,\infty)</m>, or use
        <m>\mathbb{R}</m> as an abbreviation for the set of all real numbers.
      </p>

      <p>
        The equation <m>0=4</m> isunambiguously true no matter what <m>x</m> might be. This shows
        us that all real numbers are solutions to the original linear equation.
      </p>
    </example>

    <remark>
      <p>
        What would have happened if we had continued the solving process after we obtained
        <m>1=1</m> in <xref ref="example-solving-linear-equation-identity-intro">Example</xref>?
        <md>
          <mrow>1\amp=1</mrow>
          <mrow>1\subtractright{1}\amp=1\subtractright{1}</mrow>
          <mrow>0\amp=0</mrow>
        </md>
        All we found was another unambiguously true equation, and we still conclude that all real
        numbers are solutions.
      </p>
    </remark>

    <warning>
      <p>
        Note that there is a very important difference when our process ends with <m>0=0</m>,
        compared to when it ends with <m>x=0</m>. The first equation is true for all values that
        <m>x</m> might have, and the solution set is <m>(-\infty,\infty)</m>. The second situation
        has only one solution, <m>0</m>, and the solution set is <m>\{0\}</m>.
      </p>
    </warning>

    <example>
      <p>
        Solve for <m>t</m> in the inequality <m>4t+5\gt 4t+2</m>.
      </p>

      <p>
        To solve for <m>t</m>, we aim to eliminate that <m>4t</m> on the right side by subtracting
        <m>4t</m> from each side:
        <md>
          <mrow>4t+5\amp\gt 4t+2</mrow>
          <mrow>4t+5\subtractright{4t}\amp\gt 4t+2\subtractright{4t}</mrow>
          <mrow>5\amp\gt 2</mrow>
        </md>
        We again find ourselves with the variable completely eliminated from <em>both</em> sides.
        What values of <m>t</m> would make the inequality <m>5\gt 2</m> true? The answer is that
        <em>all values</em> of <m>t</m> make <m>5\gt 2</m>, which we know is a strange sounding
        sentence. So our solution set is all real numbers, which we can write as
        <m>(-\infty,\infty)</m> or <m>\mathbb{R}</m>.
      </p>
    </example>

    <example xml:id="example-solving-linear-inequality-false-statment-intro">
      <p>
        Solve for <m>x</m> in the inequality <m>-5x+1\le -5x</m>.
      </p>

      <p>
        To solve for <m>x</m>, we aim to eliminate the <m>5x</m> from the right side by adding
        <m>5x</m> to each side:
        <md>
          <mrow>-5x+1\amp\le -5x</mrow>
          <mrow>-5x+1\addright{5x}\amp\le -5x\addright{5x}</mrow>
          <mrow>1\amp\le 0</mrow>
        </md>
        And yet again, the variable has gone missing. We can ask ourselves, <q>For which values of
        <m>x</m> is <m>1\le 0</m> true?</q> The answer is that this is impossible. There is no
        solution to this inequality. We can write the solution set using <m>\emptyset</m> or just
        say that there are <q>no solutions</q>.
      </p>
    </example>

    <p>
      Let's summarize these two special cases that sometimes arise when solving linear equations and
      inequalities.
    </p>

    <list xml:id="list-special-solution-sets">
      <title>Special Solution Sets for Equations and Inequalities</title>
      <dl>
        <li>
          <title>All Real Numbers</title>
          <p>
            When solving an equation or inequality boils down to an unambiguously true equation or
            inequality such as <m>2=2</m> or <m>0\lt2</m>, then all real numbers are solutions.
            We write this solution set as <m>(-\infty,\infty)</m> or <m>\mathbb{R}</m>.
          </p>
        </li>

        <li>
          <title>No Solution</title>
          <p>
            When solving an equation or inequality boils down to an outright false statement such as
            <m>0=2</m> or <m>0\gt2</m>, then no real number is a solution. We write this solution
            set as either <m>\{\ \}</m> or <m>\emptyset</m> or write in words something like
            <q>there are no solutions</q>.
          </p>
        </li>
      </dl>
    </list>
  </subsection>

  <subsection>
    <title>Further Examples</title>
    <p>
      These examples may have no solutions or all real numbers as the solution set. Each <example>
      calls back to the fundamental steps for solving linear equations and inequalities that we
      learned in <xref ref="section-solving-multistep-linear-equations"/>,
      <xref ref="section-solving-multistep-linear-inequalities"/>, and
      <xref ref="section-equations-and-inequalities-with-fractions"/>.
      </example>
    </p>
    <exercise label="special-fraction-equation">
      <webwork>
        <pg-code>
          Context("FiniteSolutionSets");
          $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
        </pg-code>
        <statement>
          <p>
            Solve for <m>a</m> in <m>\frac{2}{3}(a+1)-\frac{5}{6}=\frac{2}{3}a</m>.
          </p>
          <p>
            <var name="$solution" width="16"/>
          </p>
        </statement>
        <solution>
          <p>
            We recall the technique from <xref ref="section-equations-and-inequalities-with-fractions"/>
            where we clear denominators by multiplying each side of the equation by the least common
            denominator. Here, we will multiply each side by <m>6</m>. After that, we'll be able to
            simplify each side of the equation and continue:
            <md>
              <mrow>\frac{2}{3}(a+1)-\frac{5}{6}\amp=\frac{2}{3}a</mrow>
              <mrow>\multiplyleft{6}\left(\frac{2}{3}(a+1)-\frac{5}{6}\right)\amp=\multiplyleft{6}\frac{2}{3}a</mrow>
              <mrow>\multiplyleft{6}\frac{2}{3}(a+1)-\multiplyleft{6}\frac{5}{6}\amp=\multiplyleft{6}\frac{2}{3}a</mrow>
              <mrow>4(a+1)-5\amp=4a</mrow>
              <mrow>4a+4-5\amp=4a</mrow>
              <mrow>4a-1\amp=4a</mrow>
              <mrow>4a-1\subtractright{4a}\amp=4a\subtractright{4a}</mrow>
              <mrow>-1\amp=0</mrow>
            </md>
          </p>
          <p>
            The statement <m>-1=0</m> is unambiguously false, so the equation has no solution.
          </p>
        </solution>
      </webwork>
    </exercise>

    <exercise label="special-simplify-equation">
      <webwork>
        <pg-code>
          Context("FiniteSolutionSets");
          $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
        </pg-code>
        <statement>
          <p>
            Solve for <m>x</m> in the equation <m>3(x+2)-8=(5x+4)-2(x+1)</m>.
          </p>
          <p>
            <var name="$solution" width="16"/>
          </p>
        </statement>
        <solution>
          <p>
            A lot could be simplified on each side before continuing, by distributing and combining like
            terms.
            <md>
              <mrow>3(x+2)-8\amp=(5x+4)-2(x+1)</mrow>
              <mrow>3x+6-8\amp=5x+4-2x-2</mrow>
              <mrow>3x-2\amp=3x+2</mrow>
            </md>
            From here, we subtract <m>3x</m> from each side:
            <md>
              <mrow>3x-2\subtractright{3x}\amp=3x+2\subtractright{3x}</mrow>
              <mrow>-2\amp=2</mrow>
            </md>
            As the equation <m>-2=2</m> is outright false, there is no solution to this equation.
          </p>
        </solution>
      </webwork>
    </exercise>

    <exercise label="special-fraction-inequality">
      <webwork>
        <pg-code>
          Context("FiniteSolutionSets");
          $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
        </pg-code>
        <statement>
          <p>
            Solve for <m>z</m> in the inequality
            <m>\frac{3z}{5}+\frac{1}{2}\le\left(\frac{z}{10}+\frac{3}{4}\right)+\left(\frac{z}{2}-\frac{1}{4}\right)</m>.
          </p>
          <p>
            <var name="$solution" width="16"/>
          </p>
        </statement>
        <solution>
          <p>
            We start by multiplying each side of the inequality by the LCD, which is <m>20</m>. Then we
            can continue:
            <md>
              <mrow>\frac{3z}{5}+\frac{1}{2}\amp\le\left(\frac{z}{10}+\frac{3}{4}\right)+\left(\frac{z}{2}-\frac{1}{4}\right)</mrow>
              <mrow>\multiplyleft{20}\left(\frac{3z}{5}+\frac{1}{2}\right)\amp\le \multiplyleft{20}\left(\left(\frac{z}{10}+\frac{3}{4}\right)+\left(\frac{z}{2}-\frac{1}{4}\right)\right)</mrow>
              <mrow>\multiplyleft{20}\left(\frac{3z}{5}\right)+\multiplyleft{20}\left(\frac{1}{2}\right)\amp\le \multiplyleft{20}\left(\frac{z}{10}+\frac{3}{4}\right)+\multiplyleft{20}\left(\frac{z}{2}-\frac{1}{4}\right)</mrow>
              <mrow>20\cdot\left(\frac{3z}{5}\right)+20\cdot\left(\frac{1}{2}\right)\amp\le \multiplyleft{20}\left(\frac{z}{10}\right)+\multiplyleft{20}\left(\frac{3}{4}\right)+\multiplyleft{20}\left(\frac{z}{2}\right)-\multiplyleft{20}\left(\frac{1}{4}\right)</mrow>
              <mrow>12z+10\amp\le 2z+15+10z-5</mrow>
              <mrow>12z+10\amp\le 12z+10</mrow>
              <mrow>12z+10\subtractright{12z}\amp\le 12z+10\subtractright{12z}</mrow>
              <mrow>10\amp\le10</mrow>
            </md>
            As the equation <m>10\le10</m> is true for all values of <m>z</m>, all real numbers are
            solutions to the original inequality. So the solution set is <m>(-\infty,\infty)</m>, or
            just <m>\mathbb{R}</m>.
          </p>
        </solution>
      </webwork>
    </exercise>
  </subsection>

  <reading-questions>
    <exercise label="how-many-solutions-including-special">
      <statement>
        <p>
          With a linear equation in one variable, what are the possibilities for how many solutions
          it could have? One solution? Two solutions? Are there other possibilities?
        </p>
      </statement>
      <response/>
    </exercise>
    <exercise label="how-do-you-know-no-solution">
      <statement>
        <p>
          How will you know when a linear equation or inequality has no solution?
        </p>
      </statement>
      <response/>
    </exercise>
    <exercise label="how-do-you-know-all-are-solutions">
      <statement>
        <p>
          How will you know when all numbers are solutions to a linear equation or inequality?
        </p>
      </statement>
      <response/>
    </exercise>
  </reading-questions>

  <exercises>
    <subexercises>
      <title>Notation</title>
      <exercise label="write-no-solution">
        <webwork>
          <pg-code>
            Context("FiniteSolutionSets");
            $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
          </pg-code>
          <statement>
            <p>
              What is one valid way to communicate that there are no solutions to an equation?
              (There are several correct answers.)
            </p>
            <p>
              <var name="$solution" width="16"/>
            </p>
          </statement>
        </webwork>
      </exercise>
      <exercise label="write-all-numbers-are-solutions">
        <webwork>
          <pg-code>
            Context("FiniteSolutionSets");
            $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
          </pg-code>
          <statement>
            <p>
              What is one valid way to communicate that all real numbers are solutions to an equation?
              (There are several correct answers.)
            </p>
            <p>
              <var name="$solution" width="16"/>
            </p>
          </statement>
        </webwork>
      </exercise>
    </subexercises>

    <subexercises>
      <title>Skills Practice</title>
      <exercisegroup cols="2">
        <title>Equations with Special Solution Sets</title>
        <introduction>
          <p>
            Solve the equation for its variable.
          </p>
        </introduction>
        <exercise label="solve-mxequalsmxplusb">
          <webwork xml:id="webwork-solve-mxequalsmxplusb">
            <pg-code>
              $m = random(2,9);
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x");
              $right = Formula("$m $x + $b")->reduce;
              Context("FiniteSolutionSets");
              $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              #$solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-copy">
          <webwork copy="webwork-solve-mxequalsmxplusb"/>
        </exercise>
        <exercise label="solve-mxplusbequalsmxplusb">
          <webwork xml:id="webwork-solve-mxplusbequalsmxplusb">
            <pg-code>
              $m = random(2,9);
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x + $b")->reduce;
              $right = Formula("$m $x + $b")->reduce;
              Context("FiniteSolutionSets");
              #$solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxplusbequalsmxplusb-copy">
          <webwork copy="webwork-solve-mxplusbequalsmxplusb"/>
        </exercise>
        <exercise label="solve-special-with-like-terms">
          <webwork xml:id="webwork-solve-special-with-like-terms">
            <pg-code>
              do {
                ($m, $n, $b, $c) = random_subset(4,-9..-1,1..9);
                $p = $m + $n;
                $d = $b + $c;
              } until ($p != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              Context("FiniteSolutionSets");
              #$solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms-copy">
          <webwork copy="webwork-solve-special-with-like-terms"/>
        </exercise>
        <exercise label="solve-special-with-like-terms2">
          <webwork xml:id="webwork-solve-special-with-like-terms2">
            <pg-code>
              do {
                ($m, $n, $b, $c, $d) = random_subset(5,-9..-1,1..9);
                $p = $m + $n;
              } until ($p != 0 &amp;&amp; $b + $c != $d);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              Context("FiniteSolutionSets");
              $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              #$solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms2-copy">
          <webwork copy="webwork-solve-special-with-like-terms2"/>
        </exercise>
        <exercise label="solve-special-with-distribution">
          <webwork xml:id="webwork-solve-special-with-distribution">
            <pg-code>
              ($m, $b, $c) = random_subset(3,-9..-1,1..9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m ($x + $b)")->reduce;
              $right = Formula("$m ($x + $c)")->reduce;
              Context("FiniteSolutionSets");
              $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              #$solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-distribution-copy">
          <webwork copy="webwork-solve-special-with-distribution"/>
        </exercise>
        <exercise label="solve-special-with-simplification">
          <webwork xml:id="webwork-solve-special-with-simplification">
            <pg-code>
              do {
                ($m, $p, $q) = random_subset(3,-5..-2,2..5);
                $n = $m - $p*$q;
                ($b, $c, $e) = random_subset(3,-8..-1,1..8);
                $d = $m*$b - $c - $p*$e + list_random(-3..3,(0)x5);
              } until ($n != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m($x + $b) - ($n $x + $c)")->reduce;
              $right = Formula("$d + $p($e + $q $x)")->reduce;
              Context("FiniteSolutionSets");
              $solution = ($d == $m*$b - $c - $p*$e)
                ? OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-simplification-copy">
          <webwork copy="webwork-solve-special-with-simplification"/>
        </exercise>
        <exercise label="solve-special-with-simplification2">
          <webwork xml:id="webwork-solve-special-with-simplification2">
            <pg-code>
              do {
                ($m, $n, $q) = random_subset(3,-5..-2,2..5);
                $p = $m*$n + $q;
                ($c, $d) = random_subset(2,1..9);
                $b = -$d - $m*$c + list_random(-3..3,(0)x5);
              } until ($p != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$b + $m($c + $n $x)")->reduce;
              $right = Formula("$p $x - ($d + $q $x)")->reduce;
              Context("FiniteSolutionSets");
              $solution = ($b == -$d - $m*$c)
                ? OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-simplification2-copy">
          <webwork copy="webwork-solve-special-with-simplification2"/>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-fraction">
          <webwork xml:id="webwork-solve-mxequalsmxplusb-fraction">
            <pg-code>
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [2..9]));
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x");
              $right = Formula("$m $x + $b")->reduce;
              Context("FiniteSolutionSets");
              $solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              #$solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-fraction-copy">
          <webwork copy="webwork-solve-mxequalsmxplusb-fraction"/>
        </exercise>
        <exercise label="solve-special-with-like-terms-fraction">
          <webwork xml:id="webwork-solve-special-with-like-terms-fraction">
            <pg-code>
              @den = random_coprime([2..9],[2..9]);
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [$den[0]]));
              $n = Fraction(random_coprime([1..9], [$den[1]]));
              $p = $m + $n;
              do {
                ($b, $c) = random_subset(2,-9..-1,1..9);
                $d = $b + $c;
              } until ($d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              Context("FiniteSolutionSets");
              #$solution = String("no real solutions")->cmp(typeMatch => Formula("{1}"));
              $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms-fraction-copy">
          <webwork copy="webwork-solve-special-with-like-terms-fraction"/>
        </exercise>
        <exercise label="solve-special-with-simplification-fraction">
          <webwork xml:id="webwork-solve-special-with-simplification-fraction">
            <pg-code>
              @den = random_coprime([2..9],[2..9]);
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [$den[0]]));
              $p = Fraction(random_coprime([1..9], [$den[1]]));
              do {
                $q = list_random(3,-5..-2,2..5);
                $n = $m - $p*$q;
                ($b, $c, $e) = random_subset(3,-8..-1,1..8);
                $d = $m*$b - $c - $p*$e + list_random(-3..3,(0)x5);
              } until ($n != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m($x + $b) - ($n $x + $c)")->reduce;
              $right = Formula("$d + $p($e + $q $x)")->reduce;
              Context("FiniteSolutionSets");
              $solution = ($d == $m*$b - $c - $p*$e)
                ? OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/>=<var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-simplification-fraction-copy">
          <webwork copy="webwork-solve-special-with-simplification-fraction"/>
        </exercise>
      </exercisegroup>

      <exercisegroup cols="2">
        <title>Inequalities with Special Solution Sets</title>
        <introduction>
          <p>
            Solve the inequality for its variable.
          </p>
        </introduction>
        <exercise label="solve-mxequalsmxplusb-inequality">
          <webwork xml:id="webwork-solve-mxequalsmxplusb-inequality">
            <pg-code>
              $m = random(2,9);
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x");
              $right = Formula("$m $x + $b")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-inequality-copy">
          <webwork copy="webwork-solve-mxequalsmxplusb-inequality"/>
        </exercise>
        <exercise label="solve-mxplusbequalsmxplusb-inequality">
          <webwork xml:id="webwork-solve-mxplusbequalsmxplusb-inequality">
            <pg-code>
              $m = random(2,9);
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x + $b")->reduce;
              $right = Formula("$b + $m $x")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxplusbequalsmxplusb-inequality-copy">
          <webwork copy="webwork-solve-mxplusbequalsmxplusb-inequality"/>
        </exercise>
        <exercise label="solve-special-with-like-terms-inequality">
          <webwork xml:id="webwork-solve-special-with-like-terms-inequality">
            <pg-code>
              do {
                ($m, $n, $b, $c) = random_subset(4,-9..-1,1..9);
                $p = $m + $n;
                $d = $b + $c;
              } until ($p != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms-inequality-copy">
          <webwork copy="webwork-solve-special-with-like-terms-inequality"/>
        </exercise>
        <exercise label="solve-special-with-like-terms2-inequality">
          <webwork xml:id="webwork-solve-special-with-like-terms2-inequality">
            <pg-code>
              do {
                ($m, $n, $b, $c, $d) = random_subset(5,-9..-1,1..9);
                $p = $m + $n;
              } until ($p != 0 &amp;&amp; $b + $c != $d);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms2-inequality-copy">
          <webwork copy="webwork-solve-special-with-like-terms2-inequality"/>
        </exercise>
        <exercise label="solve-special-with-distribution-inequality">
          <webwork xml:id="webwork-solve-special-with-distribution-inequality">
            <pg-code>
              ($m, $b, $c) = random_subset(3,-9..-1,1..9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m ($x + $b)")->reduce;
              $right = Formula("$m ($x + $c)")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-distribution-inequality-copy">
          <webwork copy="webwork-solve-special-with-distribution-inequality"/>
        </exercise>
        <exercise label="solve-special-with-simplification2-inequality">
          <webwork xml:id="webwork-solve-special-with-simplification2-inequality">
            <pg-code>
              do {
                ($m, $n, $q) = random_subset(3,-5..-2,2..5);
                $p = $m*$n + $q;
                ($c, $d) = random_subset(2,1..9);
                $b = -$d - $m*$c + list_random(-3..3,(0)x5);
              } until ($p != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$b + $m($c + $n $x)")->reduce;
              $right = Formula("$p $x - ($d + $q $x)")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-simplification2-inequality-copy">
          <webwork copy="webwork-solve-special-with-simplification2-inequality"/>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-fraction-inequality">
          <webwork xml:id="webwork-solve-mxequalsmxplusb-fraction-inequality">
            <pg-code>
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [2..9]));
              $b = non_zero_random(-9,9);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              $left = Formula("$m $x");
              $right = Formula("$m $x + $b")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-mxequalsmxplusb-fraction-inequality-copy">
          <webwork copy="webwork-solve-mxequalsmxplusb-fraction-inequality"/>
        </exercise>
        <exercise label="solve-special-with-like-terms-fraction-inequality">
          <webwork xml:id="webwork-solve-special-with-like-terms-fraction-inequality">
            <pg-code>
              @den = random_coprime([2..9],[2..9]);
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [$den[0]]));
              $n = Fraction(random_coprime([1..9], [$den[1]]));
              $p = $m + $n;
              do {
                ($b, $c) = random_subset(2,-9..-1,1..9);
                $d = $b + $c;
              } until ($d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m $x + $d + $n $x")->reduce;
              $right = Formula("$b + $p $x + $c")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-like-terms-fraction-inequality-copy">
          <webwork copy="webwork-solve-special-with-like-terms-fraction-inequality"/>
        </exercise>
        <exercise label="solve-special-with-simplification-fraction-inequality">
          <webwork xml:id="webwork-solve-special-with-simplification-fraction-inequality">
            <pg-code>
              @den = random_coprime([2..9],[2..9]);
              Context("Fraction");
              $m = Fraction(random_coprime([1..9], [$den[0]]));
              $p = Fraction(random_coprime([1..9], [$den[1]]));
              do {
                $q = list_random(3,-5..-2,2..5);
                $n = $m - $p*$q;
                ($b, $c, $e) = random_subset(3,-8..-1,1..8);
                $d = $m*$b - $c - $p*$e + list_random(-3..3,(0)x5);
              } until ($n != 0 &amp;&amp; $d != 0);
              $x = list_random('a'..'n','p'..'z','A'..'N','P'..'T','V'..'Z');
              Context()->variables->are($x => 'Real');
              Context()->noreduce('(-x)+y', '(-x)-y');
              $left = Formula("$m($x + $b) - ($n $x + $c)")->reduce;
              $right = Formula("$d + $p($e + $q $x)")->reduce;
              $symbol = list_random('\lt', '\gt', '\leq', '\geq');
              sub weigh {
                my ($a, $b, $s) = @_;
                if ($s eq '\lt') {return ($a &lt; $b)}
                elsif ($s eq '\gt') {return ($a > $b)}
                elsif ($s eq '\leq') {return ($a &lt;= $b)}
                elsif ($s eq '\geq') {return ($a >= $b)}
              }
              Context("FiniteSolutionSets");
              $solution = (weigh($left->eval($x => 0), $right->eval($x => 0), $symbol))
                ? $solution = OneOf(Compute("R"), String("infinitely many solutions"))->cmp(typeMatch => Formula("{1}"))
                : String("no real solutions")->cmp(typeMatch => Formula("{1}"));
            </pg-code>
            <statement>
              <p>
                <m><var name="$left"/><var name="$symbol"/><var name="$right"/></m>
              </p>
              <p>
                <var name="$solution" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="solve-special-with-simplification-fraction-inequality-copy">
          <webwork copy="webwork-solve-special-with-simplification-fraction-inequality"/>
        </exercise>
      </exercisegroup>
    </subexercises>

    <subexercises>
      <title>Challenge</title>
      <exercise label="find-equation-with-special-solution-set">
        <!-- Kara's challenge problem -->
        <webwork>
          <pg-code>
            $a = random(2,20,2);
            $b = $a-1;
            $answer = Formula("$a*(x+4)");
            $answer2 = Formula("$b*x+($a*4+2)");
            $evaluator2 = $answer2->cmp(
              checker => sub {
                my ($correct,$student,$ansHash) = @_;
                Value::Error('This is not a linear equation.') unless ($student->D('x')->D('x') == 0 );
                Value::Error('x=2 is not a solution to this equation.') unless ($student->eval(x=>2) == $a*6);
                Value::Error('This equation has infinitely many solutions, not just 2.') unless ($student != $answer);
                return 1;
              }
            );
          </pg-code>
          <statement>
            <p>
              Fill in the right side of the equation to create a linear equation that meets the description.
              <ol marker="a.">
                <li>
                  <p>
                    Create a linear equation with solution set <m>\{2\}</m>.
                  </p>
                  <p>
                    <m> <var name = "$a"/>(x + 4) = </m> <var name="$answer2" evaluator="$evaluator2" width="20"/>
                  </p>
                </li>
                <li>
                  <p>
                    Create a linear equation with <em>infinitely many solutions</em>.
                  </p>
                  <p>
                    <m> <var name = "$a"/>(x + 4) = </m> <var name="$answer" width="20"/>
                  </p>
                </li>
              </ol>
            </p>
          </statement>
        </webwork>
      </exercise>
    </subexercises>
  </exercises>
</section>

