<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!-- *********************************************************************-->
<section permid="JPy" xml:id="section-graphs-and-vertex-form">
  <title>Graphs and Vertex Form</title>
  <introduction permid="kar">
    <p permid="Eph">
      In this section,
      we will explore quadratic functions using graphing technology and learn the vertex and factored forms of a quadratic function's formula.
      We will also see how parabola graphs can be shifted.
    </p>

    <figure permid="kwq">
      <caption>Alternative Video Lessons</caption>
      <video youtubeplaylist="PL2qOxw8-rmqZKPPNQQ6Z-WPjOK6HyvYx8" />
    </figure>
  </introduction>

  <subsection permid="pWH">
    <title>Exploring Quadratic Functions with Graphing Technology</title>
    <p permid="spk">
      Graphing technology is very important and useful for applications and for finding points quickly.
      Let's explore some quadratic functions with graphing technology.
    </p>

    <example permid="Eam">
      <statement>
        <p permid="lBJ">
          Use technology to graph and make a table of the quadratic function <m>f</m> defined by
          <m>f(x)=2x^2+4x-3</m> and find each of the key points or features.

          <ol permid="QvV" label="a." cols="2">
            <li permid="xmY">
              <p permid="mlq">
                Find the vertex.
              </p>
            </li>

            <li permid="duh">
              <p permid="Ssz">
                Find the vertical intercept
                (<ie/><nbsp/><m>y</m>-intercept).
              </p>
            </li>

            <li permid="JBq">
              <p permid="yzI">
                Find the horizontal or (<ie/><nbsp/><m>x</m>-intercept(s)).
              </p>
            </li>

            <li permid="pIz">
              <p permid="eGR">
                Find <m>f(-2)</m>.
              </p>
            </li>

            <li permid="VPI">
              <p permid="KOa">
                Solve <m>f(x)=3</m> using the graph.
              </p>
            </li>

            <li permid="BWR">
              <p permid="qVj">
                Solve <m>f(x)\le 3</m> using the graph.
              </p>
            </li>
            <!-- <li><p>Use technology to make a table of <m>y=f(x)</m> with <m>x</m>-values from <m>-4</m> to <m>1</m>, using an increment of <m>1</m>. Use the table to solve <m>f(x)=-3</m>.</p></li> -->
            <li permid="iea">
              <p permid="Xcs">
                State the domain and range of the function.
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="QhA">
        <sidebyside permid="wvN" widths="32% 15% 47%">
          <p permid="PFk">
            The specifics of how to use any one particular technology tool vary.
            Whether you use an app, a physical calculator,
            or something else, a table and graph should look like:
          </p>

          <tabular permid="rTL" halign="right">
            <row bottom="medium">
              <cell><m>x</m></cell>
              <cell><m>f(x)</m></cell>
            </row>
            <row>
              <cell><m>-2</m></cell>
              <cell><m>-3</m></cell>
            </row>
            <row>
              <cell><m>-1</m></cell>
              <cell><m>-5</m></cell>
            </row>
            <row>
              <cell><m>0</m></cell>
              <cell><m>-3</m></cell>
            </row>
            <row>
              <cell><m>1</m></cell>
              <cell><m>3</m></cell>
            </row>
            <row>
              <cell><m>2</m></cell>
              <cell><m>13</m></cell>
            </row>
          </tabular>

          <image permid="fFt">
            <description>the graph of the parabola f(x)=2x^2+4x-3, that opens upward, passing through the points listed in the table</description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}
                    \addplot+[domain=-3.449:1.449] {2*x^2+4*x-3};
                \end{axis}
            \end{tikzpicture}
            ]]>
            </latex-image>
          </image>

        </sidebyside>

        <sidebyside permid="cCW" widths="47% 47%" margins="0%">
          <p permid="vMt">
            Additional features of your technology tool can enhance the graph to help answer these questions.
            You may be able to make the graph appear like:
          </p>

          <image permid="LMC">
            <description>the previous graph with all of the key points labeled as described below</description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[clip=false]
                    \addplot [domain=-3.449:1.449, primarycurve] {2*x^2+4*x-3} node[below right] {$y=f(x)$};
                    \addplot [domain=-7:7, secondarycurve] {3} node[above left] {$y=3$};
                    \addplot [soliddot, color=thirdcolor] coordinates {(-1,-5)} node[below left] {$(-1,-5)$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(1,3)} node[above right] {$(1,3)$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(0,-3)} node[below right] {$(0,-3)$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(-3,3)} node[above left] {$(-3,3)$};
                    \addplot [soliddot, color=fifthcolor] coordinates {(-2.581,0)} node[above left] {$(-2.6,0)$};
                    \addplot [soliddot, color=fifthcolor] coordinates {(0.581,0)} node[above right] {$(0.6,0)$};
                \end{axis}
            \end{tikzpicture}
            ]]>
            </latex-image>
          </image>

        </sidebyside>

        <p permid="bTC">
          <ol permid="ADd" label="a.">
            <li permid="Olj">
              <p permid="DjB">
                The vertex is <m>(-1,-5)</m>.
              </p>
            </li>

            <li permid="uss">
              <p permid="jqK">
                The vertical intercept is <m>(0,-3)</m>.
              </p>
            </li>

            <li permid="azB">
              <p permid="PxT">
                The horizontal intercepts are approximately <m>(-2.6,0)</m> and <m>(0.6,0)</m>.
              </p>
            </li>

            <li permid="GGK">
              <p permid="vFc">
                When <m>x=-2</m>, <m>y=-3</m>, so <m>f(-2)=-3</m>.
              </p>
            </li>

            <li permid="mNT">
              <p permid="bMl">
                The solutions to <m>f(x)=3</m> are the <m>x</m>-values where <m>y=3</m>.
                We graph the horizontal line <m>y=3</m> and find the <m>x</m>-values where the graphs intersect.
                The solution set is <m>\{-3,1\}</m>.
              </p>
            </li>

            <li permid="SVc">
              <p permid="HTu">
                The solutions are all of the <m>x</m>-values where the function's graph is below
                (or touching)
                the line <m>y=3</m>.
                The interval is <m>[-3,1]</m>.
              </p>
            </li>
            <!-- <li>
                <p>To solve <m>f(x)=-3</m> using the table, we look for where the <m>y</m>-values are equal to <m>-3</m>. There are two solutions, <m>-2</m> and <m>0</m>. The solution set is <m>\{-2,0\}</m>.
                </p>
                <sidebyside>
                    <tabular halign="right">
                        <col right="major"/>
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <col />
                        <row bottom="minor">
                            <cell><m>x</m></cell>
                            <cell><m>-4</m></cell>
                            <cell><m>-3</m></cell>
                            <cell><m>-2</m></cell>
                            <cell><m>-1</m></cell>
                            <cell><m>0</m></cell>
                            <cell><m>1</m></cell>
                        </row>
                        <row>
                            <cell><m>f(x)</m></cell>
                            <cell><m>13</m></cell>
                            <cell><m>3</m></cell>
                            <cell><m>-3</m></cell>
                            <cell><m>-5</m></cell>
                            <cell><m>-3</m></cell>
                            <cell><m>3</m></cell>
                        </row>
                    </tabular>
                </sidebyside>
            </li> -->
            <li permid="zcl">
              <p permid="oaD">
                The domain is <m>(-\infty,\infty)</m> and the range is <m>[-5,\infty)</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>

    <p permid="Ywt">
      Now we will look at an application with graphing technology and put the points of interest in context.
    </p>

    <example permid="khv">
      <statement>
        <p permid="RIS">
          A <url href="https://en.wikipedia.org/wiki/Reduced-gravity_aircraft">reduced-gravity aircraft</url><fn>
          <url href="https://en.wikipedia.org/wiki/Reduced-gravity_aircraft">en.wikipedia.org/wiki/Reduced-gravity<underscore />aircraft</url>
          </fn> is a fixed-wing airplane that astronauts use for training.
          The airplane flies up and then down in a parabolic path to simulate the feeling of weightlessness.
          In one training flight,
          the pilot will fly <m>40</m> to <m>60</m> parabolic maneuvers.
        </p>

        <p permid="xQb">
          For the first parabolic maneuver,
          the altitude of the plane, in feet, at time <m>t</m>,
          in seconds since the maneuver began,
          is given by <m>H(t)=-16t^2+400t+30500</m>.

          <ol permid="wDe" label="a.">
            <li permid="fju">
              <p permid="UhM">
                Determine the starting altitude of the plane for the first maneuver.
              </p>
            </li>

            <li permid="LqD">
              <p permid="AoV">
                What is the altitude of the plane <m>10</m> seconds into the maneuver?
              </p>
            </li>

            <li permid="rxM">
              <p permid="gwe">
                Determine the maximum altitude of the plane and how long it takes to reach that altitude.
              </p>
            </li>

            <li permid="XEV">
              <p permid="MDn">
                The zero-gravity effect is experienced when the plane begins the parabolic path until it gets back down to <m>30{,}500</m> feet.
                Write an inequality to express this and solve it using the graph.
                Write the times of the zero-gravity effect as an interval and determine how long the astronauts experience weightlessness during each cycle.
              </p>
            </li>

            <li permid="DMe">
              <p permid="sKw">
                Use technology to make a table for <m>H</m> with <m>t</m>-values from <m>0</m> to <m>25</m> seconds.
                Use an increment of <m>5</m> seconds and then use the table to solve <m>H(t)=32100</m>.
              </p>
            </li>

            <li permid="jTn">
              <p permid="YRF">
                State the domain and range for this context.
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="woJ">
        <p permid="IaL">
          We can answer the questions based on the information in the graph.
        </p>

        <figure permid="Upd">
          <caption>Graph of <m>H(t)=-16t^2+400t+30500</m> with <m>y=30500</m></caption>
          <image permid="JXp" width="60%">
            <description>a graph of the parabola y=-16t^2+400t+30500 and the horizontal line y=30500; the parabola opens downward and has a maximum at (12.5, 33000)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                \begin{axis}[
                    ymin=270,
                    ymax=350,
                    xmin=-5,
                    xmax=30,
                    xtick={0,5,...,25},
                    minor xtick={-5,0,...,30},
                    ytick={280,290,300,310,320,330,340},
                    yticklabels={28000,29000,30000,31000,32000,33000,34000},
                    minor ytick=,
                    xlabel={$t$},
                    clip=false,
                    axis y discontinuity=crunch]
                        \addplot [primarycurve, domain=-5:30, dashed, thin] {(-16*x^2+400*x+30500)/100} node[pos=0.9, left] {$y=H(t)$};
                        \addplot [primarycurve, domain=0:25, -] {(-16*x^2+400*x+30500)/100};
                        \addplot [secondarycurve, domain=-5:30] {305} node[pos=0.5, above] {$y=30500$};
                        \addplot [soliddot, color=thirdcolor] coordinates {(0,305)} node[below right] {$(0,30500)$};
                        \addplot [soliddot, color=thirdcolor] coordinates {(25,305)} node[below left] {$(25,30500)$};
                        \addplot [soliddot, color=fourthcolor] coordinates {(10,329)} node[above left] {$(10,32900)$};
                        \addplot [soliddot, color=fifthcolor] coordinates {(12.5,330)} node[above right] {$(12.5,34000)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
          </image>

        </figure>

        <p permid="ohU">
          <ol permid="gKm" label="a.">
            <li permid="Qaw">
              <p permid="EYO">
                The starting altitude can be read from the vertical intercept,
                which is <m>(0,30500)</m>.
                The feeling of weightlessness begins at <m>30{,}500</m> feet.
              </p>
            </li>

            <li permid="whF">
              <p permid="lfX">
                After <m>10</m> seconds, the altitude of the plane is <m>32{,}900</m> feet.
              </p>
            </li>

            <li permid="coO">
              <p permid="Rng">
                For the maximum altitude of the plane we look at the vertex,
                which is approximately <m>(12.5, 33000)</m>.
                This tells us that after <m>12.5</m> seconds the plane will be at its maximum altitude of <m>33{,}000</m> feet.
              </p>
            </li>

            <li permid="IvX">
              <p permid="xup">
                We can write an inequality to describe when the plane is at or above
                <m>30{,}500</m> feet and solve it graphically.
                <md permid="eVz">
                  <mrow>H(t)\amp\geq30500</mrow>
                  <mrow>-16t^2+400t+30500\amp\geq30500</mrow>
                </md>.
                We graph the line <m>y=30500</m> and find the points of intersection with the parabola.
                The astronauts experience weightlessness from <m>0</m> seconds to <m>25</m> seconds into the maneuver,
                or <m>[0,25]</m> seconds.
                They experience weightlessness for 25 seconds in each cycle.
              </p>
            </li>

            <li permid="oDg">
              <p permid="dBy">
                To solve <m>H(t)=32100</m> using the table,
                we look for where the <m>H</m>-values are equal to <m>32100</m>.
              </p>

              <sidebyside permid="IKf">

                <tabular permid="zyk" halign="right">
                  <col right="major"/>
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <col />
                  <row bottom="minor">
                    <cell><m>t</m></cell>
                    <cell><m>0</m></cell>
                    <cell><m>5</m></cell>
                    <cell><m>10</m></cell>
                    <cell><m>15</m></cell>
                    <cell><m>20</m></cell>
                    <cell><m>25</m></cell>
                  </row>
                  <row>
                    <cell><m>H(t)</m></cell>
                    <cell><m>30500</m></cell>
                    <cell><m>32100</m></cell>
                    <cell><m>32900</m></cell>
                    <cell><m>32900</m></cell>
                    <cell><m>32100</m></cell>
                    <cell><m>30500</m></cell>
                  </row>
                </tabular>

              </sidebyside>

              <p permid="JIH">
                There are two solutions, <m>5</m> seconds and <m>20</m> seconds.
                The solution set is <m>\{5,20\}</m>.
              </p>
            </li>

            <li permid="UKp">
              <p permid="pPQ">
                When we use technology we see the entire function but in this context the plane is only on a parabolic path from <m>t=0</m> to <m>t=25</m> seconds.
                So the domain is <m>[0,25]</m>,
                and the range is the set of corresponding <m>y</m>-values which is <m>[30500,33000]</m> feet.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>

    <p permid="EDC">
      Let's look at the remote-controlled airplane dive from <xref ref="example-remote-control-airplane-quadratic-application">Example</xref>.
      This time we will use technology to answer the questions.
    </p>
<!-- TODO: This one could be interactive. I didn't fix graph issues -->
<!-- At some future time this should be made interactive with something more advanced, like GeoGebra. To make this graph in WeBWorK, it would be too crude. -->
    <example permid="QoE">
      <statement>
        <p permid="dXk">
          Maia has a remote-controlled airplane and she is going to do a stunt dive where the plane dives toward the ground and back up along a parabolic path.
          The altitude or height of the plane is given by the function <m>H</m> where <m>H(t)=0.7t^2-23t+200</m>,
          for <m>0 \le t \le 30</m>.
          The height is measured in feet and the time,
          <m>t</m>, is measured in seconds since the stunt began.

          <ol permid="cKn" label="a.">
            <li permid="ARy">
              <p permid="VWZ">
                Determine the starting height of the plane as the dive begins.
              </p>
            </li>

            <li permid="gYH">
              <p permid="Cei">
                Determine the height of the plane after <m>5</m> seconds.
              </p>
            </li>

            <li permid="NfQ">
              <p permid="ilr">
                Will the plane hit the ground, and if so, at what time?
              </p>
            </li>

            <li permid="tmZ">
              <p permid="OsA">
                If the plane does not hit the ground,
                what is the closest it gets to the ground, and at what time?
              </p>
            </li>

            <li permid="Zui">
              <p permid="uzJ">
                At what time(s) will the plane have an altitude of <m>50</m> feet?
              </p>
            </li>

            <li permid="FBr">
              <p permid="aGS">
                State the domain and the range of the function
                (in context).
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="cvS">
        <p permid="Awm">
          We have graphed the function and we will find the key information and put it in context.
        </p>

        <figure permid="MKE">
          <caption>Graph of <m>H(t)=0.7t^2-23t+200</m></caption>
          <image permid="qey" width="60%">
            <description>a graph of the quadratic function with all of the key features labeled that are described below</description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[ymin=-20,
                            ymax=240,
                            xmin=-5,
                            xmax=35,
                            xtick={5,10,...,30},
                            ytick={80,120,...,200},
                            minor ytick={20,40,...,220},
                            minor xtick={0,5,...,35},
                            xlabel={$t$},
                            clip=false]
                    \addplot [primarycurve, domain=-1.656:34.513, dashed, thin] {0.7*x^2-23*x+200} node[pos=0.90,left] {$y=H(t)$};
                    \addplot [primarycurve, domain=0:30, -] {0.7*x^2-23*x+200};
                    \addplot [secondarycurve, domain=-5:35] {50} node[pos=0.53, above] {$y=50$};
                    \addplot [soliddot, color=firstcolor] coordinates {(0,200)} node[right] {$(0,200)$};
                    \addplot [soliddot, color=thirdcolor] coordinates {(5,102.5)} node[right] {$(5,102.5)$};
                    \addplot [soliddot, color=fourthcolor] coordinates {(16.43,11.07)} node[left] {$(16.43,11.07)$};
                    \addplot [soliddot, color=fifthcolor] coordinates {(30,140)} node[left] {$(30,140)$};
                    \addplot [soliddot, color=secondcolor] coordinates {(8.971, 50)} node[below left] {$(8.97, 50)$};
                    \addplot [soliddot, color=secondcolor] coordinates {(23.886, 50)} node[below right] {$(23.89, 50)$};
                \end{axis}
            \end{tikzpicture}
            ]]>
            </latex-image>
          </image>

        </figure>

        <p permid="gDv">
          <ol permid="MRv" label="a.">
            <li permid="lIA">
              <p permid="GOb">
                The starting altitude can be read from the vertical intercept,
                which is <m>(0,200)</m>.
                When the stunt begins, the plane has a altitude of <m>200</m> feet.
              </p>
            </li>

            <li permid="RPJ">
              <p permid="mVk">
                When <m>x=5</m>, the <m>y</m>-value is <m>102.5</m>.
                So <m>H(5)=102.5</m>.
                This means that after <m>5</m> seconds,
                the plane is <m>102.5</m> feet above the ground.
              </p>
            </li>

            <li permid="xWS">
              <p permid="Tct">
                From the graph we can see that the parabola does not touch or cross the <m>x</m>-axis,
                which represents the ground.
                This means the plane does not hit the ground and there are no real solutions to the equation <m>H(t)=0</m>.
              </p>
            </li>

            <li permid="eeb">
              <p permid="zjC">
                The lowest point is the vertex,
                which is approximately <m>(16.43, 11.07)</m>.
                The minimum altitude of the plane is about <m>11</m> feet,
                which occurs after about <m>16.4</m> seconds.
              </p>
            </li>

            <li permid="Klk">
              <p permid="fqL">
                We graph the horizontal line <m>y=50</m> and look for the points of intersection.
                The plane will be <m>50</m> feet above the ground about <m>9</m> seconds after the plane begins the stunt,
                and again at about <m>24</m> seconds.
              </p>
            </li>

            <li permid="qst">
              <p permid="LxU">
                The domain for this function is given in the problem statement because only part of the parabola represents the path of the plane.
                The domain is <m>[0,30]</m>.
                For the range we look at the possible altitudes of the plane and see that it is <m>[11.07\ldots,200]</m>.
                The plane is doing this stunt from <m>0</m> to <m>30</m> seconds and its height ranges from about <m>11</m> to <m>200</m> feet above the ground.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
  </subsection>

  <subsection permid="WdQ">
    <title>The Vertex Form of a Parabola</title>
    <p permid="kKL">
      We have learned the standard form of a quadratic function's formula,
      which is <m>f(x)=ax^2+bx+c</m>.
      In this subsection,
      we will learn another form called the <term>vertex form</term>.
    </p>

    <p permid="QRU">
      Using graphing technology,
      consider the graphs of <m>f(x)=x^2-6x+7</m> and <m>g(x)=(x-3)^2-2</m> on the same axes.
    </p>

    <sidebyside permid="BfG" widths="47% 47%" margins="0%">
      <p permid="CzU">
        We see only one parabola because these are two different forms of the same function.
        Indeed, if we convert <m>g(x)</m> into standard form:
        <md permid="LcI">
          <mrow>g(x)\amp=(x-3)^2-2</mrow>
          <mrow>g(x)\amp=x^2-6x+9-2</mrow>
          <mrow>g(x)\amp=x^2-6x+7</mrow>
        </md>
        it is clear that <m>f</m> and <m>g</m> are the same function.
      </p>

      <figure permid="iHd">
        <caption>Graph of <m>f(x)=x^2-6x+7</m> and <m>g(x)=(x-3)^2-2</m></caption>
        <image permid="WlH" width="47%">
          <description>the graphs of the two parabolas overlap each other completely</description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[]
                    \addplot+[primarycurve, domain=0:6] {x^2-6*x+7};
                    \addplot+[secondarycurve, domain=0:6] {(x-3)^2-2+0.1};
                    \addplot [soliddot, color=fourthcolor] coordinates {(3,-2)} node[below] {$(3,-2)$};
                \end{axis}
            \end{tikzpicture}
            ]]>
            </latex-image>
        </image>

      </figure>
    </sidebyside>

    <p permid="wZd">
      The formula given for <m>g</m> is written in vertex form which is very useful because it allows us to read the vertex without doing any calculations.
      The vertex of the parabola is <m>(3,-2)</m>.
      We can see those numbers in <m>g(x)=(x-3)^2-2</m>.
      The <m>x</m>-value is the solution to <m>(x-3)=0</m>,
      and the <m>y</m>-value is the constant
      <em>added</em> at the end.
    </p>

    <p permid="dgm">
      Here are the graphs of three more functions with formulas in vertex form.
      Compare each function with the vertex of its graph.
    </p>

    <sidebyside permid="hmP" widths="32% 32% 32%" margins="0%">
      <figure permid="OOm" xml:id="figure-vertex-in-first-quadrant">
        <caption><m>r(x)=(x-2)^2+1</m></caption>
        <image permid="CsQ">
          <description>the graph of a parabola that opens upward and has a vertex at (2,1)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[]
                        \addplot+[domain=-0.449:4.449] {(x-2)^2+1};
                        \addplot [primarycurve,soliddot] coordinates {(2,1)} node[right, red] {$(2,1)$};
                        \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <figure permid="uVv" xml:id="figure-vertex-in-second-quadrant">
        <caption><m>s(x)=-\frac{1}{4}(x+1)^2+3</m></caption>
        <image permid="izZ">
          <description>the graph of a wide parabola that opens downward and has a vertex at (-1,3)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[]
                        \addplot+[domain=-7:5.325] {-1/4*(x+1)^2+3};
                        \addplot [primarycurve,soliddot] coordinates {(-1,3)} node[above left, red] {$(-1,3)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <figure permid="bcE" xml:id="figure-vertex-in-third-quadrant">
        <caption><m>t(x)=4(x+3)^2-3.5</m></caption>
        <image permid="OHi">
          <description>the graph of a narrow parabola that opens upward and has a vertex at (-3,-3.5)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[]
                        \addplot+[domain=-4.62:-1.38] {4*(x+3)^2-3.5};
                        \addplot [primarycurve,soliddot] coordinates {(-3,-3.5)} node[below, red] {$(-3,-3.5)$};
                    \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>
    </sidebyside>

    <p permid="Jnv">
      Notice that the <m>x</m>-coordinate of the vertex has the opposite sign as the value in the function formula.
      On the other hand,
      the <m>y</m>-coordinate of the vertex has the same sign as the value in the function formula.
      Let's look at an example to understand why.
      We will evaluate <m>r(2)</m>.
      <md permid="rjR">
        <mrow>r(\substitute{2})\amp=(\substitute{2}-2)^2+1</mrow>
        <mrow>\amp=1</mrow>
      </md>
      The <m>x</m>-value is the solution to <m>(x-2)=0</m>,
      which is positive <m>2</m>.
      When we substitute <m>\substitute{2}</m> for <m>x</m> we get the value <m>y=1</m>.
      Note that these coordinates create the vertex at <m>(2,1)</m>.
      Now we can define the vertex form of a quadratic function.
    </p>

    <fact xml:id="fact-vertex-form">
    <title>Vertex Form of a Quadratic Function</title>
    <idx><h>vertex form of a quadratic function</h></idx>
    <idx><h>quadratic function</h><h>vertex form</h></idx>
    <statement>
      <p permid="puE">
        A quadratic function with the vertex at the point <m>(h,k)</m> is given by <m>f(x)=a(x-h)^2+k</m>.
      </p>
    </statement>
    </fact>
        <!-- <p>Now you can try reading the vertex of each function.</p> -->
    <exercise permid="ZIq">
      <webwork>
          <setup>

            <pg-code>
                Context("Point");
                $vertex[0] = Point("(-4,10)");
                $vertex[1] = Point("(1,2)");
                $vertex[2] = Point("(10,-5)");
                $vertex[3] = Point("(-7,-13)");
            </pg-code>
          </setup>
          <statement>
            <p permid="wKI">
              Find the vertex of each quadratic function.

              <ol permid="VfO" label="a." cols="2">
                <li permid="WzC">
                  <p permid="rFd">
                    <m>r(x)=-2(x+4)^2+10</m>
                  </p>

                  <p permid="XMm">
                    <var name="$vertex[0]" width="10" />
                  </p>
                </li>

                <li permid="CGL">
                  <p permid="DTv">
                    <m>s(x)=5(x-1)^2+2</m>
                  </p>

                  <p permid="kaE">
                    <var name="$vertex[1]" width="10" />
                  </p>
                </li>

                <li permid="iNU">
                  <p permid="QhN">
                    <m>t(x)=(x-10)^2-5</m>
                  </p>

                  <p permid="woW">
                    <var name="$vertex[2]" width="10" />
                  </p>
                </li>

                <li permid="OVd">
                  <p permid="cwf">
                    <m>u(x)=3(x+7)^2-13</m>
                  </p>

                  <p permid="IDo">
                    <var name="$vertex[3]" width="10" />
                  </p>
                </li>
              </ol>
            </p>
          </statement>
          <solution permid="AYC">
            <p permid="sRN">
              <ol permid="sYE" label="a.">
                <li permid="vcm">
                  <p permid="oKx">
                    The vertex of <m>r(x)=-2(x\highlight{{}+4})^2\lighthigh{{}+10}</m> is <m>(\highlight{-4},\lighthigh{10})</m>.
                  </p>
                </li>

                <li permid="bjv">
                  <p permid="URG">
                    The vertex of <m>s(x)=5(x\highlight{{}-1})^2\lighthigh{{}+2}</m> is <m>(\highlight{1},\lighthigh{2})</m>.
                  </p>
                </li>

                <li permid="HqE">
                  <p permid="AYP">
                    The vertex of <m>t(x)=(x\highlight{{}-10})^2\lighthigh{{}-5}</m> is <m>(\highlight{10},\lighthigh{-5})</m>.
                  </p>
                </li>

                <li permid="nxN">
                  <p permid="hfY">
                    The vertex of <m>u(x)=3(x\highlight{{}+7})^2\lighthigh{{}-13}</m> is <m>(\highlight{-7},\lighthigh{-13})</m>.
                  </p>
                </li>
              </ol>
            </p>
          </solution>
      </webwork>
    </exercise>

    <p permid="VBN">
      Now let's do the reverse.
      When given the vertex and the value of <m>a</m>,
      we can write the function in vertex form.
    </p>
<!-- TODO: This one could be interactive. -->
<!-- Some difficulties with that. bizarro arithemtic would need to be used if you'd want to enforce vertex form and not count other forms as correct. Also, require entering the "f(x)=" part? If not, print that or leave it missing? -->
    <example permid="oRo">
      <statement>
        <p permid="Ket">
          Write a formula for the quadratic function <m>f</m> with the given vertex and value of <m>a</m>.

          <ol permid="IRw" label="a." cols="2">
            <li permid="TEW">
              <p permid="Nnh">
                Vertex <m>(-2,8)</m>, <m>a=1</m>
              </p>
            </li>

            <li permid="zMf">
              <p permid="tuq">
                Vertex <m>(4,-9)</m>, <m>a=-4</m>
              </p>
            </li>

            <li permid="fTo">
              <p permid="ZBz">
                Vertex <m>(-3,-1)</m>, <m>a=2</m>
              </p>
            </li>

            <li permid="Max">
              <p permid="FII">
                Vertex <m>(5,12)</m>, <m>a=-3</m>
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="IDb">
        <p permid="YYW">
          <ol permid="ZfN" label="a." cols="2">
            <li permid="shG">
              <p permid="lPR">
                The vertex form is <m>f(x)=(x+2)^2+8</m>.
              </p>
            </li>

            <li permid="YoP">
              <p permid="RXa">
                The vertex form is <m>f(x)=-4(x-4)^2-9</m>.
              </p>
            </li>

            <li permid="EvY">
              <p permid="yej">
                The vertex form is <m>f(x)=2(x+3)^2-1</m>.
              </p>
            </li>

            <li permid="kDh">
              <p permid="els">
                The vertex form is <m>f(x)=-3(x-5)^2+12</m>.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>

    <sidebyside permid="NtY" widths="47% 47%" margins="0%">
      <p permid="HjN">
        Once we read the vertex we can also state the domain and range.
        All quadratic functions have a domain of
        <m>(-\infty,\infty)</m> because we can put in any value to a quadratic function.
        The range, however,
        depends on the <m>y</m>-value of the vertex and whether the parabola opens upward or downward.
        When we have a quadratic function in vertex form we can read the range from the formula.
        Let's look at the graph of <m>f</m>,
        where <m>f(x)=2(x-3)^2-5</m>, as an example.
      </p>

      <figure permid="nqW" xml:id="figure-domain-and-range-from-vertex-form">
        <caption>The graph of <m>f(x)=2(x-3)^2-5</m></caption>
        <image permid="uOr" width="47%">
          <description>the graph of a parabola that opens upward with a vertex of (3,-5)</description>
            <!-- width and height set for PDF page fit -->
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[width=240pt,height=200pt]
                        \addplot+[domain=0.551:5.449] {2*(x-3)^2-5};
                        \addplot [soliddot, color=secondcolor] coordinates {(3,-5)} node[below] {$(3,-5)$};
                        \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>
    </sidebyside>

    <p permid="BIW">
      The domain is <m>(-\infty,\infty)</m>.
      The graph of <m>f</m> opens upward
      (which we know because <m>a=2\gt0</m>)
      so the vertex is the minimum point.
      The <m>y</m>-value of <m>-5</m> is the minimum.
      The range is <m>[-5,\infty)</m>.
    </p>

    <example permid="UYx">
      <statement>
        <p permid="qlC">
          Identify the domain and range of <m>g</m>,
          where <m>g(x)=-3(x+1)^2+6</m>.
        </p>
      </statement>
      <solution permid="oKk">
        <sidebyside permid="tBh" widths="47% 47%" margins="0%">
          <figure permid="lno" xml:id="figure-domain-and-range-from-vertex-form-2">
            <caption><m>g(x)=-3(x+1)^2+6</m></caption>
            <image permid="aVA" width="47%">
              <description>the graph of a parabola that opens downward with a vertex of (-1,6)</description>
            <!-- width and height set for PDF page fit -->
            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[width=240pt,height=200pt]
                        \addplot+[domain=-3.082:1.082] {-3*(x+1)^2+6};
                        \addplot [soliddot, color=secondcolor] coordinates {(-1,6)} node[left] {$(-1,6)$};
                        \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
            </image>

          </figure>

          <p permid="Fgf">
            The domain is <m>(-\infty,\infty)</m>.
            The graph of <m>g</m> opens downward
            (which we know because <m>a=-3\lt0</m>)
            so the vertex is the maximum point.
            The <m>y</m>-value of <m>6</m> is the maximum.
            The range is <m>(-\infty,6]</m>.
          </p>
        </sidebyside>
      </solution>
    </example>

    <exercise permid="FPz">
      <webwork>
          <setup>

            <pg-code>
                Context("Interval");
                $domain = Interval("(-inf,inf)");
                $range[0] = Interval("(-inf,-11]");
                $range[1] = Interval("[20,inf)");
                $range[2] = Interval("(-inf,0]");
                $range[3] = Interval("[-4,inf)");
            </pg-code>
          </setup>
          <statement>
            <p permid="cRR">
              Identify the domain and range of each quadratic function.

              <ol permid="BmX" label="a." cols="2">
                <li permid="QKq">
                  <p permid="KsB">
                    <m>w(x)=-3(x+10)^2-11</m>
                  </p>

                  <p permid="qzK">
                    The domain is <var name="$domain" width="8"/> and the range is <var name="$range[0]" width="8"/>.
                  </p>
                </li>

                <li permid="wRz">
                  <p permid="WGT">
                    <m>u(x)=4(x-7)^2+20</m>
                  </p>

                  <p permid="COc">
                    The domain is <var name="$domain" width="8"/> and the range is <var name="$range[1]" width="8"/>.
                  </p>
                </li>

                <li permid="cYI">
                  <p permid="iVl">
                    <m>y(x)=-(x-1)^2</m>
                  </p>

                  <p permid="Pcu">
                    The domain is <var name="$domain" width="8"/> and the range is <var name="$range[2]" width="8"/>.
                  </p>
                </li>

                <li permid="JfR">
                  <p permid="vjD">
                    <m>z(x)=3(x+9)^2-4</m>
                  </p>

                  <p permid="bqM">
                    The domain is <var name="$domain" width="8"/> and the range is <var name="$range[3]" width="8"/>.
                  </p>
                </li>
              </ol>
            </p>
          </statement>
          <solution permid="hfL">
            <p permid="Rux">
              <ol permid="FmW" label="a.">
                <li permid="pna">
                  <p permid="HxV">
                    The domain of <m>w</m> is <m>(-\infty,\infty)</m>.
                    The parabola opens downward so the range is <m>(-\infty,-11]</m>.
                  </p>
                </li>

                <li permid="Vuj">
                  <p permid="nFe">
                    The domain of <m>u</m> is <m>(-\infty,\infty)</m>.
                    The parabola opens upward so the range is <m>[20,\infty)</m>.
                  </p>
                </li>

                <li permid="BBs">
                  <p permid="TMn">
                    The domain of <m>y</m> is <m>(-\infty,\infty)</m>.
                    The parabola opens downward so the range is <m>(-\infty,0]</m>.
                  </p>
                </li>

                <li permid="hIB">
                  <p permid="zTw">
                    The domain of <m>z</m> is <m>(-\infty,\infty)</m>.
                    The parabola opens upward so the range is <m>[-4,\infty)</m>.
                  </p>
                </li>
              </ol>
            </p>
          </solution>
      </webwork>
    </exercise>
  </subsection>
<!-- TODO: Is this subsection needed? Doesn't seem to be in the CCOG -->
    <!-- <subsection>
        <title>Writing a Parabola in Vertex Form</title>

        <p>There is a second method to convert a quadratic function from standard form to vertex form. In <xref ref="section-parabola-axis-vertex-and-intercepts">Section</xref>, we learned a parabola's vertex formula:<me>\text{vertex: }\left(-\frac{b}{2a},\quad f(-\frac{b}{2a}) \right)</me>We can use this formula to find a parabola's vertex, which will give us the values of <m>h</m> and <m>k</m> in the vertex form <m>f(x)=a(x-h)^2+k</m>. Let's look at the next example.</p>

        <example>
            <statement>
                <p>Write the quadratic function's vertex-form equation based on the given graph.</p>

                <figure>
                    <caption>Graph of a parabola</caption>
                    <image width="47%">
                        <description></description>
                        <latex-image>
                        <![CDATA[\begin{tikzpicture}
                            \begin{axis}[ymin=-2,ymax=5,xmin=-1,xmax=6,
                                        ytick={-1,0,...,4},
                                        minor ytick={-1,0,...,4},
                                        xtick={1,2,...,5},
                                        minor xtick={1,2,...,5},
                                        xlabel={x},ylabel={y}]
                                \addplot [<->, red, domain=0.551:5.449] {(x-3)^2-1};
                            \end{axis}
                        \end{tikzpicture}
                        ]]>
                        </latex-image>
                    </image>
                </figure>
            </statement>
            <solution>
                <p>According to the graph, the parabola's vertex is at <m>(3,-1)</m>. The function's formula must be <m>f(x)=a(x-3)^2-1</m>.</p>

                <p>According to the graph, the parabola passes the point <m>(2,0)</m> (we could have chosen a different point, and would get the same solution). To find <m>a</m>'s value, we substitute <m>x=2,y=0</m> into <m>f(x)=a(x-3)^2-1</m>, and we have:<md>
                    <mrow>f(x)\amp=a(x-3)^2-1</mrow>
                    <mrow>0\amp=a(2-3)^2-1</mrow>
                    <mrow>0\amp=a(-1)^2-1</mrow>
                    <mrow>0\amp=a(1)-1</mrow>
                    <mrow>0\amp=a-1</mrow>
                    <mrow>1\amp=a</mrow>
                </md>The quadratic function's equation is<me>f(x)=(x-3)^2-1</me>.</p>
            </solution>
        </example>

        <example>
            <statement>
                <p>At the height of <m>90.2</m> feet above sea level, an object is launched at a vertical velocity of <m>14.4</m> feet per second. The object's height can be modeled by the function <m>h</m> where <m>h(t)=-16t^2+14.4t+90.2</m>, where <m>h(t)</m> represents the object's height in feet, and <m>t</m> represents time passed since the launch in seconds. When will the object reach its maximum height?</p>

                <p>To find the object's maximum height, we need to locate the parabola's vertex. We will change the function from standard form to vertex form. In this example, it would be difficult to use the method of completing the square. We will use the vertex formula instead.</p>

                <p>For <m>h(t)=-16t^2+14.4t+90.2</m>, identify that <m>a=-16</m>, <m>b=14.4</m> and <m>c=90.2</m>. The parabola's axis is:<md>
                    <mrow>x\amp=-\frac{b}{2a}</mrow>
                    <mrow>\amp=-\frac{14.4}{2(-16)}</mrow>
                    <mrow>\amp=0.45</mrow>
                </md>Since the vertex is located on the axis, the vertex's <m>x</m> value must be <m>0.45</m>. Next, to find the vertex's <m>y</m>-value, we substitute <m>x=0.45</m> into <m>h(t)</m>, and we have:<md>
                    <mrow>h(t)\amp=-16t^2+14.4t+90.2</mrow>
                    <mrow>h(0.45)\amp=-16(0.45)^2+14.4(0.45)+90.2</mrow>
                    <mrow>\amp=93.44</mrow>
                </md>The parabola's vertex is <m>(0.45,93.44)</m>. The leading coefficient in <m>h(t)=-16t^2+14.4t+90.2</m> is <m>-16</m>, which is <m>a</m>'s value in the vertex form <m>f(x)=a(x-h)^2+k</m>. Now we can write <m>h(t)</m> in vertex form:<me>h(t)=-16(t-0.45)^2+93.44</me></p>

                <p>In conclusion, <m>0.45</m> second since its launch, the object reached its maximum height, <m>93.44</m> feet.</p>
            </statement>
        </example>

        <p>We will use the new method to do <xref ref="example-completing-the-square-3x2-3x">Example</xref> again.</p>

        todo: if commented back in, note changed to "example-completing-the-square-3x2-4x" elsewhere.

        <example>
            <statement>
                <p>Convert the quadratic function <m>g(x)=-3x^2-3x-\frac{7}{4}</m> to vertex form.</p>
            </statement>
            <solution>
                <p>For <m>g(x)=-3x^2-3x-\frac{7}{4}</m>, identify that <m>a=-3</m>, <m>b=-3</m> and <m>c=-\frac{7}{4}</m>.</p>
                <p>First, we will find the parabola's axis by the axis formula:<md>
                    <mrow>x\amp=-\frac{b}{2a}</mrow>
                    <mrow>\amp=-\frac{-3}{2(-3)}</mrow>
                    <mrow>\amp=-\frac{1}{2}</mrow>
                </md>Since the vertex is located on the axis, the vertex's <m>x</m>-value is <m>-\frac{1}{2}</m>. Next, to find the vertex's <m>y</m>-value, we substitute <m>x=-\frac{1}{2}</m> into <m>g(x)</m>, and we have:<md>
                    <mrow>g(x)\amp=-3x^2-3x-\frac{7}{4}</mrow>
                    <mrow>g(-\frac{1}{2})\amp=-3\left(-\frac{1}{2}\right)^2-3\left(-\frac{1}{2}\right)-\frac{7}{4}</mrow>
                    <mrow>\amp=-3\left(\frac{1}{4}\right)+\frac{3}{2}-\frac{7}{4}</mrow>
                    <mrow>\amp=-\frac{3}{4}+\frac{6}{4}-\frac{7}{4}</mrow>
                    <mrow>\amp=\frac{-4}{4}</mrow>
                    <mrow>\amp=-1</mrow>
                </md>The parabola's vertex is at <m>(-\frac{1}{2},-1)</m>.</p>

                <p>A quadratic function's vertex form is <m>f(x)=a(x-h)^2+k</m>.</p>
                <p>By <m>g(x)=-3x^2-3x-\frac{7}{4}</m>, we know <m>a=-3</m>.</p>
                <p>By the vertex's coordinates, we know <m>h=\frac{1}{2}</m> and <m>k=-1</m>.</p>
                <p>The function's vertex-form equation is<me>g(x)=-3(x+\frac{1}{2})^2-1</me></p>
            </solution>
        </example>

        <example> -->
            <!-- Using vertex formula to put in vertex form -->
            <!-- <statement>
                <p>In <xref ref="example-multiplying-binomials-artist-income">Example</xref>, we learned artist Michael's annual income from paintings can be modeled by <m>f(x)=-10x^2+150x+1000</m>, where <m>x</m> is the number of times he will raise the price per painting by <dollar/><m>2.00</m>. To maximize his income, how should Michael set his price per painting?</p>
            </statement>
            <solution>
                <p>The function is quadratic. Identify that <m>a=-10, b=150</m> and <m>c=1000</m>.</p>

                <p>Since the leading coefficient is negative, its graph is an upside down parabola. The maximum income is represented by the parabola's vertex.</p>

                <p>The first step to find the vertex is to find the parabola's axis, which is calculated by the formula:<md>
                    <mrow>x\amp=-\frac{b}{2a}</mrow>
                    <mrow>\amp=-\frac{150}{2(-10)}</mrow>
                    <mrow>\amp=7.5</mrow>
                </md>Since a parabola's vertex is on its axis, the vertex is at <m>(7.5,y)</m>. To find the missing <m>y</m> value, we substitute <m>x=7.5</m> into <m>f(x)</m>, and we have:<md>
                    <mrow>f(x)\amp=-10x^2+150x+1000</mrow>
                    <mrow>f(7.5)\amp=-10(7.5)^2+150(7.5)+1000</mrow>
                    <mrow>\amp=1562.5</mrow>
                </md>The parabola's vertex is at <m>(7.5,1562.5)</m>. This implies Michael should raise the price per painting <m>7.5</m> times, or by <m>7.5\cdot2=15</m> dollars. This would make the price per painting <m>10+15=25</m> dollars,  and his annual income from paintings would become <dollar/><m>1562.50</m> by this math model.</p>

                <p>With graphing technology, we can double check the maximum value of the function <m>f</m> where <m>f(x)=-10x^2+150x+1000</m>.</p>

                <figure>
                    <caption>Graph of <m>f(x)=-10x^2+150x+1000</m></caption>
                    <image width="47%">
                        <description></description>
                        <latex-image>
                        <![CDATA[\begin{tikzpicture}
                            \begin{axis}[ymin=-200,ymax=2000,xmin=-2,xmax=17,
                                        ytick={500,1000,...,1500},
                                        minor ytick={500,1000,...,1500},
                                        xtick={2,4,...,16},
                                        minor xtick={2,4,...,16},
                                        xlabel={x},ylabel={y}]
                                \addplot [<->, red, domain=-2:17] {-10*x^2+150*x+1000} node[pos=0.70,above] {$f(x)$};
                                \addplot [soliddot, red] coordinates {(7.5,1562.5)} node[above, red] {$(7.5,1562.5)$};
                                \addplot [<->,blue,dashed] coordinates {(7.5,-200) (7.5,2000)} node[pos=0.50,right, blue] {$x=7.5$};
                            \end{axis}
                        \end{tikzpicture}
                        ]]>
                        </latex-image>
                    </image>
                </figure>
            </solution>
        </example>
    </subsection> -->
  <subsection permid="CkZ">
    <title>Horizontal and Vertical Shifts</title>
    <p permid="hQf">
      Let <m>f(x)=x^2</m> and <m>g(x)=(x-4)^2+1</m>.
      The graph of <m>y=f(x)</m> has its vertex at the point <m>(0,0)</m>.
      Now we will compare this with the graph of <m>y=g(x)</m> on the same axes.
    </p>

    <sidebyside permid="SdR" widths="47% 47%" margins="0%">
      <figure permid="zFo" xml:id="figure-parabola-shifted-right-and-up">
        <caption>The graph of <m>f</m> and <m>g</m></caption>
        <image permid="HcJ">
          <description>the graph of two identical parabolas that open upward; one has a vertex at (0,0) and the other one is shifted to the right by 4 units and up by one unit so it's vertex is at (4,1)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[
                            clip=false,
                            xmax=8,
                            xmin=-4,
                            ymax=14,
                            ymin=-2,
                            xtick={-2,0,...,6},
                            ytick={2,4,...,12},
                            minor xtick={-3,-2,...,7},
                            minor ytick={-1,0,...,13}]
                        \addplot+[domain=-3.742:3.742] {x^2} node [pos=0.3,left] {$y=f(x)$};
                        \addplot+[domain=0.394:7.606] {(x-4)^2+1} node [pos=0.7,right] {$y=g(x)$};
                        \addplot[soliddot] coordinates {(0,0)} node [right] {$(0,0)$};
                        \addplot[soliddot,color=secondcolor] coordinates {(4,1)} node[right] {$(4,1)$};
                        \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <p permid="Tyf">
        Both graphs open upward and have the same shape.
        Notice that the graph of <m>g</m> is the same as the graph of <m>f</m> but is shifted to the right by <m>4</m> units and up by <m>1</m> units because its vertex is <m>(4,1)</m>.
      </p>
    </sidebyside>

    <p permid="NXo">
      Let's look at another graph.
      Let <m>h(x)=-x^2</m> and let <m>j(x)=-(x+3)^2+4</m>.
    </p>

    <sidebyside permid="yla" widths="47% 47%" margins="0%">
      <figure permid="LTG" xml:id="figure-parabola-shifted-left-and-up">
        <caption>The graph of <m>h</m> and <m>j</m></caption>
        <image permid="njS">
          <description>the graph of two identical parabolas that open downward; one has a vertex at (0,0) and the other is shifted to the left by 3 units and up by 4 units to have a vertex at (-3,4)</description>

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[
                            clip=false,
                            xmin=-8,
                            xmax=4,
                            ymin=-10,
                            ymax=6,
                            xtick={-6,-4,...,2},
                            ytick={-8,-6,...,4},
                            minor xtick={-7,-6,...,3},
                            minor ytick={-9,-8,...,5}]
                        \addplot+[domain=-3.162:3.162] {-x^2} node[pos=0.65,right] {$y=h(x)$};
                        \addplot+[domain=-6.742:0.742] {-(x+3)^2+4} node[pos=0.45,left] {$y=j(x)$};
                        \addplot[soliddot] coordinates {(0,0)} node[above right] {$(0,0)$};
                        \addplot[soliddot, color=secondcolor] coordinates {(-3,4)} node[above] {$(-3,4)$};
                        \end{axis}
                \end{tikzpicture}
                ]]>
            </latex-image>
        </image>

      </figure>

      <p permid="fMx">
        Both parabolas open downward and have the same shape.
        The graph of <m>j</m> is the same as the graph of <m>h</m> but it has been shifted to the left by <m>3</m> units and up by <m>4</m> units making its vertex <m>(-3,4)</m>.
      </p>
    </sidebyside>

    <p permid="uex">
      To summarize this,
      when a quadratic function is written in <xref ref="fact-vertex-form" text="title">vertex form</xref>,
      the <m>h</m>-value is the horizontal shift and the <m>k</m>-value is the vertical shift.
    </p>
        <!-- <p>Now you can try identifying the horizontal and vertical shifts in each function.</p> -->
<!-- TODO: This one could be interactive. -->
<!-- Not really sure what the input forms would look like for that. -->
    <example permid="lWI">
      <statement>
        <p permid="WsL">
          Identify the horizontal and vertical shifts compared with <m>f(x)=x^2</m>.

          <ol permid="oYF" label="a." cols="2">
            <li permid="NPK">
              <p permid="gaF">
                <m>m(x)=(x+7)^2+3</m>
              </p>
            </li>

            <li permid="tWT">
              <p permid="MhO">
                <m>n(x)=(x-1)^2+6</m>
              </p>
            </li>

            <li permid="aec">
              <p permid="soX">
                <m>o(x)=(x-5)^2-1</m>
              </p>
            </li>

            <li permid="Gll">
              <p permid="Ywg">
                <m>p(x)=(x+3)^2-11</m>
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution permid="URt">
        <p permid="xBG">
          <ol permid="luf" label="a.">
            <li permid="msu">
              <p permid="EDp">
                The graph of <m>y=m(x)</m> has vertex at <m>(-7,3)</m>.
                Therefore the graph is the same as <m>y=f(x)</m> shifted to the left <m>7</m> units and up <m>3</m> units.
              </p>
            </li>

            <li permid="SzD">
              <p permid="kKy">
                The graph of <m>y=n(x)</m> has vertex at <m>(1,6)</m>.
                Therefore the graph is the same as <m>y=f(x)</m> shifted to the right <m>1</m> unit and up <m>6</m> units.
              </p>
            </li>

            <li permid="yGM">
              <p permid="QRH">
                The graph of <m>y=o(x)</m> has vertex at <m>(5,-1)</m>.
                Therefore the graph is the same as <m>y=f(x)</m> shifted to the right <m>5</m> units and down <m>1</m> unit.
              </p>
            </li>

            <li permid="eNV">
              <p permid="wYQ">
                The graph of <m>y=p(x)</m> has vertex at <m>(-3,-11)</m>.
                Therefore the graph is the same as <m>y=f(x)</m> shifted to the left <m>3</m> units and down <m>11</m> units.
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>
  </subsection>

  <subsection permid="isi">
    <title>The Factored Form of a Parabola</title>
    <p permid="alG">
      There is another form of a quadratic function's formula,
      called <term>factored form</term>,
      which we will explore next.
      Let's consider the two functions
      <m>q(x)=-x^2+3x+4</m> and <m>s(x)=-(x-4)(x+1)</m>.
      Using graphing technology,
      we will graph <m>y=q(x)</m> and <m>y=s(x)</m> on the same axes.
    </p>

    <sidebyside permid="esj" widths="47% 47%" margins="0%">
      <p permid="saP">
        These graphs coincide because the functions are actually the same.
        We can tell by multiplying out the formula for <m>g</m> to get back to the formula for <m>f</m>.
        <md permid="Xra">
          <mrow>g(x)\amp=-(x-4)(x+1)</mrow>
          <mrow>g(x)\amp=-(x^2-3x-4)</mrow>
          <mrow>g(x)\amp=-x^2+3x+4</mrow>
        </md>
        Now we can see that <m>f</m> and <m>g</m> are really the same function.
      </p>

      <figure permid="YhY">
        <caption>Graph of <m>q</m> and <m>s</m></caption>
        <image permid="Trb" width="47%">
          <description>the graph of two identical parabolas that open downward and overlap each other completely</description>

            <latex-image>
            <![CDATA[\begin{tikzpicture}
                \begin{axis}[]
                    \addplot+[domain=-2.14:5.14] {-x^2+3*x+4)};
                    \addplot+[domain=-2.14:5.14] {-1*(x-4)*(x+1)+0.1};
                    \addplot [soliddot, color=fourthcolor] coordinates {(4,0)} node[above right] {$(4,0)$};
                     \addplot [soliddot, color=fourthcolor] coordinates {(-1,0)} node[above left] {$(-1,0)$};
                \end{axis}
            \end{tikzpicture}
            ]]>
            </latex-image>
        </image>

      </figure>
    </sidebyside>

    <p permid="GsP">
      Factored form is very useful because we can read the <m>x</m>-intercepts directly from the function,
      which in this case are <m>(4,0)</m> and <m>(-1,0)</m>.
      We find these by looking for the values that make the factors equal to <m>0</m>,
      so the <m>x</m>-values have the opposite signs as are shown in the formula.
      To demonstrate this, we will find the roots by solving <m>g(x)=0</m>.
      <md permid="Dyj">
        <mrow>g(x)\amp=-(x-4)(x+1)</mrow>
        <mrow>\substitute{0}\amp=-(x-4)(x+1)</mrow>
      </md>
      <md permid="jFs">
        <mrow>x-4\amp=0\amp\text{or}\amp\amp x+1\amp=0</mrow>
        <mrow>x\amp=4\amp\text{or}\amp\amp x\amp=-1</mrow>
      </md>
      This shows us that the <m>x</m>-intercepts are <m>(4,0)</m> and <m>(-1,0)</m>.
    </p>

    <p permid="mzY">
      The <m>x</m>-values of the <m>x</m>-intercepts are also called
      <term>zeros</term> or <term>roots</term>.
          <idx><h>zeros of a function</h></idx>
          <idx><h>roots of a function</h></idx>
      The zeros or roots of the function <m>g</m> are <m>-1</m> and <m>4</m>.
    </p>

    <fact xml:id="fact-factored-form">
    <title>Factored Form of a Quadratic Function</title>
    <idx><h>factored form of a quadratic function</h></idx>
    <idx><h>quadratic function</h><h>factored form</h></idx>
    <statement>
      <p permid="SHh">
        A quadratic function with horizontal intercepts at <m>(r,0)</m> and <m>(s,0)</m> has the formula <m>f(x)=a(x-r)(x-s)</m>.
      </p>
    </statement>
    </fact>
        <!-- <p>Now you can try identifying the horizontal intercepts from each factored form.</p> -->
    <exercise permid="Kzs">
      <webwork>
          <setup>

            <pg-code>
                Context("Point");
                $xints[0] = List(Point(-2,0),Point(4,0));
                $xints[1] = List(Point(7,0),Point(5,0));
                $xints[2] = List(Point(-1,0),Point(-4,0));
                $xints[3] = List(Point(8,0),Point(-3,0));
            </pg-code>
          </setup>
          <statement>
            <p permid="IZa">
              Write the horizontal intercepts of each function.

              <ol permid="hug" label="a." cols="2">
                <li permid="KVe">
                  <p permid="dfZ">
                    <m>t(x)=-(x+2)(x-4)</m>
                  </p>

                  <p permid="Jni">
                    <var name="$xints[0]" width="12" />
                  </p>
                </li>

                <li permid="rcn">
                  <p permid="pur">
                    <m>u(x)=6(x-7)(x-5)</m>
                  </p>

                  <p permid="VBA">
                    <var name="$xints[1]" width="12" />
                  </p>
                </li>

                <li permid="Xjw">
                  <p permid="BIJ">
                    <m>v(x)=-2(x+1)(x+4)</m>
                  </p>

                  <p permid="hPS">
                    <var name="$xints[2]" width="12" />
                  </p>
                </li>

                <li permid="DqF">
                  <p permid="NXb">
                    <m>w(x)=10(x-8)(x+3)</m>
                  </p>

                  <p permid="uek">
                    <var name="$xints[3]" width="12" />
                  </p>
                </li>
              </ol>
            </p>
          </statement>
          <solution permid="NmU">
            <p permid="dIP">
              <ol permid="RBo" label="a.">
                <li permid="jxO">
                  <p permid="alt">
                    The horizontal intercepts of <m>t</m> are <m>(-2,0)</m> and <m>(4,0)</m>.
                  </p>
                </li>

                <li permid="PEX">
                  <p permid="GsC">
                    The horizontal intercepts of <m>u</m> are <m>(7,0)</m> and <m>(5,0)</m>.
                  </p>
                </li>

                <li permid="vMg">
                  <p permid="mzL">
                    The horizontal intercepts of <m>v</m> are <m>(-1,0)</m> and <m>(-4,0)</m>.
                  </p>
                </li>

                <li permid="bTp">
                  <p permid="SGU">
                    The horizontal intercepts of <m>w</m> are <m>(8,0)</m> and <m>(-3,0)</m>.
                  </p>
                </li>
              </ol>
            </p>
          </solution>
      </webwork>
    </exercise>

    <p permid="yOq">
      Let's summarize the three forms of a quadratic function's formula:
      <dl permid="RfP">
        <li permid="tPP">
          <title>Standard Form</title>
          <p permid="yOd">
            <m>f(x)=ax^2+bx+c</m>, with <m>y</m>-intercept <m>(0,c)</m>.
          </p>
        </li>

        <li permid="ZWY">
          <title>Vertex Form</title>
          <p permid="eVm">
            <m>f(x)=a(x-h)^2+k</m>, with vertex <m>(h,k)</m>.
          </p>
        </li>

        <li permid="Geh">
          <title>Factored Form</title>
          <p permid="Lcv">
            <m>f(x)=a(x-r)(x-s)</m>,
            with <m>x</m>-intercepts <m>(m,0)</m> and <m>(n,0)</m>.
          </p>
        </li>
      </dl>
          <idx><h>quadratic function</h><h>forms</h></idx>
    </p>
  </subsection>

  <exercises permid="Ozr">
    <exercisegroup permid="uGA" cols="3">
      <title>Review and Warmup</title>
      <exercise permid="DEN">
        <webwork source="BasicAlgebra/PolynomialMultiplication/PolynomialMultiplication35.pg" />
      </exercise>

      <exercise permid="jLW">
        <webwork source="BasicAlgebra/PolynomialMultiplication/PolynomialMultiplication35.pg" />
      </exercise>

      <exercise permid="PTf">
        <webwork source="BasicAlgebra/PolynomialMultiplication/PolynomialMultiplication75.pg" />
      </exercise>

      <exercise permid="wao">
        <webwork source="BasicAlgebra/PolynomialMultiplication/PolynomialMultiplication75.pg" />
      </exercise>

      <exercise permid="chx">
        <webwork source="BasicAlgebra/Factoring/factoring170.pg" />
      </exercise>

      <exercise permid="IoG">
        <webwork source="BasicAlgebra/Factoring/factoring170.pg" />
      </exercise>

      <exercise permid="ovP">
        <webwork source="BasicAlgebra/Factoring/factoring375.pg" />
      </exercise>

      <exercise permid="UCY">
        <webwork source="BasicAlgebra/Factoring/factoring375.pg" />
      </exercise>

      <exercise permid="AKh">
        <webwork source="BasicAlgebra/LinearInequalities/GraphToSetAndIntervalNotation20.pg" />
      </exercise>

      <exercise permid="gRq">
        <webwork source="BasicAlgebra/LinearInequalities/GraphToSetAndIntervalNotation40.pg" />
      </exercise>

      <exercise permid="MYz">
        <webwork source="BasicAlgebra/LinearInequalities/GraphToSetAndIntervalNotation10.pg" />
      </exercise>

      <exercise permid="tfI">
        <webwork source="BasicAlgebra/LinearInequalities/GraphToSetAndIntervalNotation30.pg" />
      </exercise>

    </exercisegroup>
    <exercisegroup permid="aNJ" cols="3">
      <title>Technology and Tables</title>
      <exercise permid="ZmR">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $b = non_zero_random(-4,4,1);
                      $c = non_zero_random(-3,3,1);
                   } until (($b)**2-4*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$c))/2  and  (-$b-sqrt(($b)**2-4*$c))/2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$c))/2  and  (-$b+sqrt(($b)**2-4*$c))/2 &lt; 4));
                $formula = Compute("x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="pgj">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="qGB">

                <tabular permid="bFu" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="tud">
              <p permid="JPY">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="pXh">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="Weq">
                <md permid="PMB">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="Clz">
                So the table may be completed as:
              </p>

              <sidebyside permid="WNK">

                <tabular permid="YaU" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Fua">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $b = non_zero_random(-4,4,1);
                      $c = non_zero_random(-3,3,1);
                   } until (($b)**2-4*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$c))/2  and  (-$b-sqrt(($b)**2-4*$c))/2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$c))/2  and  (-$b+sqrt(($b)**2-4*$c))/2 &lt; 4));
                $formula = Compute("x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="Vns">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="CUT">

                <tabular permid="HMD" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="ZBm">
              <p permid="isI">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="OzR">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="uHa">
                <md permid="vTK">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="aOj">
                So the table may be completed as:
              </p>

              <sidebyside permid="jcc">

                <tabular permid="Eid" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="lBj">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $b = non_zero_random(-4,4,1);
                      $c = non_zero_random(-3,3,1);
                   } until (($b)**2+4*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2+4*$c))/-2  and  (-$b-sqrt(($b)**2+4*$c))/-2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2+4*$c))/-2  and  (-$b+sqrt(($b)**2+4*$c))/-2 &lt; 4));
                $formula = Compute("-x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="BuB">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="Pjl">

                <tabular permid="nTM" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="FIv">
              <p permid="GVs">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="ncB">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="TjK">
                <md permid="caT">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="zqT">
                So the table may be completed as:
              </p>

              <sidebyside permid="vqu">

                <tabular permid="kpm" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="RIs">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $b = non_zero_random(-4,4,1);
                      $c = non_zero_random(-3,3,1);
                   } until (($b)**2+4*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2+4*$c))/-2  and  (-$b-sqrt(($b)**2+4*$c))/-2 &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2+4*$c))/-2  and  (-$b+sqrt(($b)**2+4*$c))/-2 &lt; 4));
                $formula = Compute("-x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="hBK">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="bxD">

                <tabular permid="UaV" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="lPE">
              <p permid="fyc">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="LFl">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="rMu">
                <md permid="Iic">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="XTD">
                So the table may be completed as:
              </p>

              <sidebyside permid="HEM">

                <tabular permid="Qwv" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="xPB">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $a = random(2,3,1)*list_random(-1,1);
                      $b = non_zero_random(-8,8,1);
                      $c = non_zero_random(-6,6,1);
                   } until (($b)**2-4*$a*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
                $formula = Compute("$a x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="NIT">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="nLV">

                <tabular permid="Aie" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="RWN">
              <p permid="EaM">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="khV">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="Qpe">
                <md permid="opl">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="wwn">
                So the table may be completed as:
              </p>

              <sidebyside permid="TTe">

                <tabular permid="wDE" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="dWK">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $a = random(2,3,1)*list_random(-1,1);
                      $b = non_zero_random(-8,8,1);
                      $c = non_zero_random(-6,6,1);
                   } until (($b)**2-4*$a*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
                $formula = Compute("$a x^2+$b x+$c")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="tQc">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="Aan">

                <tabular permid="gpn" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="ydW">
              <p permid="cDw">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="IKF">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="oRO">
                <md permid="Uwu">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="UYX">
                So the table may be completed as:
              </p>

              <sidebyside permid="ghw">

                <tabular permid="cKN" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="KdT">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $a = random(2,3,1)*list_random(-1,1);
                      $b = non_zero_random(-8,8,1);
                      $c = non_zero_random(-6,6,1);
                   } until (($b)**2-4*$a*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
                $k = random(20,50,1);
                $formula = Compute("$a x^2+$b x+($c+$k)")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="ZXl">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="MoF">

                <tabular permid="Mww" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="elf">
              <p permid="Bgg">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="hnp">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="Nuy">
                <md permid="ADD">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="tBH">
                So the table may be completed as:
              </p>

              <sidebyside permid="svO">

                <tabular permid="IRW" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="qlc">
        <webwork>
            <setup>

            <pg-code>
                Context()->noreduce('(-x)+y', '(-x)-y');
                $f = RandomVariableName(type=>'function');
                do {
                   do {
                      $a = random(2,3,1)*list_random(-1,1);
                      $b = non_zero_random(-8,8,1);
                      $c = non_zero_random(-6,6,1);
                   } until (($b)**2-4*$a*$c > 0);
                } until ((0 &lt; (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b-sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4) or (0 &lt; (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a)  and  (-$b+sqrt(($b)**2-4*$a*$c))/(2*$a) &lt; 4));
                $k = random(20,50,1);
                $formula = Compute("$a x^2+$b x+($c+$k)")->reduce;
                @defaultAnswers = ();
                for my $i (0..6) {
                  $x[$i] = $i-2;
                  $y[$i] = $formula->eval(x=>$x[$i]);
                  push(@defaultAnswers,$x[$i]);
                  push(@defaultAnswers,$y[$i]);
                }
                $multians = MultiAnswer(@defaultAnswers)->with(
                    singleResult => 1,
                    checker => sub {
                        my ( $correct, $student, $self ) = @_;
                        my @sxy = @{$student};
                        my @xs, @ys;
                       for my $k (0..6)
                         {$xs[$k] = $sxy[2*$k];
                          $ys[$k] = $sxy[2*$k+1];}
                       #
                       #  Loop though the student answers
                       ##
                       for ($i = 0; $i &lt;= 6; $i++) {
                         my $ith = Value::List->NameForNumber($i+1);
                         #
                         #  Check that the student's answers are Reals
                         #
                         Value->Error("Your $ith x-value is not a number") if ($xs[$i]->type ne "Number");
                         Value->Error("Your $ith y-value is not a number") if ($ys[$i]->type ne "Number");
                         #
                         #  Check that the point hasn't been given before
                         #
                         for ($j = 0, $used = 0; $j &lt; $i; $j++) {
                           my $jth = Value::List->NameForNumber($j+1);
                           if ($xs[$j] == $xs[$i] and $ys[$j] == $ys[$i]) {
                             Value->Error("Your $ith row in the table is the same as your $jth row") unless $ansHash->{isPreview};
                             $used = 1; last;
                           }
                         }
                         #
                         #  If not already used, check that it satisfies the equation
                         #    and increase the score if so.
                         #
                         if (!$used) {
                           Value->Error("Your $ith row is not correct") unless $ansHash->{isPreview} or $ys[$i] == $formula->eval(x=>$xs[$i]);
                             }
                           }
                         return 1;
                    }
                );

                for my $i (0..6) {
                Context()->flags->set(reduceConstants=>0,reduceConstantFunctions=>0);
                $rs[$i] = $formula->substitute(x=>$x[$i]);
                }
            </pg-code>
            </setup>
            <statement>
              <p permid="Geu">
                Let <m><var name="$f" />(x)=<var name="$formula" /></m>.
                Use technology to make a table of values <m><var name="$f" /></m>.
              </p>

              <sidebyside permid="YCX">

                <tabular permid="sDF" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                  <row>
                    <cell><var name="$multians" width="4" /></cell>
                    <cell><var name="$multians" width="4" /></cell>
                  </row>
                </tabular>

              </sidebyside>
            </statement>
            <solution permid="Kso">
              <p permid="ZIQ">
                This solution will use the <m>x</m>-values <m><var name="$x[0]" /></m>,
                <m><var name="$x[1]" /></m>,
                <m><var name="$x[2]" /></m>,
                <m><var name="$x[3]" /></m>, <m><var name="$x[4]" /></m>,
                <m><var name="$x[5]" /></m>, and <m><var name="$x[6]" /></m>.
                The choice to use these <m>x</m>-values is arbitrary.
                Since they are small numbers,
                they might make calculations easier.
                It's important to include some negative numbers.
              </p>

              <p permid="FPZ">
                One at a time,
                we substitute these <m>x</m>-values into the formula <m><var name="$f" />(x)=<var name="$formula" /></m> and compute.
              </p>

              <p permid="lXi">
                <md permid="gKM">
                  <mrow><var name="$rs[0]" />\amp=<var name="$y[0]" /></mrow>
                  <mrow><var name="$rs[1]" />\amp=<var name="$y[1]" /></mrow>
                  <mrow><var name="$rs[2]" />\amp=<var name="$y[2]" /></mrow>
                  <mrow><var name="$rs[3]" />\amp=<var name="$y[3]" /></mrow>
                  <mrow><var name="$rs[4]" />\amp=<var name="$y[4]" /></mrow>
                  <mrow><var name="$rs[5]" />\amp=<var name="$y[5]" /></mrow>
                  <mrow><var name="$rs[6]" />\amp=<var name="$y[6]" /></mrow>
                </md>
              </p>

              <p permid="Ser">
                So the table may be completed as:
              </p>

              <sidebyside permid="EKg">

                <tabular permid="oZf" halign="center">
                  <row bottom="medium">
                    <cell><m>x</m></cell>
                    <cell><m><var name="$f" />(x)</m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[0]" /></m></cell>
                    <cell><m><var name="$y[0]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[1]" /></m></cell>
                    <cell><m><var name="$y[1]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[2]" /></m></cell>
                    <cell><m><var name="$y[2]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[3]" /></m></cell>
                    <cell><m><var name="$y[3]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[4]" /></m></cell>
                    <cell><m><var name="$y[4]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[5]" /></m></cell>
                    <cell><m><var name="$y[5]" /></m></cell>
                  </row>
                  <row>
                    <cell><m><var name="$x[6]" /></m></cell>
                    <cell><m><var name="$y[6]" /></m></cell>
                  </row>
                </tabular>

              </sidebyside>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="GUS" cols="2">
      <title>Technology and Graphs</title>
      <exercise permid="Wsl">
        <statement>
          <p permid="mlD">
            Use technology to make a graph of <m>f</m> where <m>f(x)=x^2+3x-2</m>.
          </p>
        </statement>
        <solution permid="qzx">
          <sidebyside permid="kRp" widths="47%">

            <image permid="Vgo">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}
                        \addplot+[-4.854:1.854] {x^2+3*x-2};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="Czu">
        <statement>
          <p permid="SsM">
            Use technology to make a graph of <m>f</m> where <m>f(x)=x^2-2x-1</m>.
          </p>
        </statement>
        <solution permid="WGG">
          <sidebyside permid="QYy" widths="47%">

            <image permid="Bnx">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}
                        \addplot+[-2:4] {x^2-2*x-1};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="iGD">
        <statement>
          <p permid="yzV">
            Use technology to make a graph of <m>f</m> where <m>f(x)=-x^2+3x+2</m>.
          </p>
        </statement>
        <solution permid="CNP">
          <sidebyside permid="xfH" widths="47%">

            <image permid="huG">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}
                        \addplot+[-1.854:4.854] {-x^2+3*x+2};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="ONM">
        <statement>
          <p permid="eHe">
            Use technology to make a graph of <m>f</m> where <m>f(x)=-x^2+x+2</m>.
          </p>
        </statement>
        <solution permid="iUY">
          <sidebyside permid="dmQ" widths="47%">

            <image permid="NBP">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}
                        \addplot+[-2.541:3.541] {-x^2+x+2};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="uUV">
        <statement>
          <p permid="KOn">
            Use technology to make a graph of <m>f</m> where <m>f(x)=3x^2-6x-5</m>.
          </p>
        </statement>
        <solution permid="Pch">
          <sidebyside permid="JtZ" widths="47%">

            <image permid="tIY">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[ymin=-9,
                        ymax=9,
                        ytick = {-8,-7,...,8},
                        minor ytick = {-8,-7,...,8},
                    ]
                        \addplot+[-1.38:3.38] {3*x^2-6*x-5};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="bce">
        <statement>
          <p permid="qVw">
            Use technology to make a graph of <m>f</m> where <m>f(x)=-3x^2-8x+3</m>.
          </p>
        </statement>
        <solution permid="vjq">
          <sidebyside permid="pBi" widths="47%">

            <image permid="ZQh">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[ymin=-9,
                        ymax=9,
                        ytick = {-8,-7,...,8},
                        minor ytick = {-8,-7,...,8},
                    ]
                        \addplot+[-3.737:1.07] {-3*x^2-8*x+3};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="Hjn">
        <statement>
          <p permid="XcF">
            Use technology to make a graph of <m>f</m> where <m>f(x)=-3x^2+4x+49</m>.
          </p>
        </statement>
        <solution permid="bqz">
          <sidebyside permid="VIr" widths="47%">

            <image permid="FXq">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[ymin=-10,
                        ymax=60,
                        ytick = {-5,0,...,55},
                        minor ytick = {-9,-8,...,59},
                    ]
                        \addplot+[-3.818:5.151] {-3*x^2+4*x+49};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

      <exercise permid="nqw">
        <statement>
          <p permid="DjO">
            Use technology to make a graph of <m>f</m> where <m>f(x)=2x^2-2x+41</m>.
          </p>
        </statement>
        <solution permid="HxI">
          <sidebyside permid="BPA" widths="47%">

            <image permid="mez">

            <latex-image>
                <![CDATA[\begin{tikzpicture}
                    \begin{axis}[ymin=-10,
                        ymax=100,
                        ytick = {-10,0,...,90},
                        minor ytick = {-5,0,...,95},
                    ]
                        \addplot+[-4.954:5.954] {2*x^2-2*x+41};
                    \end{axis}
                \end{tikzpicture}]]>
            </latex-image>
            </image>

          </sidebyside>
        </solution>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="ncb" cols="2">
      <title>Technology and Features of Quadratic Function Graphs</title>
      <introduction permid="XxE">
        <p permid="QDz">
          Use technology to find features of a quadratic function and its graph.
        </p>
      </introduction>

      <exercise permid="TxF">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 10;
                do {
                    $a = non_zero_random(-2,2,1);
                    $b = non_zero_random(-4,4,1);
                    $c = random(-5,5,1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("$a x^2 + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4*$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="jqX">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="Pyg">
                <ol permid="NBp" label="a.">
                  <li permid="Iay">
                    <p permid="rjE">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="ohH">
                    <p permid="XqN">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="UoQ">
                    <p permid="DxW">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="AvZ">
                    <p permid="jFf">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="gDi">
                    <p permid="PMo">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="MKr">
                    <p permid="vTx">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="sRA">
                    <p permid="caG">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="YYJ">
                    <p permid="IhP">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="zEO">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 10;
                do {
                    $a = non_zero_random(-2,2,1);
                    $b = non_zero_random(-4,4,1);
                    $c = random(-5,5,1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("$a x^2 + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4*$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="vFp">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="bMy">
                <ol permid="tIy" label="a.">
                  <li permid="FfS">
                    <p permid="ooY">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="lnb">
                    <p permid="Uwh">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="Ruk">
                    <p permid="ADq">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="xBt">
                    <p permid="gKz">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="dIC">
                    <p permid="MRI">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="JPL">
                    <p permid="sYR">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="pWU">
                    <p permid="Zga">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="Wed">
                    <p permid="Fnj">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="fLX">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 20;
                do {
                    $a = non_zero_random(-3,3,0.1);
                    $b = non_zero_random(-6,6,0.1);
                    $c = random(-5,5,0.1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("$a x^2 + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4*$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="HTH">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="oaQ">
                <ol permid="ZPH" label="a.">
                  <li permid="Clm">
                    <p permid="lus">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="isv">
                    <p permid="RBB">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="OzE">
                    <p permid="xIK">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="uGN">
                    <p permid="dPT">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="aNW">
                    <p permid="JXc">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="GVf">
                    <p permid="qel">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="nco">
                    <p permid="Wlu">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="Tjx">
                    <p permid="CsD">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="LTg">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 20;
                do {
                    $a = non_zero_random(-3,3,0.1);
                    $b = non_zero_random(-6,6,0.1);
                    $c = random(-5,5,0.1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("$a x^2 + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4*$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2*$a),0), Point($h+sqrt($discriminant)/(2*$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4*$a*($c-$q)))/(2*$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="UhZ">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="Api">
                <ol permid="FWQ" label="a.">
                  <li permid="zqG">
                    <p permid="izM">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="fxP">
                    <p permid="OGV">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="LEY">
                    <p permid="uOe">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="rMh">
                    <p permid="aVn">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="XTq">
                    <p permid="Hcw">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="Eaz">
                    <p permid="njF">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="khI">
                    <p permid="TqO">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="QoR">
                    <p permid="zxX">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="sap">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 20;
                do {
                    $a = non_zero_random(2,5,1); #effective $a will be 1/$a
                    $b = non_zero_random(-6,6,0.1);
                    $c = random(-5,5,0.1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("x^2/$a + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4/$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*1/$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2/$a),0), Point($h+sqrt($discriminant)/(2/$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4/$a*($c-$q)))/(2/$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4/$a*($c-$q)))/(2/$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="gwr">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="MDA">
                <ol permid="mdZ" label="a.">
                  <li permid="wwa">
                    <p permid="fFg">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="cDj">
                    <p permid="LMp">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="IKs">
                    <p permid="rTy">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="oRB">
                    <p permid="YaH">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="UYK">
                    <p permid="EhQ">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="BfT">
                    <p permid="koZ">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="hnc">
                    <p permid="Qwi">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="Nul">
                    <p permid="wDr">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

      <exercise permid="Yhy">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $bound = 20;
                do {
                    $a = non_zero_random(2,5,1); #effective $a will be 1/$a
                    $b = non_zero_random(-6,6,0.1);
                    $c = random(-5,5,0.1);
                    Context()->noreduce('(-x)+y', '(-x)-y');
                    $formula = Formula("x^2/$a + $b x + $c")->reduce;
                    $discriminant = Real("($b)^2-4/$a*$c");

                    Context("Point");
                    Context()->flags->set(tolerance => 0.001);
                    $h = -$b/(2*1/$a);
                    $k = $formula->eval(x=>$h);
                    $vertex = Point($h,$k);
                    $yint = Point(0,$c);
                    if ($discriminant == 0) {$xint = List(Point($h,0));}
                    elsif ($discriminant > 0) {$xint = List(Point($h-sqrt($discriminant)/(2/$a),0), Point($h+sqrt($discriminant)/(2/$a),0));}
                    else {$xint = List(Compute("NONE"));}

                    Context("Numeric");
                    Context()->flags->set(tolerance => 0.001);
                    $p = non_zero_random(-8,8,1);
                    $fofp = $formula->eval(x=>$p);
                    $q = ($a > 0) ? non_zero_random(ceil($k+1),ceil($k+10),1) : non_zero_random(floor($k-10),floor($k-1),1);

                    Context("FiniteSolutionSets");
                    Context()->flags->remove("NumberCheck");
                    Context()->flags->set(tolerance => 0.001);
                    $s1 = (-$b - sqrt(($b)**2 - 4/$a*($c-$q)))/(2/$a);
                    $s2 = (-$b + sqrt(($b)**2 - 4/$a*($c-$q)))/(2/$a);
                    ($s1, $s2) = (min($s1,$s2), max($s1,$s2));
                    $solutions = Formula("{$s1,$s2}");

                    Context("Interval");
                    Context()->flags->set(tolerance => 0.001);
                    $domain = Compute("(-inf,inf)");
                    $range = ($a > 0) ? Interval("[$k,inf)") : Interval("(-inf,$k]");
                    $inequality = list_random('&lt;', '>', '\leq', '\geq');
                    if    ($a > 0 and $inequality eq '&lt;' or $a &lt; 0 and $inequality eq '>')    {$ineqsolution = Interval("($s1,$s2)");}
                    elsif ($a > 0 and $inequality eq '>'    or $a &lt; 0 and $inequality eq '&lt;') {$ineqsolution = Union("(-inf,$s1)U($s2,inf)");}
                    elsif ($a > 0 and $inequality eq '\leq' or $a &lt; 0 and $inequality eq '\geq') {$ineqsolution = Interval("[$s1,$s2]");}
                    elsif ($a > 0 and $inequality eq '\geq' or $a &lt; 0 and $inequality eq '\leq') {$ineqsolution = Union("(-inf,$s1]U[$s2,inf)");}
                } until (abs($h) &lt; $bound and abs($k) &lt; $bound and abs($c) &lt; $bound and abs($p) &lt; $bound and abs($fofp) &lt; $bound and abs($q) &lt; $bound and abs($s1) &lt; $bound and abs($s2) &lt; $bound and $fofp != 0);
            </pg-code>
            </setup>
            <statement>
              <p permid="sKJ">
                Let <m><var name="$f" />(x)=<var name="$formula"/></m>.
                Use technology to find the following.
              </p>
              <instruction>
                Any decimal answers should be correct to four significant digits.
              </instruction>
              <p permid="YRS">
                <ol permid="Sli" label="a.">
                  <li permid="tBu">
                    <p permid="cKA">
                      The vertex is <var name="$vertex" width="20" />.
                    </p>
                  </li>

                  <li permid="ZID">
                    <p permid="IRJ">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="FPM">
                    <p permid="oYS">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>

                  <li permid="lWV">
                    <p permid="Vgb">
                      The domain of <m><var name="$f" /></m> is <var name="$domain" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="See">
                    <p permid="Bnk">
                      The range of <m><var name="$f" /></m> is <var name="$range" width="20" />.
                    </p>
                    <instruction>
                      Use interval notation.
                    </instruction>
                  </li>

                  <li permid="yln">
                    <p permid="hut">
                      Calculate <m><var name="$f" />(<var name="$p" />)</m>. <var name="$fofp" width="20" />.
                    </p>
                  </li>

                  <li permid="esw">
                    <p permid="NBC">
                      Solve <m><var name="$f" />(x)=<var name="$q" /></m>. <var name="$solutions" width="20" />
                    </p>
                  </li>

                  <li permid="KzF">
                    <p permid="tIL">
                      Solve <m><var name="$f" />(x)<var name="$inequality" /><var name="$q" /></m>. <var name="$ineqsolution" width="20" />
                    </p>
                    <instruction>
                      Use interval notation. If you need to write a union of intervals, use <c>U</c> for the union symbol.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="Tjk">
      <title>Applications</title>
            <!-- TODO: These could be consolidated into fewer problems with multiple parts. -->
            <!-- Find the y-intercept -->
      <exercise permid="EoH">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;
                $funcX = Compute("$a*x**2+$b*x+$c")->reduce;

                $xVertex = -$b/(2*$a);
                $yVertex = $func->eval(t=>$xVertex);

                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);

                $heightU = NumberWithUnits("$c ft");

                $alt="This picture shows the graph of a parabola for the function h(t)=$func. The y-intercept of (0,$c) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $hx = ($xVertex+$xInt2)/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx+$marksep[0]/8, $hy, "h(t)",'red','left','middle'));

                $gr->stamps( closed_circle(0,$c,'red') );
                $gr->lb( new Label($marksep[0]/8, $c, "(0,$c)",'red','left','middle'));
            </pg-code>
        </setup>
        <statement>
          <p permid="EZb">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer using graphing technology.
          </p>

          <p permid="lgk">
            The object's height was <var name="$heightU" width="10" /> feet when it was launched.
          </p>
        </statement>
        <solution permid="nER">
          <p permid="ylA">
            Use technology to graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the function's <m>y</m>-intercept,
            <m>(0,<var name="$c"/>)</m>.
          </p>

          <sidebyside permid="hWJ" widths="47%">

            <image permid="SlI" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="esJ">
            According to the graph's <m>y</m>-intercept of <m>(0,<var name="$c"/>)</m>,
            the object was <m><var name="$c"/></m> feet high when it was launched.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Fill in a table using technology -->
      <exercise permid="kvQ">
        <webwork >
        <setup>

            <pg-code>
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;

                $h1 = $func->eval(t=>0.1);
                $h2 = $func->eval(t=>0.2);
                $h3 = $func->eval(t=>0.3);
            </pg-code>
        </setup>
        <statement>
          <p permid="Rnt">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer using graphing technology.
          </p>

          <p permid="xuC">
            Use a table to list the object's height within the first second after it was launched,
            at an increment of <m>0.1</m> second.
            Fill in the blanks.
            Round your answers to two decimal places when needed.
          </p>

          <sidebyside permid="OdS">

            <tabular permid="YKO">
              <col halign="center"/>
              <col halign="center"/>
              <row bottom="medium">
                <cell>Time in Seconds</cell>
                <cell>Height in Feet</cell>
              </row>
              <row>
                <cell><m>0.1</m></cell>
                <cell><var name="$h1" width="10" /></cell>
              </row>
              <row>
                <cell><m>0.2</m></cell>
                <cell><var name="$h2" width="10" /></cell>
              </row>
              <row>
                <cell><m>0.3</m></cell>
                <cell><var name="$h3" width="10" /></cell>
              </row>
            </tabular>

          </sidebyside>
        </statement>
        <solution permid="TMa">
          <p permid="KzS">
            Define the function <m>h(t)=<var name="$func"/></m> with technology.
            Set the table's starting value to <m>0</m>,
            and use an increment of <m>0.1</m>.
          </p>

          <sidebyside permid="ulb">

            <tabular permid="ysR">
              <col halign="center"/>
              <col halign="center"/>
              <row bottom="medium">
                <cell>Time in Seconds</cell>
                <cell>Height in Feet</cell>
              </row>
              <row>
                <cell><m>0.1</m></cell>
                <cell><var name="$h1"/></cell>
              </row>
              <row>
                <cell><m>0.2</m></cell>
                <cell><var name="$h2"/></cell>
              </row>
              <row>
                <cell><m>0.3</m></cell>
                <cell><var name="$h3"/></cell>
              </row>
              <row>
                <cell><m>\vdots</m></cell>
                <cell><m>\vdots</m></cell>
              </row>
            </tabular>

          </sidebyside>
        </solution>
        </webwork>
      </exercise>
            <!-- Finding a y-value for a given x-value -->
      <exercise permid="QCZ">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;
                $funcX = Compute("$a*x**2+$b*x+$c")->reduce;

                $xVertex = -$b/(2*$a);
                $yVertex = $func->eval(t=>$xVertex);

                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);
                $t1 = random(2,int($xInt2-1),1);
                $h1 = $func->eval(t=>$t1);

                $alt="This picture shows the graph of a parabola for the function h(t)=$func. The point ($t1,$h1) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $hx = ($xVertex+$xInt2)/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));

                $gr->stamps( closed_circle($t1,$h1,'red') );
                $gr->lb( new Label($t1+$marksep[0]/8, $h1, "($t1,$h1)",'red','left','middle'));
            </pg-code>
        </setup>
        <statement>
          <p permid="dBL">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Use technology to find the answer.
          </p>

          <p permid="JIU">
            The object was <var name="$h1" width="10" /> feet in the air <m><var name="$t1"/></m> seconds after it was launched.
          </p>
        </statement>
        <solution permid="zTj">
          <p permid="qHb">
            Use technology to graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the point <m>(<var name="$t1"/>,<var name="$h1"/>)</m> on the function's graph.
          </p>

          <sidebyside permid="ask" widths="47%">

            <image permid="eAa" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="WOk">
            According to the point <m>(<var name="$t1"/>,<var name="$h1"/>)</m>,
            the object was <m><var name="$h1"/></m> feet in the air <m><var name="$t1"/></m> seconds after it was launched.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Finding the vertex -->
      <exercise permid="wKi">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;
                $funcX = Compute("$a*x**2+$b*x+$c")->reduce;

                $xVertex = -$b/(2*$a);
                $yVertex = $func->eval(t=>$xVertex);
                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);

                $alt="This picture shows the graph of a parabola for the function h(t)=$func. The vertex ($xVertex,$yVertex) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $hx = ($xVertex+$xInt2)/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));

                $gr->stamps( closed_circle($xVertex,$yVertex,'red') );
                $gr->lb( new Label($xVertex, $yVertex+$marksep[1]/4, "($xVertex,$yVertex)",'red','center','bottom'));
            </pg-code>
        </setup>
        <statement>
          <p permid="pQd">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer using technology.
          </p>

          <p permid="VXm">
            <var name="$xVertex" width="10" /> seconds after its launch,
            the object reached its maximum height of <var name="$yVertex" width="10" /> feet.
          </p>
        </statement>
        <solution permid="gas">
          <p permid="CVt">
            Use technology to graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the parabola's vertex,
            <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>.
          </p>

          <sidebyside permid="Gzt" widths="47%">

            <image permid="KHj" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="jcC">
            According to the vertex point <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>,
            <m><var name="$xVertex"/></m> seconds after it was launched,
            the object reached its maximum height of <m><var name="$yVertex"/></m> feet.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Finding x-intercept -->
            <!-- TODO: The solution shows 13 decimal places! -->
      <exercise permid="cRr">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;
                $funcX = Compute("$a*x**2+$b*x+$c")->reduce;

                $xVertex = -$b/(2*$a);
                $yVertex = $func->eval(t=>$xVertex);
                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);

                $alt="This picture shows the graph of a parabola for the function h(t)=$func. The x-intercept ($xInt2,0) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $hx = ($xVertex+$xInt2)/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));

                $gr->stamps( closed_circle($xInt2,0,'red') );
                $gr->lb( new Label($xInt2, $marksep[1]/4, "($xInt2,0)",'red','right','bottom'));
            </pg-code>
        </setup>
        <statement>
          <p permid="Cev">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer using technology.
          </p>

          <p permid="ilE">
            <var name="$xInt2" width="10" /> seconds after its launch,
            the object fell to the ground at sea level.
          </p>
        </statement>
        <solution permid="MhB">
          <p permid="PjL">
            Use technology to graph the function <m>h(t)=<var name="$func"/></m>,
            and then find the parabola's positive <m>x</m>-intercept,
            <m>(<var name="$xInt2"/>,0)</m>.
          </p>

          <sidebyside permid="mGC" widths="47%">

            <image permid="qOs" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="vqU">
            According to the <m>x</m>-intercept,
            <m>(<var name="$xInt2"/>,0)</m>,
            <m><var name="$xInt2"/></m> seconds after it was launched,
            the object fell to the ground at sea level.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Solving Graphically -->
      <exercise permid="IYA">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $initSpeed = random(50,200,10);
                $a = -16;
                $b = $initSpeed;
                $c = random(100,300,50);

                $func = Compute("$a*t**2+$b*t+$c")->reduce;
                $funcX = Compute("$a*x**2+$b*x+$c")->reduce;

                $xVertex = -$b/(2*$a);
                $yVertex = $func->eval(t=>$xVertex);
                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);

                $h1 = random($c+10,int($yVertex-10),1);
                $dis1 = ($b)**2-4*$a*($c-$h1);
                $t1 = (-$b+sqrt($dis1))/(2*$a);
                $t2 = (-$b-sqrt($dis1))/(2*$a);

                $t1Show = int($t1*100+0.5)/100;
                $t2Show = int($t2*100+0.5)/100;

                $alt="This picture shows the graph of a parabola for the function h(t)=$func. A horizontal line y=$h1 is graphed, and the intersections of h(t) and y=$h1, ($t1,$h1) and ($t2,$h1), are labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, h,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");
                add_functions($gr, "$h1 for x in &lt; $min[0],$max[0]> using color:blue and weight:1");
                $gr->lb( new Label($max[0]-$marksep[0]/8, $h1-$marksep[1]/8, "y=$h1",'blue','right','top'));

                $hx = (($xVertex+$xInt2)/2+$xInt2)/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "h(t)",'red','right','middle'));

                $gr->stamps( closed_circle($t1,$h1,'red') );
                $gr->lb( new Label($t1, $h1+$marksep[1]/4, "($t1Show,$h1)",'red','center','bottom'));
                $gr->stamps( closed_circle($t2,$h1,'red') );
                $gr->lb( new Label($t2, $h1+$marksep[1]/4, "($t2Show,$h1)",'red','center','bottom'));

                $ans = List($t1,$t2);
            </pg-code>
        </setup>
        <statement>
          <p permid="OsN">
            An object was launched from the top of a hill with an upward vertical velocity of <m><var name="$b"/></m> feet per second.
            The height of the object can be modeled by the function <m>h(t)=<var name="$func"/></m>,
            where <m>t</m> represents the number of seconds after the launch.
            Assume the object landed on the ground at sea level.
            Find the answer using technology.
            Round your answers to two decimal places.
            If there is more than one answer,
            use a comma to separate them.
          </p>

          <p permid="uzW">
            The object was <m><var name="$h1"/></m> feet high at the following number of seconds after it was launched:
            <var name="$ans" width="10" />.
          </p>
        </statement>
        <solution permid="soK">
          <p permid="byd">
            Use technology to graph the functions <m>h(t)=<var name="$func"/></m> and <m>y=<var name="$h1"/></m>,
            and find their intersection points,
            <m>(<var name="$t1Show"/>,<var name="$h1"/>)</m> and <m>(<var name="$t2Show"/>,<var name="$h1"/>)</m>.
          </p>

          <sidebyside permid="SNL" widths="47%">

            <image permid="WVB" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="HFm">
            According to the two points of intersection,
            the object was <m><var name="$h1"/></m> feet high at the following times after it was launched:
            <m><var name="$t1Show"/></m> seconds and <m><var name="$t2Show"/></m> seconds.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Solving Graphically -->
      <exercise permid="pfJ">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $acceleration = random(1,5,0.1);
                $v0 = random(3,10,1);
                $a = $acceleration/2;
                $b = $v0;

                $func = Compute("$a*t**2+$b*t")->reduce;
                $funcX = Compute("$a*x**2+$b*x")->reduce;

                $t1 = random(5,20,1);
                $d1 = $func->eval(t=>$t1);

                $alt="This picture shows the graph of a parabola for the function d(t)=$func. A horizontal line y=$d1 is graphed, and the intersections of d(t) and y=$d1, ($t1,$d1), are labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$t1],[$d1,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, d,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; 0,$max[0]> using color:red and weight:1");
                add_functions($gr, "$d1 for x in &lt; $min[0],$max[0]> using color:blue and weight:1");
                $gr->lb( new Label($t1/2, $d1-$marksep[1]/8, "y=$d1",'blue','center','top'));

                $hx = $t1/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "d(t)",'red','right','middle'));

                $gr->stamps( closed_circle($t1,$d1,'red') );
                $gr->lb( new Label($t1-$marksep[0]/8, $d1+$marksep[1]/8, "($t1,$d1)",'red','right','bottom'));
            </pg-code>
        </setup>
        <statement>
          <p permid="aHf">
            In a race, a car drove through the starting line at the speed of <m><var name="$v0"/></m> meters per second.
            It was accelerating at <m><var name="$acceleration"/></m> meters per second squared.
            Its distance from the starting position can be modeled by the function <m>d(t)=<var name="$func"/></m>.
            Find the answer using technology.
          </p>

          <p permid="GOo">
            After <var name="$t1" width="10" /> seconds,
            the car was <m><var name="$d1"/></m> meters away from the starting position.
          </p>
        </statement>
        <solution permid="YvT">
          <p permid="nMv">
            Use technology to graph the functions <m>d(t)=<var name="$func"/></m> and <m>y=<var name="$d1"/></m>,
            and find the point of intersection where <m>x</m> is positive,
            <m>(<var name="$t1"/>,<var name="$d1"/>)</m>.
          </p>

          <sidebyside permid="yUU" widths="47%">

            <image permid="DcK" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="TTE">
            According to the point of intersection,
            the car was <m><var name="$d1"/></m> meters away from the starting position after <m><var name="$t1"/></m> seconds.
          </p>
        </solution>
        </webwork>
      </exercise>

      <exercise permid="VmS">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(t => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $acceleration = random(1,5,0.1);
                $v0 = random(3,10,1);
                $a = $acceleration/2;
                $b = $v0;

                $func = Compute("$a*t**2+$b*t")->reduce;
                $funcX = Compute("$a*x**2+$b*x")->reduce;

                $t1 = random(5,20,1);
                $d1 = $func->eval(t=>$t1);

                $alt="This picture shows the graph of a parabola for the function d(t)=$func. A horizontal line y=$d1 is graphed, and the intersections of d(t) and y=$d1, ($t1,$d1), are labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$t1],[$d1,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, t,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, d,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; 0,$max[0]> using color:red and weight:1");
                add_functions($gr, "$d1 for x in &lt; $min[0],$max[0]> using color:blue and weight:1");
                $gr->lb( new Label($t1/2, $d1-$marksep[1]/8, "y=$d1",'blue','center','top'));

                $hx = $t1/2;
                $hy = $func->eval(t=>$hx);
                $gr->lb( new Label($hx-$marksep[0]/8, $hy, "d(t)",'red','right','middle'));

                $gr->stamps( closed_circle($t1,$d1,'red') );
                $gr->lb( new Label($t1-$marksep[0]/8, $d1+$marksep[1]/8, "($t1,$d1)",'red','right','bottom'));
            </pg-code>
        </setup>
        <statement>
          <p permid="mVx">
            In a race, a car drove through the starting line at the speed of <m><var name="$v0"/></m> meters per second.
            It was accelerating at <m><var name="$acceleration"/></m> meters per second squared.
            Its distance from the starting position can be modeled by the function <m>d(t)=<var name="$func"/></m>.
            Find the answer using technology.
          </p>

          <p permid="TcG">
            After <var name="$t1" width="10" /> seconds,
            the car was <m><var name="$d1"/></m> meters away from the starting position.
          </p>
        </statement>
        <solution permid="EDc">
          <p permid="AaN">
            Use technology to graph the functions <m>d(t)=<var name="$func"/></m> and <m>y=<var name="$d1"/></m>,
            and find the point of intersection where <m>x</m> is positive,
            <m>(<var name="$t1"/>,<var name="$d1"/>)</m>.
          </p>

          <sidebyside permid="fcd" widths="47%">

            <image permid="jjT" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="ghW">
            According to the point of intersection,
            the car was <m><var name="$d1"/></m> meters away from the starting position after <m><var name="$t1"/></m> seconds.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Find the Vertex -->
      <exercise permid="Bub">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(l => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $length = random(50,200,10);
                $width = $length;
                $area = $length**2;
                $perimeter = 4*$length;
                $pHalf = $perimeter/2;

                $func = Compute("-l**2+$pHalf*l")->reduce;
                $funcX = Compute("-x**2+$pHalf*x")->reduce;

                $xVertex = $length;
                $yVertex = $func->eval(l=>$xVertex);
                $a = -1;
                $b = $pHalf;
                $c = 0;
                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);

                $alt="This picture shows the graph of a parabola for the function A(l)=$func. The vertex ($xVertex,$yVertex) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, l,'black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, A,'black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $lx = ($xVertex+$xInt2)/2;
                $ly = $func->eval(l=>$lx);
                $gr->lb( new Label($lx-$marksep[0]/8, $ly, "A(l)",'red','right','middle'));

                $gr->stamps( closed_circle($xVertex,$yVertex,'red') );
                $gr->lb( new Label($xVertex, $yVertex+$marksep[1]/4, "($xVertex,$yVertex)",'red','center','bottom'));

                $ans = List($length,$width);
            </pg-code>
        </setup>
        <statement>
          <p permid="zjP">
            A farmer purchased <m><var name="$perimeter"/></m> meters of fencing,
            and will build a rectangular pen with it.
            To enclose the largest possible area,
            what should the pen's length and width be?
            Model the pen's area with a function,
            and then find its maximum value.
          </p>

          <p permid="fqY">
            Use a comma to separate your answers.
          </p>

          <p permid="Lyh">
            To enclose the largest possible area,
            the pen's length and width should be <var name="$ans" width="10" /> meters.
          </p>
        </statement>
        <solution permid="kKl">
          <p permid="Mpf">
            Assume the rectangle's length is <m>l</m> meters,
            and its width is <m>w</m> meters.
            Since the rectangle's perimeter is given as <m><var name="$perimeter"/></m> meters, we have:
            <md permid="MRV">
              <mrow>P\amp=2(l+w)</mrow>
              <mrow><var name="$perimeter"/>\amp=2(l+w)</mrow>
              <mrow><var name="$pHalf"/>\amp=l+w</mrow>
              <mrow><var name="$pHalf"/>-l\amp=w</mrow>
            </md>
          </p>

          <p permid="swo">
            The area of the pen is <m>A=lw</m>.
            Substitute <m>w=<var name="$pHalf"/>-l</m> into <m>A=lw</m>, we have:
            <md permid="sZe">
              <mrow>A\amp=lw</mrow>
              <mrow>A\amp=l(<var name="$pHalf"/>-l)</mrow>
              <mrow>A\amp=-l^2+<var name="$pHalf"/>l</mrow>
            </md>
          </p>

          <p permid="YDx">
            Next, we use technology to graph the function <m>A=-l^2+<var name="$pHalf"/>l</m>,
            and then find the parabola's vertex <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>.
          </p>

          <sidebyside permid="Ljm" widths="47%">

            <image permid="Prc" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="EKG">
            According to the vertex point <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>,
            when the pen's length is <m><var name="$xVertex"/></m> meters,
            the pen has the maximum area <m><var name="$yVertex"/></m> square meters.
          </p>

          <p permid="kRP">
            When <m>l=<var name="$xVertex"/></m>, we have:
            <md permid="Zgn">
              <mrow>w\amp=<var name="$pHalf"/>-l</mrow>
              <mrow>\amp=<var name="$pHalf"/>-<var name="$xVertex"/></mrow>
              <mrow>\amp=<var name="$width"/></mrow>
            </md>
          </p>

          <p permid="QYY">
            To maximize the pen's area,
            the length and width should both be <m><var name="$width"/></m> meters.
            This implies the pen is a square.
          </p>
        </solution>
        </webwork>
      </exercise>
            <!-- Find the vertex -->
      <exercise permid="hBk">
        <webwork >
        <pg-macros><macro-file>PCCgraphMacros.pl</macro-file></pg-macros>
        <setup>

            <pg-code>
                $refreshCachedImages=1;
                Context("Numeric");
                Context()->variables->add(w => 'Real');
                Context()->flags->set(showExtraParens=>0, reduceConstants=>0);
                Context()->noreduce("(-x)-y","(-x)+y");
                Context()->flags->set(
                  tolerance => 0.005,
                  tolType => "absolute",
                );

                $width = random(50,100,10);
                $length = random($width+10,200,10);
                $area = $length*$width;
                $perimeter = $length+2*$width;

                $func = Compute("-2*w**2+$perimeter*w")->reduce;
                $funcX = Compute("-2*x**2+$perimeter*x")->reduce;

                $xVertex = $perimeter/4;
                $yVertex = $func->eval(w=>$xVertex);
                $a = -2;
                $b = $perimeter;
                $c = 0;
                $dis = ($b)**2-4*$a*$c;
                $xInt2 = (-$b-sqrt($dis))/(2*$a);
                $optimalLength = $yVertex/$xVertex;

                $alt="This picture shows the graph of a parabola for the function A(w)=$func. The vertex ($xVertex,$yVertex) is labeled.";

                ($min[0], $max[0], $min[1], $max[1], $ticknum[0], $ticknum[1], $xticks_ref, $yticks_ref, $marksep[0], $marksep[1]) = NiceGraphParameters([0,$xInt2],[$yVertex,0]);

                @xticks = @$xticks_ref;
                @yticks = @$yticks_ref;

                $gr = init_graph($min[0],$min[1],$max[0],$max[1],
                   axes=>[0,0],
                   grid=>[$ticknum[0],$ticknum[1]],
                   size=>[xPixels(),yPixels()]
                );
                $gr->lb('reset');
                for my $j (@xticks) {
                    if (abs($j) &lt; 10**(-10)) {next;}
                    $gr->lb( new Label($j, -$marksep[1]/8, $j,'black','center','top'));
                }
                $gr->lb( new Label($max[0]-$marksep[0]/2, $marksep[1]/8, 'w','black','center','bottom'));
                $gr->lb( new Label($marksep[0]/8, $max[1]-$marksep[1]/2, 'A','black','left','middle'));

                for my $j (@yticks) {
                    if (Real($j) == 0) {next;}
                    $gr->lb( new Label(-$marksep[0]/8, $j, $j,'black','right','middle'));
                }

                add_functions($gr, "$funcX for x in &lt; $min[0],$max[0]> using color:red and weight:1");

                $lx = ($xVertex+$xInt2)/2;
                $ly = $func->eval(w=>$lx);
                $gr->lb( new Label($lx-$marksep[0]/8, $ly, "A(w)",'red','right','middle'));

                $gr->stamps( closed_circle($xVertex,$yVertex,'red') );
                $gr->lb( new Label($xVertex, $yVertex+$marksep[1]/4, "($xVertex,$yVertex)",'red','center','bottom'));

                $ans = List($length,$width);
            </pg-code>
        </setup>
        <statement>
          <p permid="rFq">
            A farmer purchased <m><var name="$perimeter"/></m> meters of fencing,
            and will build a rectangular pen along a river.
            This implies the pen has only <m>3</m> fenced sides.
            To enclose the largest possible area,
            what should the pen's length and width be?
            Model the pen's area with a function,
            and then find its maximum value.
          </p>
          <instruction>
            Use a comma to separate your answers.
          </instruction>
          <p permid="XMz">
            To enclose the largest possible area,
            the pen's length and width should be <var name="$ans" width="10" /> meters.
          </p>
        </statement>
        <solution permid="QRu">
          <p permid="xgh">
            Assume the rectangle's length is <m>l</m> meters,
            and its width is <m>w</m> meters.
            Since the pen has only three fenced sides, we have:
            <md permid="Fnw">
              <mrow><var name="$perimeter"/>\amp=l+2w</mrow>
              <mrow><var name="$perimeter"/>-2w\amp=l</mrow>
            </md>
          </p>

          <p permid="dnq">
            The area of the pen is <m>A=lw</m>.
            Substitute <m>l=<var name="$perimeter"/>-2w</m> into <m>A=lw</m>, we have:
            <md permid="luF">
              <mrow>A\amp=lw</mrow>
              <mrow>A\amp=(<var name="$perimeter"/>-2w)w</mrow>
              <mrow>A\amp=<var name="$perimeter"/>w-2w^2</mrow>
              <mrow>A\amp=-2w^2+<var name="$perimeter"/>w</mrow>
            </md>
          </p>

          <p permid="Juz">
            Next, we use technology to graph the function <m>A=-2w^2+<var name="$perimeter"/>w</m>,
            and then find the parabola's vertex <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>.
          </p>

          <sidebyside permid="rqv" widths="47%">

            <image permid="vyl" pg-name="$gr">
              <description><var name="$alt" /></description>
            </image>

          </sidebyside>

          <p permid="pBI">
            According to the vertex point <m>(<var name="$xVertex"/>,<var name="$yVertex"/>)</m>,
            when the pen's width is <m><var name="$xVertex"/></m> meters,
            the pen has the maximum area of <m><var name="$yVertex"/></m> square meters.
          </p>

          <p permid="VIR">
            When <m>w=<var name="$xVertex"/></m>, we have:
            <md permid="RBO">
              <mrow>l\amp=<var name="$perimeter"/>-2w</mrow>
              <mrow>\amp=<var name="$perimeter"/>-2(<var name="$xVertex"/>)</mrow>
              <mrow>\amp=<var name="$optimalLength"/></mrow>
            </md>
          </p>

          <p permid="BQa">
            To maximize the pen's area,
            the length and width should be <m><var name="$optimalLength"/></m> meters and <m><var name="$xVertex"/></m> meters.
          </p>
        </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="zqt" cols="3">
      <title>Quadratic Functions in Vertex Form</title>
      <exercise permid="NIt">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>

      <exercise permid="tPC">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>

      <exercise permid="ZWL">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>

      <exercise permid="GdU">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex3.pg" />
      </exercise>

      <exercise permid="mld">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex4.pg" />
      </exercise>

      <exercise permid="Ssm">
        <webwork source="Math95/ParabolaVertex/ParabolaVertex4.pg" />
      </exercise>

      <exercise permid="yzv">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="eGE">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="KNN">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="qUW">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="Xcf">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="Djo">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm15.pg" />
      </exercise>

      <exercise permid="jqx">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>

      <exercise permid="PxG">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>

      <exercise permid="vEP">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>

      <exercise permid="bLY">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticVertexForm10.pg" />
      </exercise>

    </exercisegroup>
    <exercisegroup permid="fxC" cols="2">

      <exercise permid="HTh">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = 1;
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = Compute("[$k,inf)");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="DTI">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="ysr" label="a.">
                  <li permid="qGO">
                    <p permid="ZPU">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="FXd">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="WNX">
                    <p permid="mem">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="Slv">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="wYD">
              <p permid="hXj">
                <ol permid="xIx" label="a.">
                  <li permid="CVg">
                    <p permid="ysE">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="jcp">
                    <p permid="ezN">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="oaq">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = 1;
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = Compute("[$k,inf)");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="kaR">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="ezA" label="a.">
                  <li permid="Pjy">
                    <p permid="KGW">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="qOf">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="vqH">
                    <p permid="WVo">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="Dcx">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="dfM">
              <p permid="Oes">
                <ol permid="dPG" label="a.">
                  <li permid="bxQ">
                    <p permid="jjG">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="HEZ">
                    <p permid="PqP">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Uhz">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = random(0.1,9.9,0.2);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = Compute("[$k,inf)");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="Qia">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="KGJ" label="a.">
                  <li permid="nMi">
                    <p permid="vxY">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="bFh">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="TTr">
                    <p permid="HMq">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="nTz">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="JmV">
              <p permid="ulB">
                <ol permid="JWP" label="a.">
                  <li permid="AaA">
                    <p permid="UaI">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="ghJ">
                    <p permid="AhR">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="AoI">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = random(0.1,9.9,0.2);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = Compute("[$k,inf)");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="wpj">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="qNS" label="a.">
                  <li permid="MoS">
                    <p permid="gpa">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="Mwj">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="swb">
                    <p permid="sDs">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="YKB">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="pue">
              <p permid="asK">
                <ol permid="qdY" label="a.">
                  <li permid="YDk">
                    <p permid="ERK">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="EKt">
                    <p permid="kYT">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="gvR">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(-9,9,1);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="cws">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="WVb" label="a.">
                  <li permid="kRC">
                    <p permid="Rgc">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="xnl">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="QYL">
                    <p permid="duu">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="JBD">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="VBn">
              <p permid="GzT">
                <ol permid="Wlh" label="a.">
                  <li permid="xfU">
                    <p permid="pIM">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="dnd">
                    <p permid="VPV">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="MDa">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(-9,9,1);
                $h = non_zero_random(-9,9,1);
                $k = non_zero_random(-9,9,1);
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k")->reduce;
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="IDB">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="Dck" label="a.">
                  <li permid="Jum">
                    <p permid="BXe">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="ien">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="pBv">
                    <p permid="Olw">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="usF">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="BIw">
              <p permid="mHc">
                <ol permid="Csq" label="a.">
                  <li permid="VIE">
                    <p permid="azO">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="BPN">
                    <p permid="GGX">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="sKj">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(2,9,1)*random(-1,1,2);
                Context("Fraction");
                $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k");
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="oKK">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="jjt" label="a.">
                  <li permid="hWW">
                    <p permid="mOg">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="SVp">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="Oef">
                    <p permid="zcy">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="fjH">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="hPF">
              <p permid="SOl">
                <ol permid="izz" label="a.">
                  <li permid="ulo">
                    <p permid="LqQ">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="asx">
                    <p permid="rxZ">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="YRs">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(2,9,1)*random(-1,1,2);
                Context("Fraction");
                $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k");
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="URT">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="PqC" label="a.">
                  <li permid="GzG">
                    <p permid="XFi">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="DMr">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="mGP">
                    <p permid="jTA">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="QaJ">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="NWO">
              <p permid="yVu">
                <ol permid="OGI" label="a.">
                  <li permid="SNY">
                    <p permid="whS">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="yVh">
                    <p permid="cpb">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="EYB">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(2,9,1)*random(-1,1,2);
                Context("Fraction");
                $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k");
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="AZc">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="vxL" label="a.">
                  <li permid="fcq">
                    <p permid="Iwk">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="oDt">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="Ljz">
                    <p permid="UKC">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="ARL">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="udX">
              <p permid="fcD">
                <ol permid="uNR" label="a.">
                  <li permid="rqI">
                    <p permid="gYU">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="XxR">
                    <p permid="Ngd">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="lfK">
        <webwork>
            <setup>

            <pg-code>
                $f = RandomVariableName(type=>'function');
                $a = non_zero_random(2,9,1)*random(-1,1,2);
                Context("Fraction");
                $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("$a(x - $h)^2 + $k");
                Context("Interval");
                $domain = Compute("(-inf,inf)");
                $range = ($a > 0) ? Compute("[$k,inf)") : Compute("(-inf,$k]");
                Context("Point");
                $vertex = Compute("($h,$k)");
                $posneg = ($a > 0) ? "positive" : "negative";
                $updown = ($a > 0) ? "upward" : "downward";
                $greatless = ($a > 0) ? "greater" : "less";
            </pg-code>
            </setup>
            <statement>
              <p permid="hgl">
                Let <m><var name="$f"/></m> be defined by <m><var name="$f"/>(x)=<var name="$formula"/></m>.

                <ol permid="bEU" label="a.">
                  <li permid="DFa">
                    <p permid="tnm">
                      What is the domain of <m><var name="$f"/></m>?
                    </p>

                    <p permid="Zuv">
                      <var name="$domain" width="10"/>
                    </p>
                  </li>

                  <li permid="jMj">
                    <p permid="FBE">
                      What is the range of <m><var name="$f"/></m>?
                    </p>

                    <p permid="lIN">
                      <var name="$range" width="10"/>
                    </p>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="alg">
              <p permid="LjM">
                <ol permid="aVa" label="a.">
                  <li permid="PTs">
                    <p permid="RPW">
                      The function is quadratic, so the domain is <m><var name="$domain"/></m>.
                      There are no invalid inputs for this function.
                    </p>
                  </li>

                  <li permid="waB">
                    <p permid="xXf">
                      Since the formula is in vertex form,
                      we can see that the graph of <m><var name="$f"/></m> has a vertex at <m><var name="$vertex"/></m>.
                      Since the leading coefficient is <m><var name="$a"/></m> which is <var name="$posneg"/>, the parabola opens <var name="$updown"/>.
                      So the range is all numbers <var name="$greatless"/> than or equal to <m><var name="$k"/></m>.
                      So the range is <m><var name="$range"/></m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="LEL">

      <exercise permid="RmT">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                do {
                    $h = non_zero_random(-9,9,1);
                    $k = non_zero_random(-9,9,1);
                } until (abs($h) != abs($k));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("(x - $h)^2 + $k")->reduce;
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="Nnu">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="tuD">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="Gsp">
              <p permid="rqV">
                Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="xuc">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                do {
                    $h = non_zero_random(-9,9,1);
                    $k = non_zero_random(-9,9,1);
                } until (abs($h) != abs($k));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("(x - $h)^2 + $k")->reduce;
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="ZBM">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="FIV">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="mzy">
              <p permid="Xye">
                Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="dBl">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                do {
                    $h = non_zero_random(-99,99,0.1);
                    $k = non_zero_random(-99,99,0.1);
                } until (abs($h) != abs($k));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("(x - $h)^2 + $k")->reduce;
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="lQe">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="RXn">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="SGH">
              <p permid="DFn">
                Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="JIu">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                do {
                    $h = non_zero_random(-99,99,0.1);
                    $k = non_zero_random(-99,99,0.1);
                } until (abs($h) != abs($k));
                Context()->noreduce('(-x)+y','(-x)-y');
                $formula = Formula("(x - $h)^2 + $k")->reduce;
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="yew">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="elF">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="yNQ">
              <p permid="jMw">
                Since the vertex is <m>(<var name="$h"/>,<var name="$k"/>)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="pPD">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                Context("Fraction");
                do {
                    $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                    $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                    ($hnum,$hden) = $h->value;
                    ($knum,$kden) = $k->value;
                } until (abs($h) != abs($k) and $hden != 1 and $kden != 1);
                Context("Numeric");
                Context()->flags->set(reduceConstants=>0);
                Context()->noreduce('(-x)+y','(-x)-y');
                $abshnum = abs($hnum);
                if ($h > 0) {$formula = Formula("(x - $hnum/$hden)^2");}
                else {$formula = Formula("(x + $abshnum/$hden)^2");}
                $absknum = abs($knum);
                if ($k > 0) {$formula = Formula("$formula+$knum/$kden");}
                else {$formula = Formula("$formula-$absknum/$kden");}
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="KsO">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="qzX">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="eUZ">
              <p permid="PTF">
                Since the vertex is <m>\left(<var name="$h"/>,<var name="$k"/>\right)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="VWM">
        <webwork>
            <pg-macros><macro-file>PCCmacros.pl</macro-file></pg-macros>
            <setup>

            <pg-code>
                Context("Fraction");
                do {
                    $h = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                    $k = Fraction(non_zero_random(-9,9,1),non_zero_random(-9,9,1));
                    ($hnum,$hden) = $h->value;
                    ($knum,$kden) = $k->value;
                } until (abs($h) != abs($k) and $hden != 1 and $kden != 1);
                Context("Numeric");
                Context()->flags->set(reduceConstants=>0);
                Context()->noreduce('(-x)+y','(-x)-y');
                $abshnum = abs($hnum);
                if ($h > 0) {$formula = Formula("(x - $hnum/$hden)^2");}
                else {$formula = Formula("(x + $abshnum/$hden)^2");}
                $absknum = abs($knum);
                if ($k > 0) {$formula = Formula("$formula+$knum/$kden");}
                else {$formula = Formula("$formula-$absknum/$kden");}
                $hdir = ($h > 0) ? PopUp(['?','left','right'],2) : PopUp(['?','left','right'],1);
                $horizontal = MultiAnswer(abs($h),$hdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'left' and (lex_sort($co[1], $st[1]))[1] eq 'right');
                        return 0;
                    }
                );
                $vdir = ($k > 0) ? PopUp(['?','down','up'],2) : PopUp(['?','down','up'],1);
                $vertical = MultiAnswer(abs($k),$vdir)->with(
                    singleResult => 1,
                    checker => sub {
                        my ($correct, $student, $self) = @_;
                        my @co = @{$correct};
                        my @st = @{$student};
                        return 1 if ($co[0] == $st[0] and $co[1] == $st[1] or $co[0] == -$st[0] and (lex_sort($co[1], $st[1]))[0] eq 'down' and (lex_sort($co[1], $st[1]))[1] eq 'up');
                        return 0;
                    }
                );
                $hshift = ($h > 0) ? abs($h)." units right" : abs($h)." units left";
                $vshift = ($k > 0) ? abs($k)." units up" : abs($h)." units down";
            </pg-code>
            </setup>
            <statement>
              <p permid="WHg">
                Consider the graph of the equation <m>y=<var name="$formula"/></m>.
              </p>

              <p permid="COp">
                Compared to the graph of <m>y=x^2</m>,
                the vertex has been shifted <var name="$horizontal" width="5" /> units <var name="$horizontal" form="popup" /> and <var name="$vertical" width="5" /> units <var name="$vertical" form="popup" />.
              </p>
            </statement>
            <solution permid="Lci">
              <p permid="waO">
                Since the vertex is <m>\left(<var name="$h"/>,<var name="$k"/>\right)</m>,
                the graph is the graph of <m>y=x^2</m> shifted <var name="$hshift" /> and <var name="$vshift"/>.
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="rLU" cols="2">
      <title>Three Forms of Quadratic Functions</title>
            <!-- seeds set for diversity -->
      <exercise permid="CdV">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" seed="888"/>
      </exercise>

      <exercise permid="ile">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" seed="890"/>
      </exercise>

      <exercise permid="Osn">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" seed="892"/>
      </exercise>

      <exercise permid="uzw">
        <webwork source="BasicAlgebra/QuadraticFunctions/QuadraticFunctionForms10.pg" seed="895"/>
      </exercise>

    </exercisegroup>
    <exercisegroup permid="XTd" cols="2">
      <title>Factored Form and Intercepts</title>
      <exercise permid="aGF">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = 1;
                do {
                    $r = non_zero_random(-9,9,1);
                    $s = non_zero_random(-9,9,1);
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="iVy">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="HMd" label="a.">
                  <li permid="chK">
                    <p permid="eeo">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="IoT">
                    <p permid="Klx">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="rjr">
              <p permid="chX">
                <ol permid="Hcj" label="a.">
                  <li permid="owc">
                    <p permid="qsG">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="UDl">
                    <p permid="WzP">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="GNO">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = 1;
                do {
                    $r = non_zero_random(-9,9,1);
                    $s = non_zero_random(-9,9,1);
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="PcH">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="nTm" label="a.">
                  <li permid="AKu">
                    <p permid="CGY">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="gRD">
                    <p permid="iOh">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="XqA">
              <p permid="Ipg">
                <ol permid="njs" label="a.">
                  <li permid="MYM">
                    <p permid="OVq">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="tfV">
                    <p permid="vcz">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="mUX">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    $r = non_zero_random(-9,9,1);
                    $s = non_zero_random(-9,9,1);
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="vjQ">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="Uav" label="a.">
                  <li permid="Zne">
                    <p permid="bjI">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="Fun">
                    <p permid="HqR">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="DxJ">
              <p permid="owp">
                <ol permid="TqB" label="a.">
                  <li permid="lBw">
                    <p permid="nya">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="RIF">
                    <p permid="TFj">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="Tcg">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    $r = non_zero_random(-9,9,1);
                    $s = non_zero_random(-9,9,1);
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="bqZ">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="AhE" label="a.">
                  <li permid="xPO">
                    <p permid="zMs">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="dWX">
                    <p permid="fTB">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="jES">
              <p permid="UDy">
                <ol permid="zxK" label="a.">
                  <li permid="Keg">
                    <p permid="MaK">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="qlp">
                    <p permid="shT">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="zjp">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    $r = 0;
                    $s = non_zero_random(-9,9,1);
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="Hyi">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="goN" label="a.">
                  <li permid="Wsy">
                    <p permid="Ypc">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="CzH">
                    <p permid="Ewl">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="PMb">
              <p permid="AKH">
                <ol permid="fET" label="a.">
                  <li permid="iGQ">
                    <p permid="kDu">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="ONZ">
                    <p permid="QKD">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="fqy">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    $r = non_zero_random(-9,9,1);
                    $s = 0;
                } until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="nFr">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="MvW" label="a.">
                  <li permid="uVi">
                    <p permid="wRM">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="bcr">
                    <p permid="cYV">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="vTk">
              <p permid="gRQ">
                <ol permid="LMc" label="a.">
                  <li permid="HjA">
                    <p permid="Jge">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="nqJ">
                    <p permid="pnn">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="LxH">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                #do {
                    $r = non_zero_random(-9,9,1);
                    $s = $r;
                #} until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="TMA">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="sDf" label="a.">
                  <li permid="TxS">
                    <p permid="Vuw">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="zFb">
                    <p permid="BBF">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="cat">
              <p permid="MYZ">
                <ol permid="rTl" label="a.">
                  <li permid="fMk">
                    <p permid="hIO">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="LTt">
                    <p permid="NPX">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="rEQ">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                #do {
                    $r = non_zero_random(-9,9,1);
                    $s = $r;
                #} until ($r != $s);
                $f = RandomVariableName(type=>"function");
                $formula = Formula("$a(x - $r)(x - $s)")->reduce;
                $c = $a*$r*$s;
                $yint = Point(0,$c);
                $xint = ($r == $s) ? List(Point($r,0)) : List(Point($r,0),Point($s,0));
                $fof0 = $formula->substitute(x=>Formula("0"));
            </pg-code>
            </setup>
            <statement>
              <p permid="zTJ">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="YKo" label="a.">
                  <li permid="saC">
                    <p permid="tXg">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="YhL">
                    <p permid="aep">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="IhC">
              <p permid="tgi">
                <ol permid="Yau" label="a.">
                  <li permid="EoU">
                    <p permid="Gly">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="kwd">
                    <p permid="msH">
                      The quadratic function is given in factored form.
                      We can see that subbing in <m>x=<var name="$r" /></m> or <m>x=<var name="$s" /></m> would result in the output being <m>0</m>.
                      So the <m>x</m>-intercepts are <m>(<var name="$r" />, 0)</m> and <m>(<var name="$s" />, 0)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="XLZ">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    do {
                        $p = random(2,9,1);
                        $q = non_zero_random(-9,9,1);
                    } until (gcd($p,$q) == 1);
                    do {
                        $r = random(2,9,1);
                        $s = non_zero_random(-9,9,1);
                    } until (gcd($r,$s) == 1);
                } until ($p != $r);
                $f = RandomVariableName(type=>"function");
                $left = Formula("$p x - $q")->reduce;
                $right = Formula("$r x - $s")->reduce;
                $formula = Formula("$a($left)($right)")->reduce;
                $c = $a*$q*$s;
                $yint = Point(0,$c);
                $xint = Compute("($q/$p,0),($s/$r,0)");
                $fof0 = $formula->substitute(x=>Formula("0"));
                Context("Fraction");
                $r1 = Fraction($q,$p);
                $r2 = Fraction($s,$r);
            </pg-code>
            </setup>
            <statement>
              <p permid="gaS">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="ERx" label="a.">
                  <li permid="QDm">
                    <p permid="SzQ">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="wKv">
                    <p permid="yGZ">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="ooL">
              <p permid="Znr">
                <ol permid="EhD" label="a.">
                  <li permid="cRE">
                    <p permid="eOi">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="IYN">
                    <p permid="KVr">
                      The quadratic function is given in factored form.
                      For the output to be <m>0</m>, either
                      <md permid="xIX">
                        <mrow><var name="$left" />\amp=0\amp\amp\text{or}\amp<var name="$right" />\amp=0</mrow>
                        <mrow><var name="$p" />x\amp=<var name="$q" />\amp\amp\text{or}\amp<var name="$r" />x\amp=<var name="$s" /></mrow>
                        <mrow>x\amp=<var name="$r1" />\amp\amp\text{or}\amp x\amp=<var name="$r2" /></mrow>
                      </md>.
                      So the <m>x</m>-intercepts are <m>\left(<var name="$r1" />, 0\right)</m> and <m>\left(<var name="$r2" />, 0\right)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

      <exercise permid="DTi">
        <webwork>
            <setup>

            <pg-code>
                Context("Point");
                Context()->flags->set(reduceConstants=>0);
                $a = non_zero_random(-9,9,1);
                do {
                    do {
                        $p = random(2,9,1);
                        $q = non_zero_random(-9,9,1);
                    } until (gcd($p,$q) == 1);
                    do {
                        $r = random(2,9,1);
                        $s = non_zero_random(-9,9,1);
                    } until (gcd($r,$s) == 1);
                } until ($p != $r);
                $f = RandomVariableName(type=>"function");
                $left = Formula("$p x - $q")->reduce;
                $right = Formula("$r x - $s")->reduce;
                $formula = Formula("$a($left)($right)")->reduce;
                $c = $a*$q*$s;
                $yint = Point(0,$c);
                $xint = Compute("($q/$p,0),($s/$r,0)");
                $fof0 = $formula->substitute(x=>Formula("0"));
                Context("Fraction");
                $r1 = Fraction($q,$p);
                $r2 = Fraction($s,$r);
            </pg-code>
            </setup>
            <statement>
              <p permid="Mib">
                The formula for a quadratic function <m><var name="$f" /></m> is <m><var name="$f" />(x)=<var name="$formula" /></m>.

                <ol permid="kYG" label="a.">
                  <li permid="pfW">
                    <p permid="rcA">
                      The <m>y</m>-intercept is <var name="$yint" width="20" />.
                    </p>
                  </li>

                  <li permid="Vnf">
                    <p permid="XjJ">
                      The <m>x</m>-intercept(s) is/are <var name="$xint" width="20" />.
                    </p>
                    <instruction>
                      If there are no <m>x</m>-intercepts, enter <c>NONE</c>. If there are more than one, use commas to separate the points.
                    </instruction>
                  </li>
                </ol>
              </p>
            </statement>
            <solution permid="UvU">
              <p permid="FuA">
                <ol permid="koM" label="a.">
                  <li permid="Buo">
                    <p permid="DqS">
                      To find the <m>y</m>-intercept,
                      compute <m><var name="$f" />(0)=<var name="$fof0" />=<var name="$c" /></m>.
                      So the <m>y</m>-intercept is <m><var name="$yint" /></m>.
                    </p>
                  </li>

                  <li permid="hBx">
                    <p permid="jyb">
                      The quadratic function is given in factored form.
                      For the output to be <m>0</m>, either
                      <md permid="dQg">
                        <mrow><var name="$left" />\amp=0\amp\amp\text{or}\amp<var name="$right" />\amp=0</mrow>
                        <mrow><var name="$p" />x\amp=<var name="$q" />\amp\amp\text{or}\amp<var name="$r" />x\amp=<var name="$s" /></mrow>
                        <mrow>x\amp=<var name="$r1" />\amp\amp\text{or}\amp x\amp=<var name="$r2" /></mrow>
                      </md>.
                      So the <m>x</m>-intercepts are <m>\left(<var name="$r1" />, 0\right)</m> and <m>\left(<var name="$r2" />, 0\right)</m>.
                    </p>
                  </li>
                </ol>
              </p>
            </solution>
        </webwork>
      </exercise>

    </exercisegroup>
  </exercises>
</section>

