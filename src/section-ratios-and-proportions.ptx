<?xml version="1.0" encoding="UTF-8" ?>

<!-- *********************************************************************-->
<!-- Copyright 2016-2018                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!-- *********************************************************************-->

<section xml:id="section-ratios-and-proportions">

    <title>Ratios and Proportions</title>

    <objectives>
        <title>PCC Course Content and Outcome Guide</title>
        <ul cols="3">
            <li><xref ref="mth60-solve-linear-equations-and-inequalities" /></li>
            <li><xref ref="mth60-applications-of-linear-equations-and-inequalities" /></li>
            <li><xref ref="mth60-solve-ratio-and-proportion-equations" /></li>
        </ul>
    </objectives>

    <introduction>

         <figure>
            <caption>Alternative Video Lesson</caption>
            <video youtubelist="PLmuZCPhWPt-eoZ-KQPMjIvmir-E924Q99"/>
        </figure>
    </introduction>

    <subsection>
        <title>Introduction</title>

        <p>A <term>ratio</term><idx><h>ratio</h></idx> is a means of comparing two quantities using division. One common example is a unit price. For example, if a box of cereal costs <m>\$3.99</m> and weighs <m>21</m> ounces then we can write this ratio as:
            <me>\frac{\$3.99}{21\,\text{oz}}</me>

        If we want to know the unit price (that is, how much each individual ounce costs), then we can divide <m>\$3.99</m> by <m>21</m> ounces<!-- <quantity><mag>21</mag><unit base="foot"/></quantity> --> and obtain <m>\$0.19</m> per ounce. These two ratios, <m>\frac{\$3.99}{21\,\text{oz}}</m> and <m>0.19\frac{\$}{\text{oz}}</m> are equivalent, and the equation showing that they are equal is a <term>proportion</term><idx><h>proportion</h></idx>. In this case, we could write the following proportion:
            <me>\frac{\$3.99}{21\,\text{oz}}=\frac{\$0.19}{1\,\text{oz}}</me>

        In this section, we will extend this concept and write proportions where one quantity is unknown and solve for that unknown.</p>

        <remark>
            <p>Sometimes ratios are stated using a colon instead of a fraction. For example, the ratio <m>\frac{2}{1}</m> can be written as <m>2:1</m>.</p>
        </remark>

        <example xml:id="example-proportion-in-price">
            <p>Suppose we want to know the total cost for a box of cereal that weighs <m>18</m> ounces, assuming it costs the same per ounce as the <m>21</m>-ounce box. Letting <m>C</m> be this unknown cost (in dollars), we could set up the following proportion:<md>
                <mrow>\frac{\text{cost in dollars}}{\text{weight in oz}}\amp=\frac{\text{cost in dollars}}{\text{weight in oz}}</mrow>
                <mrow>\frac{\$3.99}{21\,\text{oz}}\amp=\frac{\$C}{18\,\text{oz}}</mrow>
            </md>To solve this proportion, we will first note that it will be easier to solve without units:
                <me>\frac{3.99}{21}=\frac{C}{18}</me>
            Next we want to recognize that each side contains a fraction. Our usual approach for solving this type of equation is to multiply each side by the least common denominator (LCD). In this case, the LCD of <m>21</m> and <m>18</m> is <m>126</m>. As with many other proportions we solve, it is often easier to just multiply each side by the common denominator of <m>18\cdot 21</m>, which we know will make each denominator cancel:
                <md>
                    <mrow>\frac{3.99}{21}\amp=\frac{C}{18}</mrow>
                    <mrow>\multiplyleft{18\cdot21}\frac{3.99}{21}\amp=\frac{C}{18}\multiplyright{18\cdot21}</mrow>
                    <mrow>18\cdot\cancelhighlight{21}\frac{3.99}{\cancelhighlight{21}}\amp=\frac{C}{\cancelhighlight{18}}\cdot\cancelhighlight{18}\cdot21</mrow>
                    <mrow>71.82\amp=21C</mrow>
                    <mrow>\divideunder{71.82}{21}\amp=\divideunder{21C}{21}</mrow>
                    <mrow>C\amp=3.42</mrow>
                </md>
            So assuming the cost is proportional to the cost of the <m>21</m>-ounce box, the cost for an <m>18</m>-ounce box of cereal would be <m>\$3.42</m>.</p>
        </example>
    </subsection>

    <subsection>
        <title>Solving Proportions</title>
            <p>Solving proportions uses the process of clearing denominators that we covered in <xref ref="section-linear-equations-and-inequalities-with-fractions">Section</xref>.  Because a proportion is exactly one fraction equal to another, we can simplify the process of clearing the denominators simply by multiplying both sides of the equation by both denominators.  In other words, we don't specifically need the LCD to clear the denominators.</p>

            <example xml:id="example-proportion-straight-algebra">
                <p>Solve <m>\frac{x}{8} = \frac{15}{12}</m> for <m>x</m>.</p>

                <sidebyside widths="47% 47%" margins="0%">
                    <p>Instead of finding the LCD of the two fractions, we'll simply multiply both sides of the equation by <m>8</m> and by <m>12</m>.  This will still have the effect of canceling the denominators on both sides of the equation. <md>
                        <mrow>\frac{x}{8} \amp= \frac{15}{12}</mrow>
                        <mrow>\highlight{12\cdot8\cdot}\frac{x}{8} \amp= \frac{15}{12}\highlight{\cdot12\cdot8}</mrow>
                        <mrow>12\cdot\cancelhighlight{8}\cdot\frac{x}{\cancelhighlight{8}} \amp= \frac{15}{\cancelhighlight{12}}\cdot\cancelhighlight{12}\cdot8</mrow>
                        <mrow>12\cdot x \amp= 15\cdot 8</mrow>
                        <mrow>12x \amp= 120 </mrow>
                        <mrow>\divideunder{12x}{12} \amp= \divideunder{120}{12} </mrow>
                        <mrow> x \amp= 10 </mrow>
                    </md></p>

                    <p>Our work indicates <m>10</m> is the solution.  We can check this as we would for any equation, by substituting <m>10</m> for <m>x</m> and verifying we obtain a true statement:<md>
                        <mrow>\frac{10}{8} \amp\stackrel{?}{=} \frac{15}{12}</mrow>
                        <mrow>\frac{5}{4} \amp\stackrel{\checkmark}{=} \frac{5}{4}</mrow>
                    </md>Since both fractions reduce to <m>\frac{5}{4}</m>, we know the solution to the equation <m>\frac{x}{8} = \frac{15}{12}</m> is <m>10</m> and the solution set is <m>\left\{10\right\}</m>.</p>
                </sidebyside>
            </example>

            <p>When solving proportions, we can use the name <term>cross-multiplication</term><idx><h>cross-multiplication</h></idx> to describe the process of what just occurred. Say we have a proportion<me>\frac{a}{b}=\frac{c}{d}</me>To remove fractions, we multiply both sides with the common denominator, <m>bd</m>, and we have:<md>
                <mrow>\frac{a}{b}\amp=\frac{c}{d}</mrow>
                <mrow>\multiplyleft{bd}\frac{a}{b}\amp=\frac{c}{d}\multiplyright{bd}</mrow>
                <mrow>\cancelhighlight{b}d\cdot\frac{a}{\cancelhighlight{b}}\amp=\frac{c}{\cancelhighlight{d}}\cdot b\cancelhighlight{d}</mrow>
                <mrow>ad\amp=bc</mrow>
            </md>Since <m>a</m> and <m>d</m> are <!-- <q>crossed</q> to --> diagonally across the equals sign from each other in <m>\frac{a}{b}=\frac{c}{d}</m>, as are <m>b</m> and <m>c</m>, we call this approach <term>cross-multiplication</term>. <me>
                \text{If }\frac{a}{b}=\frac{c}{d} \text{, then }ad=bc.
            </me></p>
            <p> If we understand cross-multiplication, we are able to rewrite a proportion <m>\frac{a}{b}=\frac{c}{d}</m> in an equivalent form that does not have any fractions, <m>ad=bc</m>, as our first step of work. If we had used this skill in <xref ref="example-proportion-straight-algebra">Example</xref>, we would have had:<md>
                <mrow>\frac{x}{8} \amp= \frac{15}{12}</mrow>
                <mrow>12\cdot x \amp= 15\cdot 8</mrow>
                <mrow>12x \amp= 120</mrow>
            </md>Notice this is the same equation we had in the fifth line of our work in solving <xref ref="example-proportion-straight-algebra">Example</xref>, but we obtained it without having to contemplate what we need to multiply by to clear the fractions.</p>

            <p>We are able to use cross-multiplication when solving proportions, but it is extremely important to note that cross-multiplication only works when we are solving a proportion, an equation that has one ratio or fraction equal to another ratio or fraction.  If an equation has anything more than one ratio or fraction on a single side of an equation, we cannot use cross-multiplication.  For example, we cannot use cross-multiplication to solve <m>\frac{3}{4}x-\frac{2}{5} = \frac{9}{4}</m>, unless we first manipulate the equation to have exactly one fraction and nothing else on each side of the equation.</p>

            <p>It is also important to be aware of the fact that cross-multiplication is a special version of our general process of clearing fractions: multiplying both sides of an equation by a common denominator of all the fractions in an equation.  </p>

            <example>
                <statement>
                    <p>Solve <m>\frac{t}{5} = \frac{t+2}{3} </m> for <m>t</m>.</p>
                </statement>

                <solution>
                    <p>Again this equation is a proportion, so we are able to multiply both sides of the equation by both denominators to clear the fractions:<md>
                        <mrow>\frac{t}{5} \amp= \frac{t+2}{3}</mrow>
                        <mrow>\highlight{5\cdot3\cdot}\frac{t}{5} \amp= \frac{t+2}{3}\highlight{\cdot5\cdot3}</mrow>
                        <mrow>\cancelhighlight{5}\cdot3\cdot\frac{t}{\cancelhighlight{5}} \amp= \frac{t+2}{\cancelhighlight{3}}\cdot5\cdot\cancelhighlight{3}</mrow>
                        <mrow>3 \cdot t \amp= 5\cdot(t+2)</mrow>
                    </md></p>

                    <sidebyside widths="47% 47%" margins="0%">
                        <p>It is critical that we include the parentheses around <m>t+2</m>, so that we are multiplying <m>5</m> against the entire numerator. <md>
                            <mrow>3t \amp= 5(t+2)</mrow>
                            <mrow>3t \amp= 5t+10</mrow>
                            <mrow>3t \highlight{-5t} \amp= 5t+10 \highlight{-5t}</mrow>
                            <mrow>-2t \amp= 10</mrow>
                            <mrow>\divideunder{-2t}{-2}\amp= \divideunder{10}{-2}</mrow>
                            <mrow>t \amp= -5</mrow>
                        </md></p>

                        <p>We should check that this value <m>-5</m> is actually the solution of the equation:<md>
                            <mrow>\frac{-5}{5} \amp\stackrel{?}{=} \frac{-5+2}{3}</mrow>
                            <mrow> -1 \amp\stackrel{?}{=} \frac{-3}{3}</mrow>
                            <mrow> -1 \amp\stackrel{\checkmark}{=} -1</mrow>
                        </md>Since we have verified that <m>-5</m> is the solution for <m>\frac{t}{5} = \frac{t+2}{3}</m>, we know that the solution set is <m>\{-5\}</m>.</p>
                    </sidebyside>
                </solution>
            </example>

            <example>
                <statement>
                <p>Solve <m>\frac{r+7}{8} = -\frac{9}{4} </m> for <m>r</m>.</p>
                </statement>

                <solution>
                <p>This proportion is a bit different in the fact that one fraction is negative.  The key to working with a negative fraction is to attach the negative sign to either the numerator or denominator, but not both: <me>
                    \frac{-9}{4}=-\frac{9}{4} ~~~\text{ and }~~~ \frac{9}{-4}=-\frac{9}{4}, ~~~\text{ but }~~~ \frac{-9}{-4}=+\frac{9}{4}
                </me>Since we're trying to eliminate the fractions, it will likely make the work a bit easier to attach the negative to the numerator. </p>

                <p>We'll work with the equation in the form <m>\frac{r+7}{8} = \frac{-9}{4} </m><md>
                    <mrow>\frac{r+7}{8} \amp= \frac{-9}{4}</mrow>
                    <mrow>\highlight{8\cdot 4\cdot}\frac{r+7}{8} \amp= \frac{-9}{4}\highlight{\cdot8\cdot 4}</mrow>
                    <mrow>\cancelhighlight{8}\cdot 4\cdot\frac{r+7}{\cancelhighlight{8}} \amp= \frac{-9}{\cancelhighlight{4}}\cdot8\cdot \cancelhighlight{4}</mrow>
                    <mrow>4 \cdot (r+7) \amp= 8\cdot(-9)</mrow>
                    <mrow>4r + 28 \amp= -72 </mrow>
                    <mrow>4r + 28 \highlight{-28} \amp= -72 \highlight{-28}  </mrow>
                    <mrow>4r  \amp= -100  </mrow>
                    <mrow>\divideunder{4r}{4}  \amp= \divideunder{-100}{4}  </mrow>
                    <mrow> r  \amp= -25  </mrow>
                </md>We should check that this value <m>-25</m> is actually the solution of the equation:<md>
                    <mrow>\frac{-25+7}{8} \amp\stackrel{?}{=} -\frac{9}{4}</mrow>
                    <mrow>\frac{-18}{8} \amp\stackrel{?}{=} -\frac{9}{4}</mrow>
                    <mrow> -\frac{9}{4} \amp\stackrel{\checkmark}{=} -\frac{9}{4}</mrow>
                </md>Since we have verified that <m>-25</m> is the solution for <m>\frac{r+7}{8} = -\frac{9}{4} </m>, we know that the solution set is <m>\{-25\}</m>.</p>
                </solution>
            </example>

            <example>
                <statement><p>Solve <m>\frac{x}{15}=\frac{40}{25}</m> for <m>x</m>.</p></statement>
                <solution>
                    <p>To solve this proportion, begin by multiplying both sides by both denominators.
                        <md>
                            <mrow>\frac{x}{15}\amp=\frac{40}{25}</mrow>
                            <mrow>\highlight{15\cdot25}\cdot\frac{x}{15}\amp=\frac{40}{25}\cdot\highlight{15\cdot25}</mrow>
                            <mrow>\cancelhighlight{15}\cdot25\cdot\frac{x}{\cancelhighlight{15}}\amp=\frac{40}{\cancelhighlight{25}}\cdot15\cdot\cancelhighlight{25}</mrow>
                            <mrow>25\cdot x\amp=40\cdot15</mrow>
                            <mrow>25x\amp=600</mrow>
                            <mrow>\divideunder{25x}{25}\amp=\divideunder{600}{25}</mrow>
                            <mrow>x\amp=24</mrow>
                        </md>
                        You can easily verify that this value <m>24</m> is actually the solution of the equation:
                        <md>
                            <mrow>\frac{24}{15}\amp\stackrel{?}{=}\frac{40}{25}</mrow>
                            <mrow>\frac{8}{5}\amp\stackrel{\checkmark}{=}\frac{8}{5}</mrow>
                        </md>
                    Since we have verified that <m>24</m> is the solution for <m>\frac{x}{15}=\frac{40}{25}</m>, we know that the solution set is <m>\{24\}</m>.
                    </p>
                </solution>
            </example>

            <example>
                <statement><p>Solve <m>\frac{x-4}{6}=\frac{x+3}{4}</m> for <m>x</m>.</p></statement>
                <solution>
                    <p>To solve this proportion, begin by multiplying both sides by both denominators.
                        <md>
                            <mrow>\frac{x-4}{6}\amp=\frac{x+3}{4}</mrow>
                            <mrow>\highlight{6\cdot4}\cdot\frac{x-4}{6}\amp=\frac{x+3}{4}\cdot\highlight{6\cdot4}</mrow>
                            <mrow>\cancelhighlight{6}\cdot4\cdot\frac{x-4}{\cancelhighlight{6}}\amp=\frac{x+3}{\cancelhighlight{4}}\cdot6\cdot\cancelhighlight{4}</mrow>
                            <mrow>4\cdot(x-4)\amp=(x+3)\cdot6</mrow>
                            <mrow>4x-16\amp=6x+18</mrow>
                            <mrow>4x-16\addright{16}\amp=6x+18\addright{16}</mrow>
                            <mrow>4x\amp=6x+34</mrow>
                            <mrow>4x\subtractright{6x}\amp=6x+34\subtractright{6x}</mrow>
                            <mrow>-2x\amp=34</mrow>
                            <mrow>\divideunder{-2x}{-2}\amp=\divideunder{34}{-2}</mrow>
                            <mrow>x\amp=-17</mrow>
                        </md>
                        We can check that this value is correct by substituting it back into the original equation:
                        <md>
                            <mrow>\frac{x-4}{6}\amp=\frac{x+3}{4}</mrow>
                            <mrow>\frac{\highlight{-17}-4}{6}\amp\stackrel{?}{=}\frac{\highlight{-17}+3}{4}</mrow>
                            <mrow>\frac{-21}{6}\amp\stackrel{?}{=}\frac{-14}{4}</mrow>
                            <mrow>\frac{-7}{2}\amp\stackrel{\checkmark}{=}\frac{-7}{2}</mrow>
                        </md>
                        Since we have verified that <m>-17</m> is the solution for <m>\frac{x-4}{6}=\frac{x+3}{4}</m>, we know that the solution set is <m>\{-17\}</m>.
                    </p>
                </solution>
            </example>
    </subsection>


    <subsection>
        <title>Proportionality in Similar Triangles</title>

        <p>One really useful example of ratios and proportions involves similar triangles. Two triangles are considered <term>similar</term> if they have the same angles and their side lengths are proportional, as shown in <xref ref="figure-similar-triangles-intro">Figure</xref>:</p>

        <figure xml:id="figure-similar-triangles-intro">
            <caption>Similar Triangles</caption>
            <image width="47%">
                <description></description>
                    <latex-image>
                        <![CDATA[
                            \begin{tikzpicture}
                                \coordinate (A) at (0,1);
                                \coordinate (B) at (0,0);
                                \coordinate (C) at (1.732,0);
                                \draw (A)--(B)--(C)--cycle;
                                \tkzLabelSegment[sloped](B,A){$1$ cm}
                                \tkzLabelSegment[below](B,C){$\sqrt{3}$ cm}
                                \tkzLabelSegment[sloped](C,A){$2$ cm}
                                \tkzMarkAngle[size=0.3](B,A,C)
                                \tkzLabelAngle[pos = -0.2](B,A,C){$60^{\circ}$}
                                \tkzMarkRightAngle(A,B,C)

                                \coordinate (D) at (3,3);
                                \coordinate (E) at (3,0);
                                \coordinate (F) at (8.196,0);
                                \draw (D)--(E)--(F)--cycle;
                                \tkzLabelSegment[sloped](E,D){$3$ cm}
                                \tkzLabelSegment[below](E,F){$3\sqrt{3}$ cm}
                                \tkzLabelSegment[sloped](F,D){$6$ cm}
                                \tkzMarkAngle[size=0.3](E,D,F)
                                \tkzLabelAngle[pos = 0.7](E,D,F){$60^{\circ}$}
                                \tkzMarkRightAngle(D,E,F)
                            \end{tikzpicture}
                        ]]>
                </latex-image>
            </image>
        </figure>

        <p>In the first triangle in <xref ref="figure-similar-triangles-intro">Figure</xref>, the ratio of the left side length to the hypotenuse length is <m>\frac{1\,\text{cm}}{2\,\text{cm}}</m>; in the second triangle, the ratio of the left side length to the hypotenuse length is <m>\frac{3\,\text{cm}}{6\,\text{cm}}</m>. Since both reduce to <m>\frac{1}{2}</m>, we can write the following proportion:<me>\frac{1\,\text{cm}}{2\,\text{cm}}=\frac{3\,\text{cm}}{6\,\text{cm}}</me></p>

        <p>If we extend this concept, we can use it to solve for an unknown side length. Consider the two similar triangles in the next example.</p>

        <example>
            <figure xml:id="figure-similar-triangles-with-unknown">
                <caption>Similar Triangles</caption>
                <image width="70%">
                    <description></description>
                    <latex-image>
                        <![CDATA[
                            \begin{tikzpicture}
                                \coordinate (A) at (0,3);
                                \coordinate (B) at (0,0);
                                \coordinate (C) at (4,0);
                                \draw (A)--(B)--(C)--cycle;
                                \tkzLabelSegment[sloped](B,A){$3$ cm}
                                \tkzLabelSegment[below](B,C){$4$ cm}
                                \tkzMarkAngle[size=0.7](A,C,B)
                                \tkzLabelAngle[pos = 0.6,left](A,C,B){$53.13^{\circ}$}
                                \tkzMarkRightAngle(A,B,C)

                                \coordinate (D) at (5,4.5);
                                \coordinate (E) at (5,0);
                                \coordinate (F) at (11,0);
                                \draw (D)--(E)--(F)--cycle;
                                \tkzLabelSegment[sloped](E,D){$x$ cm}
                                \tkzLabelSegment[below](E,F){$6$ cm}
                                \tkzMarkAngle[size=0.7](D,F,E)
                                \tkzLabelAngle[pos = 0.6,left](D,F,E){$53.13^{\circ}$}
                                \tkzMarkRightAngle(D,E,F)
                            \end{tikzpicture}
                       ]]>
                       </latex-image>
                </image>
            </figure>

            <p>Since the two triangles are similar, we know that their side length should be proportional. To determine the unknown length, we can set up a proportion and solve for <m>x</m>:<md>
                    <mrow>\frac{\text{bigger triangle's left side length in cm}}{\text{bigger triangle's bottom side length in cm}}\amp=\frac{\text{smaller triangle's left side length in cm}}{\text{smaller triangle's bottom side length in cm}}</mrow>
                    <mrow>\frac{x\,\text{cm}}{6\,\text{cm}}\amp=\frac{3\,\text{cm}}{4\,\text{cm}}</mrow>
                    <mrow>\frac{x}{6}\amp=\frac{3}{4}</mrow>
                    <mrow>\multiplyleft{12}\frac{x}{6}\amp=\multiplyleft{12}\frac{3}{4}\qquad\text{(12 is the least common denominator)}</mrow>
                    <mrow>2x\amp=9</mrow>
                    <mrow>\divideunder{2x}{2}\amp=\divideunder{9}{2}</mrow>
                    <mrow>x\amp=\frac{9}{2}\ \text{or}\ 4.5</mrow>
                </md>
                The unknown side length is then <quantity><mag>4.5</mag><unit prefix="centi" base="meter"/></quantity>.
            </p>
        </example>

        <remark><p>
            Looking at the triangles in <xref ref="figure-similar-triangles-intro">Figure</xref>, you may notice that there are many different proportions you could set up, such as:
                <md>
                    <mrow>\frac{2\,\text{cm}}{1\,\text{cm}}\amp=\frac{6\,\text{cm}}{3\,\text{cm}}</mrow>
                    <mrow>\frac{2\,\text{cm}}{6\,\text{cm}}\amp=\frac{1\,\text{cm}}{3\,\text{cm}}</mrow>
                    <mrow>\frac{6\,\text{cm}}{2\,\text{cm}}\amp=\frac{3\,\text{cm}}{1\,\text{cm}}</mrow>
                    <mrow>\frac{3\sqrt{3}\,\text{cm}}{\sqrt{3}\,\text{cm}}\amp=\frac{3\,\text{cm}}{1\,\text{cm}}</mrow>
                </md>
            This is often the case when we set up ratios and proportions.</p>

        <p>If we take a second look at <xref ref="figure-similar-triangles-with-unknown">Figure</xref>, there are also several other proportions we could have used to find the value of <m>x</m>.<md>
            <mrow>\frac{\text{bigger triangle's left side length}}{\text{smaller triangle's left side length}}\amp=\frac{\text{bigger triangle's bottom side length}}{\text{smaller triangle's bottom side length}} </mrow>
            <mrow />
            <mrow>\frac{\text{smaller triangle's bottom side length}}{\text{bigger triangle's bottom side length}}\amp=\frac{\text{smaller triangle's left side length}}{\text{bigger triangle's left side length}} </mrow>
            <mrow />
            <mrow>\frac{\text{bigger triangle's bottom side length}}{\text{smaller triangle's bottom side length}}\amp=\frac{\text{bigger triangle's left side length} }{\text{smaller triangle's left side length}} </mrow>
        </md>Written as algebraic proportions, these three equations would, respectively, be <md>
            <mrow>\frac{x\,\text{cm}}{3\,\text{cm}}\amp= \frac{6\,\text{cm}}{4\,\text{cm}}, \amp
            \frac{4\,\text{cm}}{6\,\text{cm}}\amp= \frac{3\,\text{cm}}{x\,\text{cm}}, \amp
            \frac{6\,\text{cm}}{4\,\text{cm}}\amp= \frac{x\,\text{cm}}{3\,\text{cm}}</mrow>
        </md>While these are only a few of the possibilities,  if we clear the denominators from any properly designed proportion, every one is equivalent to <m>x=4.5</m>.</p>
        </remark>
    </subsection>

    <subsection>
        <title>Creating and Solving Proportions</title>

        <p>Proportions can be used to solve many real-life applications. The key to using proportions to solve such applications is to first set up a ratio where all values are known.  We then set up a second ratio that will be proportional to the first, but has one value in the ratio unknown.  Let's look at a few examples.</p>

        <example>
            <statement>
                <p>Property taxes for a residential property are proportional to the assessed value of the property. Assume that a certain property in a given neighborhood is assessed at <m>\$234{,}100</m> and its annual property taxes are <m>\$2{,}518.92</m>. What are the annual property taxes for a house that is assessed at <m>\$287{,}500</m>?</p>
            </statement>
            <solution>
                <p>Let <m>T</m> be the annual property taxes (in dollars) for a property assessed at <m>\$287{,}500</m>. We can write and solve this proportion:
                        <md>
                            <mrow>\frac{\text{tax}}{\text{property value}}\amp=\frac{\text{tax}}{\text{property value}}</mrow>
                            <mrow>\frac{2518.92}{234100}\amp=\frac{T}{287500}</mrow>
                        </md>
                    The least common denominator of this proportion is rather large, so we will instead multiply each side by <m>234100</m> and <m>287500</m> and simplify from there:
                        <md>
                            <mrow>\frac{2518.92}{234100}\amp=\frac{T}{287500}</mrow>
                            <mrow>\multiplyleft{234100\cdot287500}\frac{2518.92}{234100}\amp=\frac{T}{287500}\multiplyright{234100\cdot287500}</mrow>
                            <mrow>287500\cdot2518.92 \amp=T\cdot 234100</mrow>
                            <mrow>\frac{287500\cdot 2518.92}{234100} \amp=\frac{234100T}{234100}</mrow>
                            <mrow>T\amp\approx3093.50</mrow>
                        </md>
                    The property taxes for a property assessed at <m>\$287,500</m> are <m>\$3,093.50</m>.
                </p>
            </solution>
        </example>

        <example>
            <statement>
                <p>Tagging fish is a means of estimating the size of the population of fish in a body of water (such as a lake). A sample of fish is taken, tagged, and then redistributed into the lake. When another sample is taken, the proportion of fish that are tagged out of that sample are assumed to be proportional to the total number of fish tagged out of the entire population of fish in the lake.
                    <me>\frac{\text{number of tagged fish in sample}}{\text{number of fish in sample}}=\frac{\text{number of tagged fish total}}{\text{number of fish total}}</me></p>

                <p>Assume that <m>90</m> fish are caught and tagged. Once they are redistributed, a sample of <m>200</m> fish is taken. Of these, <m>7</m> are tagged. Estimate how many fish total are in the lake.</p>
            </statement>
            <solution>
                <p>Let <m>n</m> be the number of fish in the lake. We can set up a proportion for this scenario:
                        <md>
                            <mrow>\frac{7}{200}\amp=\frac{90}{n}</mrow>
                        </md>
                    To solve for <m>n</m>, which is in a denominator, we'll need to multiply each side by both <m>200</m> and <m>n</m>:
                        <md>
                            <mrow>\frac{7}{200}\amp=\frac{90}{n}</mrow>
                            <mrow>\multiplyleft{200\cdot n}\frac{7}{200}\amp=\frac{90}{n}\multiplyright{200\cdot n}</mrow>
                            <mrow>{\cancelhighlight{200}\cdot n}\cdot\frac{7}{\cancelhighlight{200}}\amp=\frac{90}{\cancelhighlight{n}}\cdot200\cdot\cancelhighlight{n}</mrow>
                            <mrow>7n\amp=1800</mrow>
                            <mrow>\frac{7n}{7}\amp=\frac{1800}{7}</mrow>
                            <mrow>n\amp\approx 2471.4286</mrow>
                        </md>
                According to this sample, we can estimate that there are about <m>2,471</m> fish in the lake.</p>
            </solution>
        </example>

        <example>
            <statement>
                <p>Infant Tylenol contains <quantity><mag>160</mag><unit prefix="milli" base="gram"/></quantity> of acetaminophen in each <quantity><mag>5</mag><unit prefix="milli" base="liter"/></quantity> of liquid medicine. If Bao's baby is prescribed <quantity><mag>60</mag><unit prefix="milli" base="gram"/></quantity> of acetaminophen, how many milliliters of liquid medicine should he give them?</p>
            </statement>
            <solution>
                <p>Assume Bao should give <m>q</m> milliliters of liquid medicine, and we can set up the following proportion:
                    <md>
                        <mrow>\frac{\text{amount of liquid medicine in mL}}{\text{amount of acetaminophen in mg}}\amp=\frac{\text{amount of liquid medicine in mL}}{\text{amount of acetaminophen in mg}}</mrow>
                        <mrow>\frac{5\,\text{mL}}{160\,\text{mg}}\amp=\frac{q\,\text{mL}}{60\,\text{mg}}</mrow>
                        <mrow>\frac{5}{160}\amp=\frac{q}{60}</mrow>
                        <mrow>\multiplyleft{160\cdot 60}\frac{5}{160}\amp=\frac{q}{60}\multiplyright{160\cdot 60}</mrow>
                        <mrow>60\cdot 5 \amp=q\cdot 160</mrow>
                        <mrow>300\amp=160q</mrow>
                        <mrow>\frac{300}{160}\amp=\frac{160q}{160}</mrow>
                        <mrow>q\amp=1.875</mrow>
                    </md>
                So to give <quantity><mag>60</mag><unit prefix="milli" base="gram"/></quantity> of acetaminophen to his baby, Bao should give <quantity><mag>1.875</mag><unit prefix="milli" base="liter"/></quantity> of liquid medicine.
                </p>
            </solution>
        </example>

        <example>
            <statement>
                <p>Sarah is an architect and she's making a scale model of a building. The actual building will be <quantity><mag>30</mag><unit base="foot"/></quantity> tall. In the model, the height of the building will be <quantity><mag>2</mag><unit base="inch"/></quantity>. How tall should she make the model of a person who is <quantity><mag>5</mag><unit base="foot"/></quantity> <quantity><mag>6</mag><unit base="inch"/></quantity> tall so that the model is to scale?
                </p>
            </statement>
            <solution>
                <p>Let <m>h</m> be the height of the person in Sarah's model, which we'll measure in inches. We'll create a proportion that compares the building and person's heights in the model to their heights in real life:
                    <md>
                        <mrow>\frac{\text{height of model building in inches}}{\text{height of actual building in feet}}\amp=\frac{\text{height of model person in inches}}{\text{height of actual person in feet}}</mrow>
                        <mrow>\frac{2\,\text{in}}{30\,\text{ft}}\amp=\frac{h\,\text{in}}{5\,\text{ft}\,6\text{in}}</mrow>
                    </md>
                Before we can just eliminate the units, we'll need to convert <quantity><mag>5</mag><unit base="foot"/></quantity> <quantity><mag>6</mag><unit base="inch"/></quantity> to feet:
                        <me>\frac{2\,\text{in}}{30\,\text{ft}}=\frac{h\,\text{in}}{5.5\,\text{ft}}</me>
                Now we can remove the units and continue solving:
                    <md>
                        <mrow>\frac{2}{30}\amp=\frac{h}{5.5}</mrow>
                        <mrow>\multiplyleft{30\cdot5.5}\frac{2}{30}\amp=\frac{h}{5.5}\multiplyright{30\cdot5.5}</mrow>
                        <mrow>5.5\cdot 2 \amp= h\cdot 30</mrow>
                        <mrow>11\amp=30h</mrow>
                        <mrow>\frac{11}{30}\amp=\frac{30h}{30}</mrow>
                        <mrow>\frac{11}{30}\amp=h</mrow>
                        <mrow>h\amp\approx 0.3667</mrow>
                    </md>
                Sarah should make the model of a person who is <quantity><mag>5</mag><unit base="foot"/></quantity> <quantity><mag>6</mag><unit base="inch"/></quantity> tall be <m>\frac{11}{30}</m> inches (about <m>0.3667</m> inches) tall.
                </p>
            </solution>
        </example>
    </subsection>

    <exercises>
        <exercisegroup cols="4">
            <title>Review and Warmup</title>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction05.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction05.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction10.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction10.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction20.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction20.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction30.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/FractionMixedNumbersBasics/ReduceFraction30.pg" />
            </exercise>
        </exercisegroup>
        <exercisegroup>
            <title>Setting Up Ratios and Proportions</title>
             <exercise>
                <webwork source="BasicAlgebra/LinearEquationApplications/ModelingWithEquation80.pg" />
            </exercise>
             <exercise>
                <webwork source="BasicAlgebra/LinearEquationApplications/ModelingWithEquation80.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/LinearEquationApplications/ModelingWithEquation90.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/LinearEquationApplications/ModelingWithEquation90.pg" />
            </exercise>
        </exercisegroup>
        <exercisegroup cols="3">
            <title>Solving Proportions</title>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = $p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($p[1]/$p[2]);
                            } until (($c &lt; $d) and ($a &lt; 20) );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m> and then simplify the right side of the equation. <md>
                            <mrow>\frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>}\amp= \frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>
                            <mrow>x \amp= \frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>
                            <mrow> x \amp= <var name="$a"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = $p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($p[1]/$p[2]);
                            } until (($c &lt; $d) and ($a &lt; 20) );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m> and then simplify the right side of the equation. <md>
                            <mrow> \frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>} \amp= \frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>
                            <mrow>x \amp= \frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>
                            <mrow> x \amp= <var name="$a"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>


            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = $p[1]*$p[0];
                                $b = $p[1]*$p[2];
                                $c = $p[3]*$p[0];
                                $d = $p[3]*$p[2];
                                $r = Fraction($p[2]/$p[0]);
                            } until (($a &lt; 20) );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{<var name="$b"/>}{x}=\frac{<var name="$d"/>}{<var name="$c"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{<var name="$b"/>}{x}=\frac{<var name="$d"/>}{<var name="$c"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m>x</m> and <m><var name="$c"/></m>.  We'll then isolate <m>x</m> by dividing both sides of the equation by <m><var name="$d"/></m>.<md>
                            <mrow>\frac{<var name="$b"/>}{x}\multiplyright{x\cdot <var name="$c"/>} \amp= \frac{<var name="$d"/>}{<var name="$c"/>} \multiplyright{x\cdot <var name="$c"/>}</mrow>
                            <mrow><var name="$b"/>\cdot<var name="$c"/> \amp=  <var name="$d"/>x </mrow>
                            <mrow>\divideunder{<var name="$c"/>\cdot<var name="$b"/>}{<var name="$d"/>} \amp= \divideunder{ <var name="$d"/>x}{<var name="$d"/>} </mrow>
                            <mrow><var name="$a"/> \amp= x</mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$b"/>}{<var name="$a"/>}</m> and <m>\frac{<var name="$d"/>}{<var name="$c"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = $p[1]*$p[0];
                                $b = $p[1]*$p[2];
                                $c = $p[3]*$p[0];
                                $d = $p[3]*$p[2];
                                $r = Fraction($p[2]/$p[0]);
                            } until (($a &lt; 20) );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{<var name="$b"/>}{x}=\frac{<var name="$d"/>}{<var name="$c"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{<var name="$b"/>}{x}=\frac{<var name="$d"/>}{<var name="$c"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m>x</m> and <m><var name="$c"/></m>.  We'll then isolate <m>x</m> by dividing both sides of the equation by <m><var name="$d"/></m>.<md>
                            <mrow>\frac{<var name="$b"/>}{x}\multiplyright{x\cdot <var name="$c"/>} \amp= \frac{<var name="$d"/>}{<var name="$c"/>} \multiplyright{x\cdot <var name="$c"/>}</mrow>
                            <mrow><var name="$b"/>\cdot<var name="$c"/> \amp=  <var name="$d"/>x </mrow>
                            <mrow>\divideunder{<var name="$c"/>\cdot<var name="$b"/>}{<var name="$d"/>} \amp= \divideunder{ <var name="$d"/>x}{<var name="$d"/>} </mrow>
                            <mrow><var name="$a"/> \amp= x</mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$b"/>}{<var name="$a"/>}</m> and <m>\frac{<var name="$d"/>}{<var name="$c"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>


            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,3));
                                $q = random(2,6,1);
                                $b = $p[0];
                                $d = $p[1];
                                $x = $d+3;
                                $e = $d-$b;
                                $c = $q*$e;
                                $a = ($c*$b)/($b-$d);
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $r = Fraction($a/$b);
                            } until ($e &gt; 1);
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x}{<var name="$b"/>} \amp= \frac{x - <var name="$c"/>}{<var name="$d"/>}  </mrow>
                            <mrow>  \frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>\cdot \left(x - <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>x - <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x \subtractright{<var name="$b"/>x} \amp= <var name="$b"/>x - <var name="$bc"/> \subtractright{<var name="$b"/>x} </mrow>
                            <mrow> <var name="$dmb"/>x  \amp= - <var name="$bc"/>  </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>}  \amp= \divideunder{- <var name="$bc"/>}{<var name="$dmb"/>}  </mrow>
                            <mrow>x \amp= <var name="$a"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$a"/>-<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 5} (NchooseK(8,2));
                                $q = random(2,6,1);
                                $b = $p[0];
                                $d = $p[1];
                                $dc = $d+2;
                                $e = $d-$b;
                                $c = $q*$e;
                                $a = ($c*$b)/($d-$b);
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $r = Fraction($a/$b);
                            } until ( $b &lt; $dc );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x}{<var name="$b"/>} \amp= \frac{x + <var name="$c"/>}{<var name="$d"/>}  </mrow>
                            <mrow>  \frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x + <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>\cdot \left(x + <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>x + <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x \subtractright{<var name="$b"/>x} \amp= <var name="$b"/>x + <var name="$bc"/> \subtractright{<var name="$b"/>x} </mrow>
                            <mrow> <var name="$dmb"/>x  \amp=  <var name="$bc"/>  </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>}  \amp= \divideunder{ <var name="$bc"/>}{<var name="$dmb"/>}  </mrow>
                            <mrow>x \amp= <var name="$a"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$a"/>+<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 6} (NchooseK(8,2));
                                $q = random(2,6,1);
                                $b = $p[0];
                                $d = $p[1];
                                $dc = $d+2;
                                $e = $d-$b;
                                $c = $q*$e;
                                $a = ($c*$b)/($d-$b);
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $r = Fraction($a/$b);
                            } until ( $b &lt; $dc );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x}{<var name="$b"/>} \amp= \frac{x + <var name="$c"/>}{<var name="$d"/>}  </mrow>
                            <mrow>  \frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x + <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>\cdot \left(x + <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>x + <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x \subtractright{<var name="$b"/>x} \amp= <var name="$b"/>x + <var name="$bc"/> \subtractright{<var name="$b"/>x} </mrow>
                            <mrow> <var name="$dmb"/>x  \amp=  <var name="$bc"/>  </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>}  \amp= \divideunder{ <var name="$bc"/>}{<var name="$dmb"/>}  </mrow>
                            <mrow>x \amp= <var name="$a"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$a"/>+<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                 </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 5} (NchooseK(8,2));
                                $q = random(2,6,1);
                                $b = $p[0];
                                $d = $p[1];
                                $dc = $d+2;
                                $e = $d-$b;
                                $c = $q*$e;
                                $a = ($c*$b)/($b-$d);
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $r = Fraction($a/$b);
                            } until ( ($b &lt; $dc) );
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x}{<var name="$b"/>} \amp= \frac{x - <var name="$c"/>}{<var name="$d"/>}  </mrow>
                            <mrow>  \frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>\cdot \left(x - <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x \amp= <var name="$b"/>x - <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x \subtractright{<var name="$b"/>x} \amp= <var name="$b"/>x - <var name="$bc"/> \subtractright{<var name="$b"/>x} </mrow>
                            <mrow> <var name="$dmb"/>x  \amp= - <var name="$bc"/>  </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>}  \amp= \divideunder{- <var name="$bc"/>}{<var name="$dmb"/>}  </mrow>
                            <mrow>x \amp= <var name="$a"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$a"/>-<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,2));
                                $b = $p[0];
                                $d = $p[0] + $p[1];
                                $a = random(2,16,1);
                                $c = random(2,16,1);
                                $check1 = -1*$a*$d + (-1)*$b*$c;
                                $check2 = $d - $b;
                                $Ans = $check1/$check2;
                                $ad = $a*$d;
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $nbcmad = -1*$bc - $ad;
                                $r = Fraction(($Ans + $a)/$b);
                            } until ( $check1%$check2 == 0);
                            $Ans = Fraction($Ans);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x + <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x + <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x + <var name="$a"/>}{<var name="$b"/>} \amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} </mrow>
                            <mrow>   \frac{x + <var name="$a"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>\cdot\left(x + <var name="$a"/>\right) \amp= <var name="$b"/>\cdot \left(x - <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x + <var name="$ad"/> \amp= <var name="$b"/>x - <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x + <var name="$ad"/>\subtractright{<var name="$b"/>x}  \amp= <var name="$b"/>x - <var name="$bc"/>\subtractright{<var name="$b"/>x} </mrow>
                            <mrow><var name="$dmb"/>x + <var name="$ad"/>  \amp= -<var name="$bc"/> </mrow>
                            <mrow><var name="$dmb"/>x + <var name="$ad"/> \subtractright{ <var name="$ad"/>} \amp= -<var name="$bc"/>  \subtractright{ <var name="$ad"/>}</mrow>
                            <mrow><var name="$dmb"/>x \amp= <var name="$nbcmad"/> </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>} \amp= \divideunder{<var name="$nbcmad"/>}{<var name="$dmb"/>} </mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$Ans"/>+<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$Ans"/>-<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,2));
                                $b = $p[0];
                                $d = $p[0] + $p[1];
                                $a = random(3,16,1);
                                $c = random(3,16,1);
                                $check1 = $a*$d + $b*$c;
                                $check2 = $d - $b;
                                $Ans = $check1/$check2;
                                $ad = $a*$d;
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $bcpad = $bc + $ad;
                                $r = Fraction(($Ans - $a)/$b);
                            } until ( $check1%$check2 == 0);
                            $Ans = Fraction($Ans);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x + <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x - <var name="$a"/>}{<var name="$b"/>} \amp= \frac{x + <var name="$c"/>}{<var name="$d"/>} </mrow>
                            <mrow>   \frac{x - <var name="$a"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x + <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>\cdot\left(x - <var name="$a"/>\right) \amp= <var name="$b"/>\cdot \left(x + <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x - <var name="$ad"/> \amp= <var name="$b"/>x + <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x - <var name="$ad"/>\subtractright{<var name="$b"/>x}  \amp= <var name="$b"/>x + <var name="$bc"/>\subtractright{<var name="$b"/>x} </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/>  \amp= <var name="$bc"/> </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/> \addright{ <var name="$ad"/>} \amp= <var name="$bc"/>  \addright{ <var name="$ad"/>}</mrow>
                            <mrow><var name="$dmb"/>x \amp= <var name="$bcpad"/> </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>} \amp= \divideunder{<var name="$bcpad"/>}{<var name="$dmb"/>} </mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$Ans"/>-<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$Ans"/>+<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,2));
                                $b = $p[0];
                                $d = $p[0] + $p[1];
                                $a = random(2,16,1);
                                $c = random(2,16,1);
                                $check1 = $a*$d - $b*$c;
                                $check2 = $d - $b;
                                $Ans = $check1/$check2;
                                $ad = $a*$d;
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $nbcpad = -1*$bc + $ad;
                                $r = Fraction( ($Ans-$a)/$b);
                            } until ( $check1%$check2 == 0);
                            $Ans = Fraction($Ans);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x - <var name="$a"/>}{<var name="$b"/>} \amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} </mrow>
                            <mrow>   \frac{x - <var name="$a"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>\cdot\left(x - <var name="$a"/>\right) \amp= <var name="$b"/>\cdot \left(x - <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x - <var name="$ad"/> \amp= <var name="$b"/>x - <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x - <var name="$ad"/>\subtractright{<var name="$b"/>x}  \amp= <var name="$b"/>x - <var name="$bc"/>\subtractright{<var name="$b"/>x} </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/>  \amp= - <var name="$bc"/> </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/> \addright{ <var name="$ad"/>} \amp= -<var name="$bc"/>  \addright{ <var name="$ad"/>}</mrow>
                            <mrow><var name="$dmb"/>x \amp= <var name="$nbcpad"/> </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>} \amp= \divideunder{<var name="$nbcpad"/>}{<var name="$dmb"/>} </mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$Ans"/>-<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$Ans"/>-<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,2));
                                $b = $p[0];
                                $d = $p[0] + $p[1];
                                $a = random(3,16,1);
                                $c = random(3,16,1);
                                $check1 = $a*$d - $b*$c;
                                $check2 = $d - $b;
                                $Ans = $check1/$check2;
                                $ad = $a*$d;
                                $bc = $b*$c;
                                $dmb = $d-$b;
                                $nbcpad = -1*$bc + $ad;
                                $r = Fraction( ($Ans-$a)/$b);
                            } until ( $check1%$check2 == 0);
                            $Ans = Fraction($Ans);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x - <var name="$a"/>}{<var name="$b"/>}=\frac{x - <var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we will first multiply both sides of the equation by <m><var name="$b"/></m> and <m><var name="$d"/></m>.<md>
                            <mrow>  \frac{x - <var name="$a"/>}{<var name="$b"/>} \amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} </mrow>
                            <mrow>   \frac{x - <var name="$a"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>}\amp= \frac{x - <var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>\cdot <var name="$d"/>} </mrow>
                            <mrow><var name="$d"/>\cdot\left(x - <var name="$a"/>\right) \amp= <var name="$b"/>\cdot \left(x - <var name="$c"/>\right)</mrow>
                        </md> We will now finish using the standard process for solving linear equations in one variable: simplify, isolate, eliminate, check, and summarize. <md>
                            <mrow><var name="$d"/>x - <var name="$ad"/> \amp= <var name="$b"/>x - <var name="$bc"/></mrow>
                            <mrow><var name="$d"/>x - <var name="$ad"/>\subtractright{<var name="$b"/>x}  \amp= <var name="$b"/>x - <var name="$bc"/>\subtractright{<var name="$b"/>x} </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/>  \amp= - <var name="$bc"/> </mrow>
                            <mrow><var name="$dmb"/>x - <var name="$ad"/> \addright{ <var name="$ad"/>} \amp= -<var name="$bc"/>  \addright{ <var name="$ad"/>}</mrow>
                            <mrow><var name="$dmb"/>x \amp= <var name="$nbcpad"/> </mrow>
                            <mrow> \divideunder{<var name="$dmb"/>x}{<var name="$dmb"/>} \amp= \divideunder{<var name="$nbcpad"/>}{<var name="$dmb"/>} </mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md> </p>
                        <p>Since <m>\frac{<var name="$Ans"/>-<var name="$a"/>}{<var name="$b"/>}</m> and <m>\frac{<var name="$Ans"/>-<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>


            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = -1*$p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($a/$b);
                            } until (($c &gt; $d) and ($a &lt; 20) and ($c%$d != 0 ));
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m> and then simplify the right side of the equation. <md>
                            <mrow>\frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>}\amp= -\frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>
                            <mrow>x \amp= -\frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>
                            <mrow> x \amp= <var name="$a"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>-\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = -1*$p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($a/$b);
                            } until (($c &gt; $d) and ($a &lt; 20) and ($c%$d != 0 ));
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$a}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m> and then simplify the right side of the equation. <md>
                            <mrow>\frac{x}{<var name="$b"/>} \multiplyright{<var name="$b"/>}\amp= -\frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>
                            <mrow>x \amp= -\frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>
                            <mrow> x \amp= <var name="$a"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$a"/>}{<var name="$b"/>}</m> and <m>-\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$a"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$a"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = -1*$p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($a/$b);
                                $e = random(2,5,1);
                                $cbd = $c*$b/$d;
                                $Ans = $a-$e;
                                $r = Fraction(-1*$c/$d);
                            } until (($c &gt; $d) and ($a &lt; 20) and ($c%$d != 0 ));
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x+<var name="$e"/>}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x+<var name="$e"/>}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m>, then simplify the right side of the equation, and then subtract <m><var name="$e"/></m>. <md>
                            <mrow>\frac{x+<var name="$e"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>}\amp= -\frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>

                            <mrow>x+<var name="$e"/> \amp= -\frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>

                            <mrow>x+<var name="$e"/> - <var name="$e"/> \amp= -<var name="$cbd"/>- <var name="$e"/></mrow>

                            <mrow> x \amp= <var name="$Ans"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$Ans"/>+<var name="$e"/>}{<var name="$b"/>}</m> and <m>-\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork>
                    <setup>
                        <pg-code>
                            Context("LimitedFraction");
                            Context()-&gt;flags-&gt;set(formatStudentAnswer=>'parsed');
                            do {
                                @p = map{$_ + 2} (NchooseK(8,4));
                                $a = -1*$p[0]*$p[1];
                                $b = $p[0]*$p[2];
                                $c = $p[3]*$p[1];
                                $d = $p[3]*$p[2];
                                $r = Fraction($a/$b);
                                $e = random(2,5,1);
                                $cbd = $c*$b/$d;
                                $Ans = $a+$e;
                                $r = Fraction(-1*$c/$d);
                            } until (($c &gt; $d) and ($a &lt; 20) and ($c%$d != 0 ));
                            $a = Fraction($a);
                            Context("FiniteSolutionSets");
                            $ans = Formula("{$Ans}");
                        </pg-code>
                    </setup>
                    <statement>
                        <p>Solve <m>\frac{x-<var name="$e"/>}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>.</p>
                        <p><var name="$ans" width="10" /></p>
                    </statement>
                    <solution>
                        <p>To solve <m>\frac{x-<var name="$e"/>}{<var name="$b"/>}=-\frac{<var name="$c"/>}{<var name="$d"/>}</m> for <m>x</m>, we can multiply both sides of the equation by <m><var name="$b"/></m>, then simplify the right side of the equation, and then subtract <m><var name="$e"/></m>. <md>
                            <mrow>\frac{x-<var name="$e"/>}{<var name="$b"/>} \multiplyright{<var name="$b"/>}\amp= -\frac{<var name="$c"/>}{<var name="$d"/>} \multiplyright{<var name="$b"/>} </mrow>

                            <mrow>x-<var name="$e"/> \amp= -\frac{<var name="$c"/> \cdot <var name="$b"/>}{<var name="$d"/>}</mrow>

                            <mrow>x-<var name="$e"/> + <var name="$e"/> \amp= -<var name="$cbd"/>+ <var name="$e"/></mrow>

                            <mrow> x \amp= <var name="$Ans"/></mrow>
                        </md></p>
                        <p>Since <m>\frac{<var name="$Ans"/>-<var name="$e"/>}{<var name="$b"/>}</m> and <m>-\frac{<var name="$c"/>}{<var name="$d"/>}</m> both are equivalent to <m><var name="$r"/></m>, <m> <var name="$Ans"/> </m> is verified as the solution and the solution set is <m>\left\{ <var name="$Ans"/> \right\}</m>.</p>
                    </solution>
                </webwork>
            </exercise>
        </exercisegroup>

        <exercisegroup cols="2">
            <title>Applications</title>
            <exercise>
                <webwork source="BasicAlgebra/Geometry/SimilarTriangles10.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/Geometry/SimilarTriangles20.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/Geometry/SimilarTriangles50.pg" seed="1596"/>
            </exercise>
            <exercise>
                <webwork source="BasicAlgebra/Geometry/SimilarTriangles60.pg" seed="1601"/>
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion10.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion11.pg" />
            </exercise>

            <exercise>
                <webwork>
                    <pg-macros>
                        <macro-file>PCCmacros.pl</macro-file>
                        <macro-file>contextCurrency.pl</macro-file>
                    </pg-macros>
                    <setup>
                        <pg-code>
                            Context("Currency");
                            Context()-&gt;currency-&gt;addSymbol("dollars","dollar");
                            Context()-&gt;flags-&gt;set(trimTrailingZeros=>1);
                            $sex = list_random('male','female');
                            $name = RandomName(sex => $sex);
                            $pronoun = ($sex eq 'male') ? 'he' : 'she';
                            $w = random(10,17,1)+0.5;
                            $h = random(4,8,2);
                            $wages = $w * $h;
                            $wh = random(22,32,2);
                            $p = $w * $wh;
                            $hours = numberWord($h);
                            $whours = numberWord($wh);
                            $cAns = Currency($p);
                        </pg-code>
                    </setup>
                    <statement>
                        <p><var name="$name" /> makes <m>\$<var name="$wages" /></m> every <var name="$hours" /> hours <var name="$pronoun" /> works.  How much will <var name="$pronoun" /> make if <var name="$pronoun" /> works <var name="$whours" /> hours this week?</p>
                        <p>If <var name="$name" /> works <var name="$whours" /> hours this week, <var name="$pronoun" /> will make <var name="$cAns" width="10" />.</p>
                    </statement>
                    <solution>
                        <p>Let <m>x</m> represent how much money (in dollars) that <var name="$name" /> will make by working <var name="$whours" /> hours this week.</p>
                        <p>To solve this use a proportion, we begin by setting up a ratio where we know all the values: <m>\frac{<var name="$wages" /> \text{ dollars}}{<var name="$h"/>\text{ hours}}</m></p>
                        <p>We then set up a second ratio, using the same structure, where one value is unknown: <m>\frac{x \text{ dollars}}{<var name="$wh"/>\text{ hours}}</m> </p>
                        <p>We finish by setting the two ratios equal and solve the resulting proportion. <md>
                            <mrow>\frac{<var name="$wages" />}{<var name="$h"/>} \amp= \frac{x}{<var name="$wh"/>}</mrow>
                            <mrow>\frac{<var name="$wages" />}{<var name="$h"/>}\multiplyright{<var name="$wh"/>} \amp= \frac{x}{<var name="$wh"/>}\multiplyright{<var name="$wh"/>}</mrow>
                            <mrow> <var name="$p"/> \amp= x</mrow>
                        </md><var name="$name" /> will make <var name="$cAns" /> by working <var name="$whours" /> hours this week. </p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <pg-macros>
                        <macro-file>PCCmacros.pl</macro-file>
                        <macro-file>contextCurrency.pl</macro-file>
                    </pg-macros>
                    <setup>
                        <pg-code>
                            Context("Currency");
                            Context()-&gt;currency-&gt;addSymbol("dollars","dollar");
                            Context()-&gt;flags-&gt;set(trimTrailingZeros=>1);
                            $sex = list_random('male','female');
                            $name = RandomName(sex => $sex);
                            $pronoun = ($sex eq 'male') ? 'he' : 'she';
                            $w = random(10,17,1)+0.5;
                            $h = random(6,14,2);
                            $wages = $w * $h;
                            $wh = random(26,38,2);
                            $p = $w * $wh;
                            $hours = numberWord($h);
                            $whours = numberWord($wh);
                            $cAns = Currency($p);
                        </pg-code>
                    </setup>
                    <statement>
                        <p><var name="$name" /> makes <m>\$<var name="$wages" /></m> every <var name="$hours" /> hours <var name="$pronoun" /> works.  How much will <var name="$pronoun" /> make if <var name="$pronoun" /> works <var name="$whours" /> hours this week?</p>
                        <p>If <var name="$name" /> works <var name="$whours" /> hours this week, <var name="$pronoun" /> will make <var name="$cAns" width="10" />.</p>
                    </statement>
                    <solution>
                        <p>Let <m>x</m> represent how much money (in dollars) that <var name="$name" /> will make by working <var name="$whours" /> hours this week.</p>
                        <p>To solve this use a proportion, we begin by setting up a ratio where we know all the values: <m>\frac{<var name="$wages" /> \text{ dollars}}{<var name="$h"/>\text{ hours}}</m></p>
                        <p>We then set up a second ratio, using the same structure, where one value is unknown: <m>\frac{x \text{ dollars}}{<var name="$wh"/>\text{ hours}}</m> </p>
                        <p>We finish by setting the two ratios equal and solve the resulting proportion. <md>
                            <mrow>\frac{<var name="$wages" />}{<var name="$h"/>} \amp= \frac{x}{<var name="$wh"/>}</mrow>
                            <mrow>\frac{<var name="$wages" />}{<var name="$h"/>}\multiplyright{<var name="$wh"/>} \amp= \frac{x}{<var name="$wh"/>}\multiplyright{<var name="$wh"/>}</mrow>
                            <mrow> <var name="$p"/> \amp= x</mrow>
                        </md><var name="$name" /> will make <var name="$cAns" /> by working <var name="$whours" /> hours this week. </p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion20.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion21.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion30.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion31.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion40.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion41.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion50.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion51.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion60.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion61.pg" />
            </exercise>

            <exercise>
                <webwork>
                    <pg-macros>
                        <macro-file>PCCmacros.pl</macro-file>
                    </pg-macros>
                    <setup>
                        <pg-code>
                            $tt = 39;
                            $sp = 42;
                            $st = 9;
                            $Ans = 42*39/9;
                        </pg-code>
                    </setup>
                    <statement>
                        <p>To try to determine the health of the Rocky Mountain elk population in the Wenaha Wildlife Area, the Oregon Department of Fish and Wildlife caught, tagged, and released <m><var name="$tt" /></m> Rocky Mountain elk.  A week later, they returned and observed <m><var name="$sp" /></m> Rocky Mountain elk, <m><var name="$st" /></m> of which had tags.  Approximately how many Rocky Mountain elk are in the Wenaha Wildlife Area?</p>
                        <p>There are approximately <var name="$Ans" width="10" /> elk in the wildlife area.</p>
                    </statement>
                    <solution>
                        <p>Let <m>x</m> represent how many elk are in the Wenaha Wildlife Area.</p>
                        <p>To solve this use a proportion, we begin by setting up a ratio from the sample where we know all the values: <m>\frac{<var name="$st" /> \text{ tagged elk}}{<var name="$sp"/>\text{ total elk}}</m></p>
                        <p>We then set up a second ratio, using the same structure, where one value is unknown: <m>\frac{<var name="$tt" /> \text{ tagged elk}}{x \text{ total elk}}</m> </p>
                        <p>We finish by setting the two ratios equal and solve the resulting proportion. <md>
                            <mrow>\frac{<var name="$st" />}{<var name="$sp"/>} \amp= \frac{<var name="$tt"/>}{x}</mrow>
                            <mrow>\frac{<var name="$st" />}{<var name="$sp"/>} \multiplyright{<var name="$sp"/>\cdot x} \amp= \frac{<var name="$tt"/>}{x}\multiplyright{<var name="$sp"/>\cdot x}</mrow>
                            <mrow><var name="$st" /> \cdot x \amp= <var name="$tt"/>\cdot <var name="$sp"/></mrow>
                            <mrow>\divideunder{<var name="$st" /> \cdot x}{<var name="$st" />} \amp= \divideunder{<var name="$tt"/>\cdot <var name="$sp"/>}{<var name="$st" />}</mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md>There are approximately <m><var name="$Ans"/></m> elk in the Wenaha Wildlife Area.  </p>
                    </solution>
                </webwork>
            </exercise>
            <exercise>
                <webwork>
                    <pg-macros>
                        <macro-file>PCCmacros.pl</macro-file>
                    </pg-macros>
                    <setup>
                        <pg-code>
                            $tt = 28;
                            $sp = 63;
                            $st = 18;
                            $Ans = 63*28/18;
                        </pg-code>
                    </setup>
                    <statement>
                        <p>To try to determine the health of the black-tailed deer population in the Jewell Meadow Wildlife Area, the Oregon Department of Fish and Wildlife caught, tagged, and released <m><var name="$tt" /></m> black-tailed deer.  A week later, they returned and observed <m><var name="$sp" /></m> black-tailed deer, <m><var name="$st" /></m> of which had tags.  Approximately how many black-tailed deer are in the Jewell Meadow Wildlife Area?</p>
                        <p>There are approximately <var name="$Ans" width="10" /> deer in the wildlife area.</p>
                    </statement>
                    <solution>
                        <p>Let <m>x</m> represent how many black-tailed deer are in the Jewell Meadow Wildlife Area.</p>
                        <p>To solve this use a proportion, we begin by setting up a ratio from the sample where we know all the values: <m>\frac{<var name="$st" /> \text{ tagged deer}}{<var name="$sp"/>\text{ total deer}}</m></p>
                        <p>We then set up a second ratio, using the same structure, where one value is unknown: <m>\frac{<var name="$tt" /> \text{ tagged deer}}{x \text{ total deer}}</m> </p>
                        <p>We finish by setting the two ratios equal and solve the resulting proportion. <md>
                            <mrow>\frac{<var name="$st" />}{<var name="$sp"/>} \amp= \frac{<var name="$tt"/>}{x}</mrow>
                            <mrow>\frac{<var name="$st" />}{<var name="$sp"/>} \multiplyright{<var name="$sp"/>\cdot x} \amp= \frac{<var name="$tt"/>}{x}\multiplyright{<var name="$sp"/>\cdot x}</mrow>
                            <mrow><var name="$st" /> \cdot x \amp= <var name="$tt"/>\cdot <var name="$sp"/></mrow>
                            <mrow>\divideunder{<var name="$st" /> \cdot x}{<var name="$st" />} \amp= \divideunder{<var name="$tt"/>\cdot <var name="$sp"/>}{<var name="$st" />}</mrow>
                            <mrow>x \amp= <var name="$Ans"/></mrow>
                        </md>There are approximately <m><var name="$Ans"/></m> black-tailed deer in the Jewell Meadow Wildlife Area.  </p>
                    </solution>
                </webwork>
            </exercise>

            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion70.pg" />
            </exercise>
            <exercise>
                <webwork source="BasicMath/Proportion/MTH20Proportion71.pg" />
            </exercise>
        </exercisegroup>

        <exercisegroup>
            <title>Challenge</title>

            <exercise><!-- Kara's challenge problem -->
                <webwork>
                    <setup>
                        <pg-code>
                            $girl = random(2,6,2);
                            $boy = random(3,7,2);
                            $factor = random(5,9,1);
                            $totalgirls = Real($girl*$factor);
                            $total = Real(($girl+$boy)*$factor);
                        </pg-code>
                    </setup>
                    <statement>
                        <p>The ratio of girls to boys in a preschool is <var name="$girl"/> to <var name="$boy"/>.  If there are <var name="$total"/> kids in the school, how many girls are there in the preschool?</p>
                        <p><var name="$totalgirls" width="10"/></p>
                    </statement>
                    <solution>
                        <p> Let <m>x</m> be the number of girls in the preschool.  We can represent this situation with the following proportion:</p>
                        <p><m>\frac{<var name="$girl"/>}{<var name="$girl"/>+<var name="$boy"/>} = \frac{x}{<var name="$total"/>}</m>.  Solve this equation for <m>x</m>.</p>
                    </solution>
                </webwork>
            </exercise>
        </exercisegroup>
    </exercises>
</section>
