<?xml version="1.0" encoding="UTF-8" ?>
<!-- *********************************************************************-->
<!-- Copyright 2016-2019                                                  -->
<!-- Portland Community College                                           -->
<!--                                                                      -->
<!-- This file is part of Open Resources for Community College Algebra    -->
<!-- (ORCCA).                                                             -->
<!--                                                                      -->
<!-- Open Resources for Community College Algebra is licensed under a     -->
<!-- Creative Commons Attribution 4.0 International License.              -->
<!--                                                                      -->
<!-- Under this license, any user of this textbook or the textbook        -->
<!-- contents must provide proper attribution as follows.                 -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- include in every digital format page view (including but not         -->
<!-- limited to EPUB, PDF, and HTML) and on every physical printed page   -->
<!-- the following attribution:                                           -->
<!--                                                                      -->
<!-- Original source material, products with readable and accessible      -->
<!-- math content, and other information freely available at              -->
<!-- pcc.edu/orcca.                                                       -->
<!--                                                                      -->
<!-- If you redistribute all or part of this textbook, then you must      -->
<!-- reproduce any math content (such as math expressions and equations)  -->
<!-- in a readable manner, and offer math content in at least one web     -->
<!-- accessible manner.                                                   -->
<!--                                                                      -->
<!-- The Portland Community College name, Portland Community College      -->
<!-- logo, ORCCA name, Open Resources for Community College Algebra name, -->
<!-- ORCCA logo, and front and back cover designs are not subject to the  -->
<!-- Creative Commons license and may not be reproduced without the       -->
<!-- prior and express written consent of Portland Community College.     -->
<!--                                                                      -->
<!-- For questions regarding this license, please contact                 -->
<!-- orcca-group@pcc.edu.                                                 -->
<!-- *********************************************************************-->
<section xml:id="section-introduction-to-exponent-properties">
  <title>Introduction to Exponent Rules</title>
  <objectives>
    <title>PCC Course Content and Outcome Guide</title>
    <ul>
      <li>
        <xref ref="ccog-develop-exponent-rules" text="local">MTH 65 CCOG</xref>
      </li>
    </ul>
  </objectives>

  <introduction>
    <p>
      In this section, we look at some properties of exponents that we can use when simplifying
      expressions that have multiplication and exponents.
    </p>

    <figure>
      <caption>Alternative Video Lesson</caption>
      <video youtubeplaylist="PLmuZCPhWPt-frT2IoPCYbXFHCp1OgGRzi"/>
    </figure>
  </introduction>

  <subsection>
    <title>Exponent Basics</title>
    <p>
      Before we discuss any exponent rules, let's remind ourselves about exponent fundamentals.
      When working with expressions with exponents, we have the following vocabulary:
      <me>
        \text{base}^{\text{exponent}} = \text{power}
      </me>
      For example, when we calculate <m>8^{2} = 64</m>, the <term>base</term> is <m>8</m>, the                <idx><h>exponent</h><h>base</h></idx>
      <term>exponent</term> is <m>2</m>, and the expression <m>8^{2}</m> is called the 2nd                    <idx><h>base</h></idx>
      <term>power</term> of <m>8</m>.                                                                         <idx><h>exponent</h></idx>
                                                                                                              <idx><h>exponent</h><h>power</h></idx>
                                                                                                              <idx><h>power</h></idx>
    </p>

    <p>
      Exponents indicate repeated multiplication. When the exponent is a positive integer, the power
      can be rewritten as repeated multiplication of the base. For example, the <m>4</m>th power of
      <m>3</m> can be written as <m>4</m> factors of <m>3</m> like so:
      <me>
        3^{4} = 3 \cdot 3 \cdot 3 \cdot 3
      </me>
    </p>
  </subsection>

  <subsection>
    <title>Exponent Rules</title>
    <paragraphs>
      <title>Product Rule</title>
      <p>
        If we write out <m>3^5\cdot 3^2</m> without using exponents,
        we'd have:
        <me>
          3^5 \cdot 3^2 = \left(3 \cdot 3\cdot 3\cdot 3\cdot 3\right) \cdot \left(3 \cdot 3\right)
        </me>
        If we then count how many <m>3</m>s are being multiplied together,
        we find we have <m>5+2=7</m>,
        a total of seven <m>3</m>s.
        So <m>3^5\cdot 3^2</m> simplifies like this:
        <md>
          <mrow>3^5\cdot 3^2 \amp= 3^{5+2}</mrow>
          <mrow>\amp= 3^7</mrow>
        </md>
      </p>
    </paragraphs>

    <example>
      <statement>
        <p>
          Simplify <m>x^2\cdot x^3</m>.
        </p>

        <p>
          To simplify <m>x^2\cdot x^3</m>,
          we write this out in its expanded form,
          as a product of <m>x</m>'s, we have
          <md>
            <mrow>x^2\cdot x^3 \amp=(x\cdot x)(x \cdot x \cdot x)</mrow>
            <mrow>\amp=x\cdot x\cdot x \cdot x \cdot x</mrow>
            <mrow>\amp=x^5</mrow>
          </md>
          Note that we obtained the exponent of <m>5</m> by adding <m>2</m> and <m>3</m>.
        </p>
      </statement>
    </example>

    <p>
      This demonstrates our first exponent rule, the <term>Product Rule</term>: when multiplying two          <idx><h>exponent</h><h>product rule</h></idx>
      expressions that have the same base, we can simplify the product by adding the exponents.
      <men xml:id="equation-product-rule">
        x^m \cdot x^n = x^{m+n}
      </men>
    </p>

    <exercise>
      <webwork source="Contrib/PCC/BasicAlgebra/Exponents/exponentsMultiplication0.pg" />
    </exercise>

    <p>
      Recall that <m>x=x^1</m>. It helps to remember this when multiplying certain expressions together.
    </p>
    <example>
      <statement>
        <p>
          Multiply <m>x(x^3+2)</m> by using the distributive property.
        </p>

        <p>
          According to the distributive property,
          <me>
            x(x^3+2)=x\cdot x^3 + x\cdot2
          </me>
          How can we simplify that term <m>x\cdot x^3</m>?
          It's really the same as <m>x^1\cdot x^3</m>,
          so according to the Product Rule, it is <m>x^4</m>.
          So we have:
          <md>
            <mrow>x(x^3+2)\amp=x\cdot x^3 + x\cdot2</mrow>
            <mrow>\amp=x^4+2x</mrow>
          </md>
        </p>
      </statement>
    </example>

    <paragraphs>
      <title>Power to a Power Rule</title>
      <p>
        If we write out <m>\left(3^5\right)^2</m> without using exponents,
        we'd have <m>3^5</m> multiplied by itself:
        <md>
          <mrow>\left(3^5\right)^2 \amp= \left(3^5\right)\cdot \left(3^5\right)</mrow>
          <mrow>\amp= \left(3\cdot 3\cdot 3\cdot 3 \cdot 3 \right) \cdot \left(3 \cdot 3\cdot 3\cdot 3\cdot 3\right)</mrow>
        </md>
        If we again count how many <m>3</m>s are being multiplied,
        we have a total of two groups each with five <m>3</m>s.
        So we'd have <m>2\cdot 5=10</m> instances of a <m>3</m>.
        So <m>\left(3^5\right)^2</m> simplifies like this:
        <md>
          <mrow>\left(3^5\right)^2 \amp= 3^{2\cdot 5}</mrow>
          <mrow>\amp= 3^{10}</mrow>
        </md>
      </p>
    </paragraphs>

    <example>
      <statement>
        <p>
          Simplify <m>\left(x^2\right)^3</m>.
        </p>

        <p>
          To simplify <m>\left(x^2\right)^3</m>,
          we write this out in its expanded form,
          as a product of <m>x</m>'s, we have
          <md>
            <mrow>\left(x^2\right)^3 \amp=\left(x^2\right) \cdot \left(x^2\right)\cdot\left(x^2\right)</mrow>
            <mrow>\amp=(x \cdot x)\cdot (x \cdot x)\cdot (x \cdot x)</mrow>
            <mrow>\amp=x^6</mrow>
          </md>
          Note that we obtained the exponent of <m>6</m> by multiplying <m>2</m> and <m>3</m>.
        </p>
      </statement>
    </example>

    <p>
      This demonstrates our second exponent rule, the <term>Power to a Power Rule</term>: when a              <idx><h>exponent</h><h>power to a power rule</h></idx>
      base is raised to an exponent and that expression is raised to another exponent, we multiply
      the exponents.
      <me>
        \left(x^m\right)^n = x^{m \cdot n}
      </me>
    </p>

    <exercise>
      <webwork source="Contrib/PCC/BasicAlgebra/Exponents/exponentsMultiplication20.pg" />
    </exercise>

    <paragraphs>
      <title>Product to a Power Rule</title>
      <p>
        The third exponent rule deals with having multiplication inside a set of parentheses and an exponent outside the parentheses.
        If we write out <m>\left(3t\right)^5</m> without using an exponent,
        we'd have <m>3t</m> multiplied by itself five times:
        <me>
          (3t)^5= (3t)(3t)(3t)(3t)(3t)
        </me>
        Keeping in mind that there is multiplication between every <m>3</m> and <m>t</m>,
        and multiplication between all of the parentheses pairs,
        we can reorder and regroup the factors:
        <md>
          <mrow>\left(3t\right)^5 \amp= (3\cdot t)\cdot (3\cdot t)\cdot (3\cdot t)\cdot (3\cdot t)\cdot (3\cdot t)</mrow>
          <mrow>\amp= \left(3\cdot 3\cdot 3\cdot 3\cdot 3 \right) \cdot \left(t \cdot t \cdot t \cdot t \cdot t\right)</mrow>
          <mrow>\amp= 3^5 t^5</mrow>
        </md>
        We could leave it written this way if <m>3^5</m> feels especially large.
        But if you are able to evaluate <m>3^5=243</m>,
        then perhaps a better final version of this expression is <m>243t^5</m>.
      </p>
      <p>
        We essentially applied the outer exponent to each factor inside the parentheses.
        It is important to see how the exponent <m>5</m> applied to <em>both</em> the <m>3</m> <em>and</em> the <m>t</m>,
        not just to the <m>t</m>.
      </p>
    </paragraphs>

    <example>
      <statement>
        <p>
          Simplify <m>(xy)^5</m>.
        </p>

        <p>
          To simplify <m>(xy)^5</m>,
          we write this out in its expanded form,
          as a product of <m>x</m>'s and <m>y</m>'s, we have
          <md>
            <mrow>(xy)^5 \amp=(x \cdot y) \cdot (x \cdot y) \cdot (x \cdot y) \cdot (x \cdot y) \cdot (x \cdot y)</mrow>
            <mrow>\amp=(x \cdot x \cdot x \cdot x \cdot x) \cdot (y \cdot y \cdot y \cdot y \cdot y)</mrow>
            <mrow>\amp=x^5 y^5</mrow>
          </md>
          Note that the exponent on <m>xy</m> can simply be applied to both <m>x</m> and <m>y</m>.
        </p>
      </statement>
    </example>

    <p>
      This demonstrates our third exponent rule, the <term>Product to a Power Rule</term>: when a             <idx><h>exponent</h><h>product to a power rule</h></idx>
      product is raised to an exponent, we can apply the exponent to each factor in the product.
      <me>
        \left(x\cdot y\right)^n = x^{n}\cdot y^{n}
      </me>
    </p>

    <exercise>
      <webwork source="Contrib/PCC/BasicAlgebra/Exponents/exponentsMultiplication23.pg" />
    </exercise>

    <list xml:id="list-properties-of-exponents">
      <title>Summary of the Rules of Exponents for Multiplication</title>                                     <idx><h>exponent</h><h>introductory rules</h></idx>
      <introduction>
        <p>
          If <m>b</m> and <m>c</m> are real numbers, and <m>m</m> and <m>n</m> are positive
          integers, then we have the following rules:
        </p>
      </introduction>

      <dl>
        <li xml:id="item-exponent-product-rule">
          <title>Product Rule</title>
          <p>
            <m>b^{m} \cdot b^{n} = b^{m+n}</m>
          </p>
        </li>

        <li xml:id="item-exponent-power-to-a-power-rule">
          <title>Power to a Power Rule</title>
          <p>
            <m>(b^{m})^{n} = b^{m\cdot n}</m>
          </p>
        </li>

        <li xml:id="item-exponent-product-to-a-power-rule">
          <title>Product to a Power Rule</title>
          <p>
            <m>(bc)^{m} = b^{m} \cdot c^{m}</m>
          </p>
        </li>
      </dl>
    </list>

    <p>
      Many examples will make use of more than one exponent rule. In deciding which exponent rule to
      work with first, it's important to remember that the order of operations still applies.
    </p>

    <example>
      <statement>
        <p>
          Simplify the following expressions.

          <ol marker="a." cols="2">
            <li>
              <p>
                <m>\left(3^7r^5\right)^4</m>
              </p>
            </li>

            <li>
              <p>
                <m>\left(t^3\right)^2\cdot \left(t^4\right)^5</m>
              </p>
            </li>
          </ol>
        </p>
      </statement>
      <solution>
        <p>
          <ol marker="a.">
            <li>
              <p>
                Since we cannot simplify anything inside the parentheses,
                we'll begin simplifying this expression using the <xref ref="item-exponent-product-to-a-power-rule" text="title" />.
                We'll apply the outer exponent of 4 to each factor inside the parentheses.
                Then we'll use the <xref ref="item-exponent-power-to-a-power-rule" text="title" /> to finish the simplification process.
                <md>
                  <mrow>\left(3^7r^5\right)^4 \amp= \left(3^7\right)^4 \cdot \left(r^5\right)^4</mrow>
                  <mrow>\amp= 3^{7\cdot4} \cdot r^{5\cdot 4}</mrow>
                  <mrow>\amp= 3^{28}r^{20}</mrow>
                </md>
                Note that <m>3^{28}</m> is too large to actually compute, even with a calculator,
                so we leave it written as <m>3^{28}</m>.
              </p>
            </li>

            <li>
              <p>
                According to the order of operations,
                we should first simplify any exponents before carrying out any multiplication.
                Therefore, we'll begin simplifying this by applying the <xref ref="item-exponent-power-to-a-power-rule" text="title" /> and then finish using the <xref ref="item-exponent-product-rule" text="title" />.
                <md>
                  <mrow>\left(t^3\right)^2\cdot \left(t^4\right)^5 \amp= t^{3\cdot2}\cdot t^{4\cdot5}</mrow>
                  <mrow>\amp= t^6 \cdot t^{20}</mrow>
                  <mrow>\amp= t^{6+20}</mrow>
                  <mrow>\amp= t^{26}</mrow>
                </md>
              </p>
            </li>
          </ol>
        </p>
      </solution>
    </example>

    <remark>
      <p>
        We cannot simplify an expression like <m>x^2y^3</m> using the <xref ref="item-exponent-product-rule" text="title" />,
        as the factors <m>x^2</m> and <m>y^3</m> do not have the same base.
      </p>
    </remark>
  </subsection>

  <reading-questions>
    <exercise>
      <statement>
        <p>
          How many exponent rules are discussed in this section? Write an example of each rule in action.
        </p>
      </statement>
    </exercise>
    <exercise>
      <statement>
        <p>
          The order of operations say that operations inside parentheses should get the highest priority.
          But with <m>(5x)^3</m>, you cannot actually do anything with the <m>5</m> and the <m>x</m>.
          Which exponent rule allows you to sidestep the order of operations and still simplify this expression a little?
        </p>
      </statement>
    </exercise>
  </reading-questions>

  <exercises>
    <subexercises>
      <title>Review and Warmup</title>
      <exercisegroup cols="3">
        <introduction>
          <p>
            Evaluate each power expression.
          </p>
        </introduction>
        <exercise label="evaluate-integer-power">
          <webwork xml:id="webwork-evaluate-integer-power">
            <pg-code>
              @a = (random(2,5), random(-5,-2), random(-5,-2));
              $a[3] = $a[0];
              @n = (random(2,5), list_random(2,4), list_random(3,5), list_random(2,4));
              @answer = map {Real($a[$_]**$n[$_])} (0..3);
              $answer[3] = -$answer[3];
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$a[0]"/>^<var name="$n[0]"/></m>
                </p>
                <p>
                  <var name="$answer[0]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>(<var name="$a[1]"/>)^<var name="$n[1]"/></m>
                </p>
                <p>
                  <var name="$answer[1]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>(<var name="$a[2]"/>)^<var name="$n[2]"/></m>
                </p>
                <p>
                  <var name="$answer[2]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>-<var name="$a[3]"/>^<var name="$n[3]"/></m>
                </p>
                <p>
                  <var name="$answer[3]" width="6"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="evaluate-integer-power-copy">
          <webwork copy="webwork-evaluate-integer-power"/>
        </exercise>
        <exercise label="evaluate-fraction-power">
          <webwork xml:id="webwork-evaluate-fraction-power">
            <pg-code>
              Context("Fraction");
              $a[0] = Fraction(random_coprime([1 .. 5], [2 .. 5]));
              $a[1] = Fraction(random_coprime([-5 .. -1], [2 .. 5]));
              $a[2] = Fraction(random_coprime([-5 .. -1], [2 .. 5]));
              $a[3] = $a[0];
              @n = (random(2,5), list_random(2,4), list_random(3,5), list_random(2,4));
              @answer = map {$a[$_]**$n[$_]} (0..3);
              $answer[3] = -$answer[3];
           </pg-code>
            <task>
              <statement>
                <p>
                  <m>\left(<var name="$a[0]"/>\right)^<var name="$n[0]"/></m>
                </p>
                <p>
                  <var name="$answer[0]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>\left(<var name="$a[1]"/>\right)^<var name="$n[1]"/></m>
                </p>
                <p>
                  <var name="$answer[1]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>\left(<var name="$a[2]"/>\right)^<var name="$n[2]"/></m>
                </p>
                <p>
                  <var name="$answer[2]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>-\left(<var name="$a[3]"/>\right)^<var name="$n[3]"/></m>
                </p>
                <p>
                  <var name="$answer[3]" width="6"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="evaluate-fraction-power-copy">
          <webwork copy="webwork-evaluate-fraction-power"/>
        </exercise>
        <exercise label="evaluate-decimal-power">
          <webwork xml:id="webwork-evaluate-decimal-power">
            <pg-code>
              @a = (random(2,5), random(-5,-2), random(-5,-2));
              $a[3] = $a[0];
              @a = map {$_ + random(0.1,0.9,0.1)} @a;
              @n = (random(2,5), list_random(2,4), list_random(3,5), list_random(2,4));
              @answer = map {Real($a[$_]**$n[$_])} (0..3);
              $answer[3] = -$answer[3];
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$a[0]"/>^<var name="$n[0]"/></m>
                </p>
                <p>
                  <var name="$answer[0]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>(<var name="$a[1]"/>)^<var name="$n[1]"/></m>
                </p>
                <p>
                  <var name="$answer[1]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>(<var name="$a[2]"/>)^<var name="$n[2]"/></m>
                </p>
                <p>
                  <var name="$answer[2]" width="6"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>-<var name="$a[3]"/>^<var name="$n[3]"/></m>
                </p>
                <p>
                  <var name="$answer[3]" width="6"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="evaluate-decimal-power-copy">
          <webwork copy="webwork-evaluate-decimal-power"/>
        </exercise>
      </exercisegroup>
    </subexercises>

    <subexercises>
      <title>Skills Practice</title>
      <exercisegroup cols="3">
        <introduction>
          <p>
            Use the properties of exponents to simplify the expression.
          </p>
        </introduction>
        <exercise label="exponent-number-times-power">
          <webwork xml:id="webwork-exponent-number-times-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              Context("Form");
              $b=random(2,9,1);
              $m=1;
              $n=random(2,10,1);
              $total = $m+$n;
              $given = Formula("$b * $b^($n)");
              $ans = Formula("$b^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-number-times-power-copy">
          <webwork copy="webwork-exponent-number-times-power"/>
        </exercise>
        <exercise label="exponent-number-power-times-power">
          <webwork xml:id="webwork-exponent-number-power-times-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              Context("Form");
              $b=random(2,9,1);
              ($m, $n) = random_subset(2, 2..10);
              $total = $m+$n;
              $given = Formula("$b^($m) * $b^($n)");
              $ans = Formula("$b^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-number-power-times-power-copy">
          <webwork copy="webwork-exponent-number-power-times-power"/>
        </exercise>
        <exercise label="exponent-variable-power-times-power">
          <webwork xml:id="webwork-exponent-variable-power-times-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              ($m, $n) = random_subset(2, 2..10);
              $total = $m+$n;
              $given = Formula("$x^($m) * $x^($n)");
              $ans = Formula("$x^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
            <solution>
              <p>
                We have two powers with the same base. So we can keep that base, and add the
                exponents: <m><var name="$ans"/></m>.
              </p>
            </solution>
          </webwork>
        </exercise>
        <exercise label="exponent-variable-power-times-power-copy">
          <webwork copy="webwork-exponent-variable-power-times-power"/>
        </exercise>
        <exercise label="exponent-three-powers">
          <webwork xml:id="webwork-exponent-three-powers">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              ($m, $n, $k) = random_subset(3, 2..10);
              $total = $m+$n+$k;
              $given = Formula("$x^($m) * $x^($n) * $x^($k)");
              $ans = Formula("$x^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-three-powers-copy">
          <webwork copy="webwork-exponent-three-powers"/>
        </exercise>
        <exercise label="exponent-power-to-power">
          <webwork xml:id="webwork-exponent-power-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              Context("Form");
              $b=random(2,9,1);
              ($m, $n) = random_subset(2, 2..10);
              $total = $m*$n;
              $given = Formula("($b^($n))^$m");
              $ans = Formula("$b^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-power-to-power-copy">
          <webwork copy="webwork-exponent-power-to-power"/>
        </exercise>
        <exercise label="exponent-variable-power-to-power">
          <webwork xml:id="webwork-exponent-variable-power-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              ($m, $n) = random_subset(2, 2..10);
              $total = $m*$n;
              $given = Formula("($x^($n))^$m");
              $ans = Formula("$x^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
            <solution>
              <p>
                This expression raises <m><var name="$x"/></m> to an exponent, and then raises the
                result to another exponent. In this situation we can multiply the two exponents and
                get: <m><var name="$ans"/></m>.
              </p>
            </solution>
          </webwork>
        </exercise>
        <exercise label="exponent-variable-power-to-power-copy">
          <webwork copy="webwork-exponent-variable-power-to-power"/>
        </exercise>
        <exercise label="exponent-product-to-power">
          <webwork xml:id="webwork-exponent-product-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $b = random(2,5);
              $m = random(2,4);
              $bm = $b**$m;
              $given = Formula("($b $x)^$m");
              $ans = Formula("$bm $x^$m");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-to-power-copy">
          <webwork copy="webwork-exponent-product-to-power"/>
        </exercise>
        <exercise label="exponent-product-to-power-multivar">
          <webwork xml:id="webwork-exponent-product-to-power-multivar">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              ($x, $y) = random_subset(2, 'a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real', $y => 'Real');
              $b = random(2,5);
              $m = random(2,4);
              $bm = $b**$m;
              $given = Formula("($b $x $y)^$m");
              $ans = Formula("$bm $x^$m $y^$m");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-to-power-multivar-copy">
          <webwork copy="webwork-exponent-product-to-power-multivar"/>
        </exercise>
        <exercise label="exponent-product-with-power-to-power">
          <webwork xml:id="webwork-exponent-product-with-power-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $b = random(2,5);
              $m = random(2,4);
              $n = random(2,9);
              $bm = $b**$m;
              $total = $m*$n;
              $given = Formula("($b $x^$n)^$m");
              $ans = Formula("$bm $x^$total");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
            <solution>
              <p>
                This expression is a product of two things (<m><var name="$b"/></m> and
                <m><var name="$x"/>^{<var name="$n"/>}</m>) that is then raised to a power. We can
                raise each of the two factors to that outer power instead:
                <me><var name="$b"/>^{<var name="$m"/>}\left(<var name="$x"/>^{<var name="$n"/>}\right)^{<var name="$m"/>}</me>
                and then use the power-to-power property of exponents to simplify it more:
                <m><var name="$ans"/></m>.
              </p>
            </solution>
          </webwork>
        </exercise>
        <exercise label="exponent-product-with-power-to-power-copy">
          <webwork copy="webwork-exponent-product-with-power-to-power"/>
        </exercise>
        <exercise label="exponent-product-with-powers">
          <webwork xml:id="webwork-exponent-product-with-powers">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @b = random_subset(2, 2 .. 9);
              @m = random_subset(2, 2 .. 9);
              $total = $m + $n;
              $b = $b[0] * $b[1];
              $given = Formula("($b[0] $x^$m[0])($b[1] $x^$m[1])");
              $ans = Formula("$b $x^$total");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-with-powers-copy">
          <webwork copy="webwork-exponent-product-with-powers"/>
        </exercise>
        <exercise label="exponent-negative-product-with-power-to-power">
          <webwork xml:id="webwork-exponent-negative-product-with-power-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $b = random(-5,-2);
              $m = random(2,4);
              $n = random(2,9);
              $bm = $b**$m;
              $total = $m*$n;
              $given = Formula("($b $x^$n)^$m");
              $ans = Formula("$bm $x^$total");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-negative-product-with-power-to-power-copy">
          <webwork copy="webwork-exponent-negative-product-with-power-to-power"/>
        </exercise>
        <exercise label="exponent-three-product-powers">
          <webwork xml:id="webwork-exponent-three-product-powers">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @b = random_subset(3, 2 .. 9);
              ($m, $n, $k) = random_subset(3, 2..10);
              $total = $m+$n+$k;
              $given = Formula("($b[0]$x)^($m) * ($b[1]$x)^($n) * ($b[2]$x)^($k)");
              $b = $b[0] * $b[1] * $b[2];
              $ans = Formula("$b $x^($total)");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-three-product-powers-copy">
          <webwork copy="webwork-exponent-three-product-powers"/>
        </exercise>
        <exercise label="exponent-product-with-power-to-power-negated">
          <webwork xml:id="webwork-exponent-product-with-power-to-power-negated">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $b = random(2,5);
              $m = random(2,4);
              $n = random(2,9);
              $bm = $b**$m;
              $total = $m*$n;
              $given = Formula("-($b $x^$n)^$m");
              $ans = Formula("-$bm $x^$total");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-with-power-to-power-negated-copy">
          <webwork copy="webwork-exponent-product-with-power-to-power-negated"/>
        </exercise>
        <exercise label="exponent-quotient-with-powers">
          <webwork xml:id="webwork-exponent-quotient-with-powers">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @b = random_subset(2, 2 .. 9);
              @m = random_subset(2, 2 .. 9);
              $total = $m + $n;
              $b = $b[0] * $b[1];
              $given = Formula("($x^$m[0]/$b[0])($x^$m[1]/$b[1])");
              $ans = Formula("$x^$total/$b");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-quotient-with-powers-copy">
          <webwork copy="webwork-exponent-quotient-with-powers"/>
        </exercise>
        <exercise label="exponent-quotient-to-power">
          <webwork xml:id="webwork-exponent-quotient-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $b = random(2 .. 5);
              @m = random_subset(2, 2 .. 9);
              $total = $m * $n;
              $bm = $b ** $m[1];
              $given = Formula("($x^$m[0]/$b)^$m[1]");
              $ans = Formula("$x^$total/$bm");
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="6"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-quotient-to-power-copy">
          <webwork copy="webwork-exponent-quotient-to-power"/>
        </exercise>
      </exercisegroup>

      <exercisegroup cols="2">
        <introduction>
          <p>
            Use the properties of exponents together with the distributive property to write the
            expression in a new simplified way where there are no grouping symbols.
          </p>
        </introduction>
        <exercise label="exponent-distribute-monomial-linear">
          <webwork xml:id="webwork-exponent-distribute-monomial-linear">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              ($a, $b, $c) = random_subset(3, -10 .. -2, 2..10);
              $b = abs($b);
              $given = Formula("$a $x * ($b $x + $c)")->reduce;
              $ans = Formula("$ab $x^2 + $ac $x")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-distribute-monomial-linear-copy">
          <webwork copy="webwork-exponent-distribute-monomial-linear"/>
        </exercise>
        <exercise label="exponent-distribute-power-linear">
          <webwork xml:id="webwork-exponent-distribute-power-linear">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $m = random(2,8);
              $total = $m+1;
              ($a, $b, $c) = random_subset(3, -10 .. -2, 2..10);
              $b = abs($b);
              $given = Formula("$a $x^$m * ($b $x + $c)")->reduce;
              $ans = Formula("$ab $x^$total + $ac $x^$m")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-distribute-power-linear-copy">
          <webwork copy="webwork-exponent-distribute-power-linear"/>
        </exercise>
        <exercise label="exponent-distribute-power-binomial">
          <webwork xml:id="webwork-exponent-distribute-power-binomial">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = random_subset(3, 2 .. 9);
              @n = ($m[0] + $m[1], $m[0] + $m[2]);
              ($a, $b, $c) = random_subset(3, -10 .. -2, 2..10);
              $b = abs($b);
              $given = Formula("$a $x^$m[0] * ($b $x^$m[1] + $c $x^$m[2])")->reduce;
              $ans = Formula("$ab $x^$n[0] + $ac $x^$n[1]")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-distribute-power-binomial-copy">
          <webwork copy="webwork-exponent-distribute-power-binomial"/>
        </exercise>
      </exercisegroup>

      <exercisegroup cols="2">
        <title>Comparisons</title>
        <introduction>
          <p>
            Simplify each expression, taking note of the different behavior when adding versus
            multiplying. If it's not possible to simplify an expression more than it already is,
            then the answer should just be the same given expression.
          </p>
        </introduction>
        <exercise label="exponent-compare-add-multiply-var">
          <webwork xml:id="webwork-exponent-compare-add-multiply-var">
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $g[0] = Formula("$x + $x");
              $g[1] = Formula("$x * $x");
              $ans[0] = Formula("2$x");
              $ans[1] = Formula("$x^2");
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$g[0]"/></m>
                </p>
                <p>
                  <var name="$ans[0]" width="16"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m><var name="$x"/>\cdot <var name="$x"/></m>
                </p>
                <p>
                  <var name="$ans[1]" width="16"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="exponent-compare-add-multiply-var-copy">
          <webwork copy="webwork-exponent-compare-add-multiply-var"/>
        </exercise>
        <exercise label="exponent-compare-add-multiply-powers">
          <webwork xml:id="webwork-exponent-compare-add-multiply-powers">
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $m = random(2,9);
              $m2 = 2*$m;
              $g[0] = Formula("$x^$m+ $x^$m");
              $g[1] = Formula("$x^$m * $x^$m");
              $ans[0] = Formula("2$x^$m");
              $ans[1] = Formula("$x^($m2)");
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$g[0]"/></m>
                </p>
                <p>
                  <var name="$ans[0]" width="16"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m><var name="$x"/>^<var name="$m"/>\cdot <var name="$x"/>^<var name="$m"/></m>
                </p>
                <p>
                  <var name="$ans[1]" width="16"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="exponent-compare-add-multiply-powers-copy">
          <webwork copy="webwork-exponent-compare-add-multiply-powers"/>
        </exercise>
        <exercise label="exponent-compare-add-multiply-different-powers">
          <webwork xml:id="webwork-exponent-compare-add-multiply-different-powers">
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = random_subset(2, 2 .. 9);
              $total = $m[0] + $m[1];
              $g[0] = Formula("$x^$m[0] + $x^$m[1]");
              $g[1] = Formula("$x^$m[0] * $x^$m[1]");
              $ans[0] = $g[0];
              $ans[1] = Formula("$x^($total)");
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$g[0]"/></m>
                </p>
                <p>
                  <var name="$ans[0]" width="16"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m><var name="$x"/>^<var name="$m[0]"/>\cdot <var name="$x"/>^<var name="$m[1]"/></m>
                </p>
                <p>
                  <var name="$ans[1]" width="16"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="exponent-compare-add-multiply-different-powers-copy">
          <webwork copy="webwork-exponent-compare-add-multiply-different-powers"/>
        </exercise>
        <exercise label="exponent-compare-add-multiply-three-powers">
          <webwork xml:id="webwork-exponent-compare-add-multiply-three-powers">
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = random_subset(2, 2 .. 9);
              $m[2] = $m[0];
              $ans[0] = Formula("2 $x^$m[0] + $x^$m[1]");
              @m = random_subset(3, @m);
              $total = $m[0] + $m[1] + $m[2];
              $g[0] = Formula("$x^$m[0] + $x^$m[1] + $x^$m[2]");
              $g[1] = Formula("$x^$m[0] * $x^$m[1] * $x^$m[2]");
              $ans[1] = Formula("$x^($total)");
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$g[0]"/></m>
                </p>
                <p>
                  <var name="$ans[0]" width="16"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m><var name="$x"/>^<var name="$m[0]"/>\cdot <var name="$x"/>^<var name="$m[1]"/>\cdot <var name="$x"/>^<var name="$m[2]"/></m>
                </p>
                <p>
                  <var name="$ans[1]" width="16"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="exponent-compare-add-multiply-three-powers-copy">
          <webwork copy="webwork-exponent-compare-add-multiply-three-powers"/>
        </exercise>
        <exercise label="exponent-compare-add-multiply-powers-coeffs">
          <webwork xml:id="webwork-exponent-compare-add-multiply-powers-coeffs">
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              $m = random(2,9);
              $m2 = 2*$m;
              @c = random_subset(2 .. 9);
              $c2 = $c[0] + $c[1];
              $c22 = $c[0]*$c[1];
              $g[0] = Formula("$c[0] $x^$m + $c[1] $x^$m");
              $g[1] = Formula("($c[0]$x^$m) * ($c[1] $x^$m)");
              $ans[0] = Formula("$c2 $x^$m");
              $ans[1] = Formula("$c22 $x^($m2)");
            </pg-code>
            <task>
              <statement>
                <p>
                  <m><var name="$g[0]"/></m>
                </p>
                <p>
                  <var name="$ans[0]" width="16"/>
                </p>
              </statement>
            </task>
            <task>
              <statement>
                <p>
                  <m>\left(<var name="$c[0]"/><var name="$x"/>^<var name="$m"/>\right)\cdot\left(<var name="$c[1]"/><var name="$x"/>^<var name="$m"/>\right)</m>
                </p>
                <p>
                  <var name="$ans[1]" width="16"/>
                </p>
              </statement>
            </task>
          </webwork>
        </exercise>
        <exercise label="exponent-compare-add-multiply-powers-coeffs-copy">
          <webwork copy="webwork-exponent-compare-add-multiply-powers-coeffs"/>
        </exercise>
      </exercisegroup>
      <exercisegroup cols="2">
        <title>Simplifying Expressions</title>
        <introduction>
          <p>
            Simplify the given expression.
          </p>
        </introduction>
        <exercise label="exponent-product-power-to-power">
          <webwork xml:id="webwork-exponent-product-power-to-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = random_subset(3, 2 .. 4);
              @b = random_subset(2, 2 .. 5);
              $m = $m[0] + $m[1]*$m[2];
              $b = $b[0]*$b[1]**$m[2];
              $given = Formula("$b[0] $x^$m[0] * ($b[1] $x^$m[1])^$m[2]")->reduce;
              $ans = Formula("$b $x^$m")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-power-to-power-copy">
          <webwork copy="webwork-exponent-product-power-to-power"/>
        </exercise>
        <exercise label="exponent-product-power-to-power-multivar">
          <webwork xml:id="webwork-exponent-product-power-to-power-multivar">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              ($x, $y) = random_subset(2, 'a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real', $y => 'Real');
              ($m[0], $n[0], $m[1], $n[1]) = random_subset(4, 1 .. 9);
              $m[2] = random(2,4);
              @b = random_subset(2, 2 .. 5);
              $m = $m[0] + $m[1]*$m[2];
              $n = $n[0] + $n[1]*$m[2];
              $b = $b[0]*$b[1]**$m[2];
              $given = Formula("$b[0] $x^$m[0] $y^$n[0] * ($b[1] $x^$m[1] $y^$n[1])^$m[2]")->reduce;
              $ans = Formula("$b $x^$m $y^$n")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-product-power-to-power-multivar-copy">
          <webwork copy="webwork-exponent-product-power-to-power-multivar"/>
        </exercise>
        <exercise label="exponent-simplify-two-terms">
          <webwork xml:id="webwork-exponent-simplify-two-terms">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = num_sort(random_subset(3, 1 .. 9));
              $m = $m[0] + $m[2];
              push @m, $m[0] + $m[2] - $m[1];
              do {
                @b = random_subset(4, -9 .. -2, 2 .. 9);
                $b[0] = abs($b[0]);
                $b = $b[0]*$b[1] + $b[2]*$b[3];
              } until ($b != 0);
              $given = Formula("($b[0] $x^$m[0])($b[1] $x^$m[2]) + ($b[2] $x^$m[1])($b[3] $x^$m[3]) ")->reduce;
              $ans = Formula("$b $x^$m")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-simplify-two-terms-copy">
          <webwork copy="webwork-exponent-simplify-two-terms"/>
        </exercise>
        <exercise label="exponent-simplify-two-terms-subtraction">
          <webwork xml:id="webwork-exponent-simplify-two-terms-subtraction">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              @m = num_sort(random_subset(3, 1 .. 9));
              $m = $m[0] + $m[2];
              push @m, $m[0] + $m[2] - $m[1];
              do {
                @b = random_subset(4, -9 .. -2, 2 .. 9);
                $b[0] = abs($b[0]);
                $b = $b[0]*$b[1] - $b[2]*$b[3];
              } until ($b != 0);
              $given = Formula("($b[0] $x^$m[0])($b[1] $x^$m[2]) - ($b[2] $x^$m[1])($b[3] $x^$m[3]) ")->reduce;
              $ans = Formula("$b $x^$m")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-simplify-two-terms-subtraction-copy">
          <webwork copy="webwork-exponent-simplify-two-terms-subtraction"/>
        </exercise>
        <exercise label="exponent-simplify-two-terms-with-power">
          <webwork xml:id="webwork-exponent-simplify-two-terms-with-power">
            <pg-macros>
              <macro-file>contextForm.pl</macro-file>
            </pg-macros>
            <pg-code>
              $x = list_random('a' .. 'k', 'm', 'n', 'p' .. 'r', 't' .. 'z');
              Context("Form")->variables->are($x => 'Real');
              do {
                @m = num_sort(random_subset(3, 1 .. 9));
                @n = random_subset(2,2,3);
                $m[3] = $m[0] + $m[2]*$n[0] - $m[1]*$n[1];
              } until ($m[3] > 1);
              $m = $m[0] + $m[2]*$n[0];
              do {
                @b = random_subset(4, -9 .. -2, 2 .. 9);
                $b[0] = abs($b[0]);
                $b = $b[0]*$b[1]**$n[0] + $b[2]**$n[1]*$b[3];
              } until ($b != 0);
              $given = Formula("($b[0] $x^$m[0])($b[1] $x^$m[2])^$n[0] + ($b[2] $x^$m[1])^$n[1]($b[3] $x^$m[3]) ")->reduce;
              $ans = Formula("$b $x^$m")->reduce;
            </pg-code>
            <statement>
              <p>
                <m><var name="$given"/></m>
              </p>
              <p>
                <var name="$ans" width="16"/>
              </p>
            </statement>
          </webwork>
        </exercise>
        <exercise label="exponent-simplify-two-terms-with-power-copy">
          <webwork copy="webwork-exponent-simplify-two-terms-with-power"/>
        </exercise>
      </exercisegroup>
    </subexercises>

    <subexercises>
      <title>Applications</title>
      <exercise label="simplify-dilated-volume">
        <webwork>
          <pg-code>
            $var = list_random(@ORCCA_variables);
            $scale = random(2,9);
            $scaleWord = numberWord($scale);
            $c = $scale**3;
            Context("Form")->variables->are($var => 'Real');;
            $answer = Formula("$c $var^3");
          </pg-code>
          <statement>
            <p>
              If a cube has sides of length <m><var name="$var"/></m>, its volume is
              <m><var name="$var"/>^3</m>. If we grew the side lengths to make them
              <var name="$scaleWord"/> times as long, the volume would become
              <m>(<var name="$scale"/><var name="$var"/>)^3</m>. Simplify this expression using the
              product-to-power rule.
            </p>
            <p>
              <var name="$answer" width="10"/>
            </p>
          </statement>
        </webwork>
      </exercise>
      <exercise label="simplify-dilated-area">
        <webwork>
          <pg-code>
            $var = list_random(@ORCCA_variables);
            $scale = random(2,9);
            $scaleWord = numberWord($scale);
            $c = $scale**2;
            Context("Form")->variables->are($var => 'Real');
            $answer = Formula("$c $var^3");
          </pg-code>
          <statement>
            <p>
              If a square has side of length <m><var name="$var"/></m>, its area is
              <m><var name="$var"/>^2</m>. If we grew the side lengths to make them
              <var name="$scaleWord"/> times as long, the area would become
              <m>(<var name="$scale"/><var name="$var"/>)^2</m>. Simplify this expression using the
              product-to-power rule.
            </p>
            <p>
              <var name="$answer" width="10"/>
            </p>
          </statement>
        </webwork>
      </exercise>
      <exercise label="simplify-bit-counts">
        <webwork>
          <pg-code>
            Context("Form");
            $answer = Formula("10^38");
          </pg-code>
          <statement>
            <p>
              There are about <m>10^{19}</m> grains of sand on Earth. On average there are also
              about <m>10^{19}</m> atoms in a grain of sand. Using these numbers, how many total
              atoms are there on Earth in grains of sand?
            </p>
            <p>
              <var name="$answer" width="10"/>
            </p>
          </statement>
        </webwork>
      </exercise>
      <exercise label="simplify-star-count">
        <webwork>
          <pg-code>
            Context("Form");
            $answer = Formula("10^20");
          </pg-code>
          <statement>
            <p>
              There are roughly <m>10^8</m> stars in an average galaxy, and there are roughly
              <m>10^{12}</m> galaxies in the observable universe. Using these numbers, how many
              stars are there in the observable universe? Answer using a simplified exponent
              expression, not the actual result.
            </p>
            <p>
              <var name="$answer" width="10"/>
            </p>
          </statement>
        </webwork>
      </exercise>
    </subexercises>

    <subexercises>
      <title>Challenge</title>
      <exercise>
        <webwork>
          <pg-code>
            $m = random(5,15,1);
            $n = $m + random(10,20,1);
            $p = random(50,100,5);
            $answerb = $p-1;
            $q = random(500,2000,50);
            $answerc = $q-1;
          </pg-code>
          <task>
            <statement>
              <p>
                Let <m>x^{<var name="$m"/>}\cdot x^{a}=x^{<var name="$n"/>}</m>, where <m>a</m> is a
                natural number. How many possibilities are there for <m>a</m>?
              </p>
              <p>
                <var name="'1'" width="10"/>
              </p>
            </statement>
            <solution>
              <p>
                Let <m>x^{<var name="$m"/>}\cdot x^{a}=x^{<var name="$n"/>}</m>. Then <m>a</m> must
                be <m><var name="$n"/>-<var name="$m"/></m>. So there is only one possibility for
                <m>a</m>.
              </p>
            </solution>
          </task>
          <task>
            <statement>
              <p>
                Let <m>x^{b}\cdot x^{c}=x^{<var name="$p"/>}</m>, where <m>b</m> and <m>c</m> are
                natural numbers. How many possibilities are there for <m>b</m>?
              </p>
              <p>
                <var name="$answerb" width="10"/>
              </p>
            </statement>
            <solution>
              <p>
                Let <m>x^{b}\cdot x^{c}=x^{<var name="$p"/>}</m>. Many possibilities exist for
                <m>b</m> and <m>c</m>. Since <m>b</m> and <m>c</m> must be natural numbers, let's
                start with <m>b</m> equal to <m>1</m>. Then <m>c</m> must equal <var name="$p-1"/>,
                because <m>x^{1}\cdot x^{<var name="$p-1"/>}=x^{<var name="$p"/>}</m>. Considering
                all of the possibilities for <m>b</m>, notice that <m>b</m> can equal
                <m>1,2,3,\ldots,<var name="$p-1"/></m>. Therefore, there are <var name="$p-1"/>
                possibilities for <m>b</m>. Note that <m>b</m> can't equal <var name="$p"/> because
                that would force <m>c</m> to be zero,which is not a natural number.
              </p>
            </solution>
          </task>
          <task>
            <statement>
              <p>
                Let <m>x^{d}\cdot x^{e}=x^{<var name="$q"/>}</m> where <m>d</m> and <m>e</m> are
                natural numbers. How many possibilities are there for <m>d</m>?
              </p>
              <p>
                <var name="$answerc" width="10"/>
              </p>
            </statement>
            <solution>
              <p>
                Let <m>x^{d}\cdot x^{e}=x^{<var name="$q"/>}</m>. Many possibilities exist for
                <m>d</m> and <m>e</m>. Since <m>d</m> and <m>e</m> must be natural numbers, let's
                start with <m>d</m> equal to <m>1</m>. Then <m>e</m> must equal <var name="$q-1"/>,
                because <m>x^{1}\cdot x^{<var name="$q-1"/>}=x^{<var name="$q"/>}</m>. Considering
                all of the possibilities for <m>d</m>, notice that <m>d</m> can equal
                <m>1,2,3,\ldots,<var name="$q-1"/></m>. Therefore, there are <var name="$q-1"/>
                possibilities for <m>d</m>. Note that <m>d</m> can't equal <var name="$q"/> because
                that would force <m>e</m> to be zero, which is not a natural number.
              </p>
            </solution>
          </task>
        </webwork>
      </exercise>
      <exercise>
        <webwork>
          <pg-macros>
            <macro-file>parserPopUp.pl</macro-file>
          </pg-macros>
          <pg-code>
            $m = random(2,3,1);
            $mraised = 100*$m;
            $p = random(4,5,1);
            $praised = 100*$p;
            $less = DropDown(['&lt;','>','='],0);
            $more = DropDown(['&lt;','>','='],1);
            $equal = DropDown(['&lt;','>','='],2);
            if ($m**$p == $p**$m) {$popup = $equal;}
            else {
              $popup = ($m**$p > $p**$m) ? $more : $less;
            };
          </pg-code>
          <statement>
            <p>
              Choose the correct inequality or equal sign to make the relation true.
            </p>
            <p>
              <m><var name="$m"/>^{<var name="$praised"/>}</m> <var name="$popup" form="popup"/> <m><var name="$p"/>^{<var name="$mraised"/>}</m>
            </p>
          </statement>
          <solution>
            <p>
              Trying to calculate <m><var name="$m"/>^{<var name="$praised"/>}</m> or
              <m><var name="$p"/>^{<var name="$mraised"/>}</m> results in very large numbers. Notice
              that
              <m><var name="$m"/>^{<var name="$praised"/>}=<var name="$m"/>^{(<var name="$p"/>\cdot 100)}</m>.
              Also, notice that
              <m><var name="$p"/>^{<var name="$mraised"/>}=<var name="$p"/>^{(<var name="$m"/>\cdot100)}</m>.
              So to know if
              <m><var name="$m"/>^{<var name="$praised"/>}</m> or
              <m><var name="$p"/>^{<var name="$mraised"/>}</m> is bigger, we only have to compare
              <m><var name="$m"/>^{<var name="$p"/>}</m> with
              <m><var name="$p"/>^{<var name="$m"/>}</m>.
            </p>
          </solution>
        </webwork>
      </exercise>
      <exercise>
        <webwork>
          <pg-code>
            do {
              $a = random(-15,-1,2);
              $b = random(-10,-2,2);
              $coefficient = $a + $b;
            } until ($coefficient &lt; -7);
            $ax = Formula("$a x")->reduce;
            $bx = Formula("$b x")->reduce;
            $complete = Formula("$coefficient*x")->reduce;
            $m = MultiAnswer($ax, $bx)->with(
            singleResult => 1,
              checker => sub {
                my ( $correct, $student, $self ) = @_;
                my ( $astu, $bstu ) = @{$student};
                my ( $acor, $bcor ) = @{$correct};
                if ( $astu == Formula("0")) {Value::Error('None of your values can be zero.')};
                if ( $bstu == Formula("0")) {Value::Error('None of your values can be zero.')};
                if ($astu+$bstu != $complete) {return 0;} else {return 1};
              }
            );
            do {
              $c = random(-15,-1,2);
              $d = random(-10,-2,2);
              $expx = random(15,30,5);
              $coefficient2 = $c + $d;
            } until ($coefficient2 &lt; -7);
            $cexp = Formula("$c*(x**$expx)")->reduce;
            $dexp = Formula("$d*(x**$expx)")->reduce;
            $complete2 = Formula("$coefficient2*(x**$expx)");
            $m2 = MultiAnswer($cexp, $dexp)->with(
            singleResult => 1,
              checker => sub {
                my ( $correct, $student, $self ) = @_;
                my ( $cstu, $dstu ) = @{$student};
                my ( $ccor, $dcor ) = @{$correct};
                if ( $cstu == 0) {Value::Error('None of your values can be zero.')};
                if ( $dstu == 0) {Value::Error('None of your values can be zero.')};
                if ($cstu+$dstu != $complete2) {return 0;} else {return 1};
              }
            );
            $p = list_random(2,3,5,7,11,13,17);
            $exp1 = random(20,50,5);
            $exp2 = random(10,30,10);
            $exp3 = $exp1 + $exp2;
            $expa = Formula("(x**$exp1)")->reduce;
            $expb = Formula("(x**$exp2)")->reduce;
            $num = Formula("$p");
            $complete3 = Formula("$num*(x**$exp3)");
            $m3 = MultiAnswer($num, $expa, $expb)->with(
            singleResult => 1,
              checker => sub {
                my ( $correct, $student, $self ) = @_;
                my ( $numstu, $expastu, $expbstu ) = @{$student};
                my ( $numcor, $expacor, $expbcor ) = @{$correct};
                if ( $numstu == 1) {Value::Error('None of your values can be one.')};
                if ( $expastu == 1) {Value::Error('None of your values can be one.')};
                if ( $expbstu == 1) {Value::Error('None of your values can be one.')};
                if ($numstu*$expastu*$expbstu != $complete3) {return 0;} else {return 1};
              }
            );
          </pg-code>
          <statement>
            <p>
              Fill in the blanks with algebraic expressions that make the equation true. You may
              not use <m>0</m> or <m>1</m> in any of the blank spaces. For example, if
              <m>\mathord{?}+\mathord{?}=8x</m>, then one possible answer is <m>3x+5x=8x</m>.
            </p>
            <p>
              There are infinitely many correct answers. <em>Be creative.</em> After finding a
              correct answer, see if you can come up with a different answer that is also correct.
            </p>
            <p>
              <ol marker="a.">
                <li>
                  <p>
                    <var name="$m" width="6"/><m>{}+{}</m><var name="$m" width="6"/><m>{}=<var name="$complete"/></m>
                  </p>
                </li>
                <li>
                  <p>
                    <var name="$m2" width="6"/><m>{}+{}</m><var name="$m2" width="6"/><m>{}=<var name="$complete2"/></m>
                  </p>
                </li>
                <li>
                  <p>
                    <var name="$m3" width="6"/><m>{}\cdot{}</m><var name="$m3" width="6"/><m>{}\cdot{}</m><var name="$m3" width="6"/><m>{}=<var name="$complete3"/></m>
                  </p>
                </li>
              </ol>
            </p>
          </statement>
          <solution>
            <p>
              <ol marker="a.">
                <li>
                  <p>
                    Think about adding like terms. One possibility is:
                    <m><var name="$ax"/> + <var name="$bx"/> = <var name="$complete"/></m>.
                  </p>
                </li>
                <li>
                  <p>
                    Think about adding like terms. One possibility is:
                    <m><var name="$cexp"/> + <var name="$dexp"/> = <var name="$complete2"/></m>.
                  </p>
                </li>
                <li>
                  <p>
                    Think about multiplying exponents. One possibility is:
                    <m><var name="$num"/> \cdot <var name="$expa"/> \cdot <var name="$expb"/> = <var name="$complete3"/></m>.
                  </p>
                </li>
              </ol>
            </p>
          </solution>
        </webwork>
      </exercise>
    </subexercises>
  </exercises>
</section>

